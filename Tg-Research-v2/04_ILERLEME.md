# TG-Research v2 — İlerleme Günlüğü (build log)

Son güncelleme: **2026-07-13 (6. oturum)** — **WP12 + WP13 İMPLEMENTE EDİLDİ + 3-tur codex review (tüm P1 kapatıldı) + ANA BRANCH'E MERGE'LENDİ (`9735c19`, §4.36).** Kalan finish-line (bağımlılık-kritik sırayla): (1) migration 131+132 izole staging'e apply [HARD PREREQ — kod bu RPC'leri çağırıyor], (2) push, (3) Railway deploy, (4) manuel QA (§4.36'da tam checklist). Backlog 6 madde (blocker değil).** "Long Horizon, Parallel Opus Agent'larla kodla" talimatıyla CRM_IMPLEMENTATION_PLAN.md'nin orchestrator/coding-agent/review-gate deseni uygulandı: 10 slice (3 fazda gerçek paralellik) → 5 paralel codex review batch'i (4'ü kaynak-yarışıyla yarım kaldı, `codex exec resume` ile kurtarıldı) → 3 gerçek P1 (geo-reject race, AI-revize rate-limit yokluğu, client taslağının sunucuya hiç gönderilmemesi) → 2 fix turu (round 1 yüzeysel mitigasyon, round 2 gerçek DB-seviyesi CAS migration'ları 131+132) → final re-review P1=0. Önceki (§4.32-4.34): kullanıcının wizard'ı elle incelemesi 2 boşluk buldu → WP12+WP13 plana eklendi, WP13 4 paralel agent'la TÜM domain'ler denetlenip kapsamı genişletildi, wizard görsel-yeniden-tasarımı commit'lendi (bir eşzamanlı day-loop oturumuyla git-index yarışı yaşanıp güvenle kurtarıldı). Önceki durum aşağıda korunuyor.

Önceki son güncelleme: **2026-07-12 (5. oturum devamı)** — **§4.29'un 2 blocker'ı (ICP kart yığılması + kalibrasyon dead-end) + market:analyze reload-race İKİSİ DE GERÇEKTEN DÜZELTİLDİ VE CANLI KANITLANDI (§4.30)** — kalibrasyon dead-end'in kapanışı bu projenin en kritik bulgusuydu: gerçek bir ICP artık `calibration_state='calibrated'`'a **BAŞLADIĞI ruleset_version'da, sıfır apply-revision detour'uyla** ulaşıyor (SQL ile kanıtlandı, eski bozuk davranışla tam tersi — eski ICP 6 ruleset bump'ı gerektiriyordu). **Wizard görsel yeniden tasarımı da Stage 1 + Stage 2 olarak TAMAMLANDI (§4.31)** — CRM sidebar wizard'da kaldırıldı, faz-bazlı ilerleme, adım-geçiş motion'ı, birleşik AI-bekleme bileşeni, ICP/offer kartı hiyerarşisi + "Detaylar" çekmecesi; Stage 2'de gerçek bir UYGULAMA ÇÖKMESİ bulunup düzeltildi (IcpCard.tsx'in Textarea prop çakışması — düzeltilmeden önce ICP inceleme ekranı HER YERDE çöküyordu). **Kullanıcının "3 domain"i (WP11, wizard UX blocker'ları, görsel yeniden tasarım) ÜÇÜ DE bitti.** Branch: `ssalihyetim/TG-Research` (worktree) — **DİKKAT: branch ÇOK sayıda paralel/otonom oturumun (night-loop/day-loop) ortak çalışma alanı**, `git status` bu oturumla ilgisiz değişiklikler de gösteriyor; sadece research path'lerine dokun.

**SESSION HANDOFF (yeni oturum buradan devam etsin):**
- **Wizard görsel yeniden tasarımı (Stage 1+2) + WP11 polish artık COMMİT'Lİ** — commit `ea1db00` (bkz. §4.34 için TAM önemli bir uyarı: bu commit'in MESAJI yanıltıcı, gerçek içerik doğru). WP12/WP13 artık dosya-çakışması engeliyle karşılaşmadan başlayabilir.
- **Kalan bilinen küçük sorunlar (blocker DEĞİL):** step-14 kalibrasyon banner'ında tarih HTML-entity kaçışlı görünüyor (`7&#x2F;11&#x2F;2026` yerine `7/11/2026`) — kozmetik, §4.31 sonunda not edildi, henüz düzeltilmedi. WP11'in kendi P2/P3 cila listesi (§4.27) hâlâ açık, blocker değil.
- ~~AÇIK, insan kararı gerektiren: `server/src/index.ts`'teki union-merge stash-conflict çözümü~~ ✅ **KAPANDI** (kullanıcı başka bir oturumda çözdü, 2026-07-13 teyit edildi — dosya artık `git status`'ta bile görünmüyor, temiz).
- **(4. oturumdan kalıntı, hâlâ açık):** WP8b round 9 review hiç yapılmadı (§4.22, kullanıcı "codex'i unut" dedi).
- Migration: 116/117/122 WP11'in (117 numara çakışması var, bkz. §4.27 — kozmetik, engelleyici değil); en son gerçek sıradaki numara için `ls supabase/migrations | sort | tail` ile teyit et.
- **Codex CLI pratik notu:** `codex exec`'i `run_in_background:true` ile çağırmak yeterli (Bash tool'un kendi mekanizması) — bir shell `&` İLE BİRLİKTE kullanmayın (çift-arka-plana atma, bildirimi kırar). ~15-20dk sürebilir, sabırlı olun.
- **Review-loop pratik notu (bu oturumda 2 kez tekrarlandı):** bir review round'u gerçek bir bulgu döndürdüğünde, fix-sonrası tek bir `if` ile değil, SINIRLI bir `while (round<=N)` ile tekrar review'e sokmayı unutma — aksi halde bulunan bulgu asla düzeltilmeden bir sonraki faza geçilebiliyor (bu tam olarak §4.30/§4.31'de birkaç kez oldu, orkestratör her seferinde fark edip ek bir kapanış turu başlattı).
- **Tüm WP6/7/8a/8b/9/10/11 + AuthContext.tsx değişiklikleri hâlâ UNCOMMITTED** (kullanıcı `versiyonla` demeden commit atılmaz — proje kuralı).

## 4.36 WP12 + WP13 ANA BRANCH'E MERGE'LENDİ + KALAN İŞLER (finish-line) (2026-07-13, 6. oturum)

**Merge:** `ssalihyetim/TG-Research-wp12-13`'ün 6 commit'i `ssalihyetim/TG-Research`'e merge edildi → merge commit `9735c19` (`--no-ff`). Çakışma SIFIR (yalnız 2 locale dosyası hem merge hem dirty'ydi — foreign `activities.unassigned` day-loop satırı stash-koru/merge/pop ile korundu, disjoint bölge olduğu için pop temiz geçti; `unassigned` satırı restore edildi, JSON geçerli). Merge sonrası `npm run build` YEŞİL. Worktree kaldırıldı (`git worktree remove`), branch silindi (`git branch -d`, fully-merged doğrulandı). Ana branch şu an `origin/ssalihyetim/TG-Research`'ten **8 commit ileride** (WP12/WP13 6 commit + docs f31b1fc + merge 9735c19). Foreign uncommitted dosyalar (coldcall/digest/admin/validation/.gitignore/CLAUDE.md + stash@{1} digest) EL DEĞMEDEN duruyor.

**KALAN İŞLER — WP12/WP13'ü tam canlıya almak için (sırasıyla, bağımlılık-kritik):**

1. **[DB — HARD PREREQ] Migration 131 + 132 izole staging'e (`iehqsuludghrhosgxhnr`) apply.** Merge edilen KOD bu RPC'leri ÇAĞIRIYOR: `research_persist_geo_analysis` (131 v2, CREATE OR REPLACE — eski imzayla aynı, geriye uyumlu), `research_update_hs_code` + `research_persist_market_slice` (132, YENİ fonksiyonlar). **132 uygulanmadan kod deploy edilirse HS-düzenleme ve market:analyze persist'i "function does not exist" ile PATLAR.** Sıra: önce migration, sonra deploy (standart). Prod'a ASLA. Apply öncesi `list_migrations` ile bilinmeyen migration + numara çakışması kontrolü (paralel oturumlar 131/132'yi kapmış olabilir). 132 SECURITY DEFINER + service_role-only GRANT içeriyor — apply sonrası fonksiyonların canlı DB'de var olduğunu probe et.
2. **[PUSH] 8 commit'i `origin/ssalihyetim/TG-Research`'e push** (fetch + divergence kontrolü, force YOK). Kullanıcı kararı.
3. **[DEPLOY] Railway research-api + worker redeploy** (migration apply'DAN SONRA). Worker'da yeni RPC'ler çağrılıyor. Kullanıcı kararı.
4. **[MANUEL QA — kullanıcının kontrol edeceği yerler, staging'de]:**
   - **WP12:** yeni bir test tenant'la login → wizard'a iner → wizard'ı YARIDA bırak (bir ICP calibrate etme) → `/dashboard`'a git → **dashboard'da "kurulumu tamamla" banner'ı görünmeli** + **nav'da "Research" öğesinde mor nokta rozeti** olmalı. Sonra bir ICP'yi calibrated'a getir → banner + rozet KAYBOLMALI. Nav'da yeni **"Company Knowledge" linki** görünmeli, `/research/full`'a gitmeli, tüm rollere açık.
   - **WP13 — Company Knowledge:** `/research/full` → profildeki bir cümleyi düzenle → **"Kaydet" (ICP yeniden ÜRETİLMEDEN kaydolmalı)**. Bir ICP/offer kartında **"AI ile yeniden yaz"** → serbest metin talimat yaz → taslak görünmeli (mevcut vs taslak) → Uygula → kartın kendi Save'iyle kaydolmalı. **ICP kartının "not" alanına yönlendirme yazıp revize et → artık gerçekten AI promptuna ulaşmalı** (eski sessiz-yoksayma bug'ı kapandı).
   - **Geo kaldırma:** wizard adım 8/coğrafya → yanlış eklenen bir ülke chip'ini **"kaldır" (soft-reject)** → kırmızıya dönmeli, adım 9/10 aktif akışından çıkmalı.
   - **HS elle-ekleme:** `/research/full` → HS Codes sekmesi → **elle bir HS kodu ekle** (source=manual, otomatik onaylı) + var olan bir kodun **code/description'ını düzenle**.
   - **Race guard'ları (opsiyonel, derin):** bir hücre analiz edilirken reject et → analiz bitince hücre draft'a DÖNMEMELİ (rejected kalmalı). Bir HS kodunu market:analyze koşarken düzenle → eski kod evidence'ı geri gelmemeli.
5. **[BACKLOG — blocker DEĞİL, §4.35 sonundaki 6 madde]:** ICP not-CAS race, prompt-hardening, `what_they_do` char-limit hassasiyeti, reviseAny COGS admin-panel etiketi, wizard tüm-hücre-rejected auto-skip, geoAnalyze CAS-skip log metni. Gelecek bir turda.

**Not (migration numara riski):** 131/132 bu worktree'de yazıldı; ana branch'e merge edildiğinde repo dosya-numarası olarak 131/132 oldular AMA staging DB'de en yüksek uygulı 130'du — apply anında `ls supabase/migrations | sort | tail` + `list_migrations` ile GERÇEK durumu teyit et, paralel bir oturum 131/132'yi başka bir şey için kapmış olabilir (o zaman renumber gerekir).

## 4.35 WP12 + WP13 İMPLEMENTE EDİLDİ + 3-tur codex review + tüm P1'ler kapatıldı — SHIP, worktree'de commit'li (2026-07-13, 6. oturum, "Long Horizon" otonom koşu)

**Talimat:** kullanıcı — "Bundan sonra kalan işleri implement edilecek olanları belirle. Long Horizon, Parallel Opus Agent'larla kodla, sen Fable Orchestrator'sın, GPT-5-6-SOL-HIGH ile de review'e gönder." CRM_IMPLEMENTATION_PLAN.md §1.1'in ORCHESTRATOR/CODING-AGENT/REVIEW-GATE deseni WP12/WP13'e uygulandı.

**Ortam:** izole worktree `ssalihyetim/TG-Research-wp12-13` (`/Users/salihyetim/orca/workspaces/TG-Core-copy-16.06/TG-Research-wp12-13`, `ssalihyetim/TG-Research`'ten dallandırıldı) — aynı ana `TG-Research` worktree'sinde bugün erken saatlerde yaşanan git-index yarışı (§4.34) tekrarlanmasın diye. Karar doğru çıktı: worktree izolasyonu sayesinde eşzamanlı day-loop CRM oturumuyla (D3/D4/C2-residual koşuyordu) SIFIR çakışma oldu.

**10 slice, 3 faz, gerçek paralellik (Workflow tool, opus 4.8 xhigh implementer):** Faz 1 (W0 onboarding-complete hook + W1 ICP-note bug-fix, paralel) → Faz 2 (X1 dashboard nudge, X2 nav rozeti+Company Knowledge linki, Y2 geo-hücre kaldırma, Y3 HS elle-ekleme, Z1 paylaşımlı AI-revize servisi, 5'i paralel) → Faz 3 (Y1 profil-kaydet+çerçeveleme+AI-revize, Z2 ICP-karta bağlama, Z3 offer-karta bağlama, 3'ü paralel, Z1'e bağımlı). 30 agent, 0 hata. **Ders (yeniden doğrulandı):** eşzamanlı fazlarda `npm run build` bazı slice'ların verify'ında GEÇİCİ olarak KIRMIZI çıktı (başka bir o-anda-yazılan slice'ın dosyası yüzünden) — orchestrator kendi taze `npm run build`'unu TÜM fazlar bitince çalıştırıp yeşili doğruladı, slice'ların kendi (geçici) buildOk:false raporlarına güvenmedi.

**Codex gpt-5.6-sol high review — 5 paralel batch (locale dosyaları dışlandı, gürültü):** **KRİTİK OLAY:** 5 codex süreci GERÇEKTEN paralel çalıştırılınca 4'ü (en küçüğü hariç) "hook: Stop" işaretine hiç ulaşmadan (muhtemelen kaynak yarışı/starvation) yarım kaldı — arka plan task'ı exit code 0 dönmesine rağmen. `codex exec resume <session-id> --last` ile HEPSİ kurtarıldı (session id transkriptlerin başlığında hazır duruyordu) — hiçbir kesif kaybolmadı. **Ders: 5 paralel codex süreci güvenli değil, review'leri ARDIŞIK resume et.** Ayrıca bir batch'te (W0+W1) YANLIŞ dosya yolu (`icp/icpRevise.ts` yerine gerçek `worker/handlers/icpRevise.ts`) yüzünden codex'e EKSİK bir diff verildi, bu da sahte bir P1 üretti ("note SELECT edilmiyor" — halbuki ediliyordu) — doğru yolla yeniden review edilip gerçek bulgu (küçük bir P2, not-CAS race) ortaya çıktı. **Toplam gerçek bulgu: 3×P1 + 7×P2** — (P1) geo-reject race (bir analyze job reject'i geri alabiliyordu), (P1) AI-revize endpoint'inde rate-limit yoktu, (P1) AiReviseButton client'ın kaydedilmemiş taslağını değil DB'deki eski değeri sunucuya gönderiyordu (server zaten DB'den okuyordu, currentValue hiç POST edilmiyordu).

**Fix round 1 (4 paralel track, dosya-ayrık):** 3 P1 + 4 P2 (2'si backlog'a bırakıldı — not-CAS ve prompt-hardening, gerçek bug değil öneri) düzeltildi. **Re-review (round 2) bunların YÜZEYSEL mitigasyon olduğunu, kök-nedeni KAPATMADIĞINI buldu** — rate limiter in-memory/kullanıcı-başına (kalıcı/tenant-çapı değil), iki race hâlâ check-then-act (TOCTOU), atomik DB-seviyesi CAS değil.

**Fix round 2 (3 paralel track, gerçek DB-seviyesi CAS):** migration 131 (`research_persist_geo_analysis` RPC'sine aynı UPDATE içinde `AND status IS DISTINCT FROM 'rejected'` — row-lock-time atomik CAS), migration 132 (`research_update_hs_code` + `research_persist_market_slice`, ikisi de `FOR UPDATE` kilitli, HS kod değişince stale market evidence'ı önleyen atomik CAS + fail-closed staleness check), reviseAny.ts'e kalıcı tenant-çapı günlük tavan (`research_search_log`'u sorgulayan, 200/gün, LLM çağrısından ÖNCE). **Round 3 (final) re-review: P1=0.** Kalan: 1 backlog P2 (COGS-log insert hatası günlük tavanı nadiren fail-open bırakabilir — güvenlik açığı değil, dayanıklılık notu), 1 backlog P3 (worker CAS-skip'i "persisted" diye loglar — kozmetik).

**Migration'lar 131+132 SADECE DOSYA — hiçbir DB'ye APPLY EDİLMEDİ** (izole staging dahil). Uygulama kararı kullanıcıya bırakıldı.

**6 commit, worktree `ssalihyetim/TG-Research-wp12-13`'te, ANA BRANCH'E MERGE EDİLMEDİ:**
- `9a018fb` — ICP revise promptuna müşteri notu (bağımsız bug-fix)
- `bf4ea08` — WP12 (onboarding-complete hook + dashboard nudge + nav rozeti + Company Knowledge linki)
- `7cc9ba5` — geo-hücre kaldırma + migration 131
- `fb27aec` — HS elle-ekleme/düzenleme + migration 132
- `689244d` — WP13 çekirdeği: paylaşımlı AI-revize servisi + ICP/offer/profil kartlarına bağlama + profil bağımsız kaydet + Company Knowledge çerçeveleme
- `e4f7ecd` — tüm yukarıdakilerin i18n anahtarları (TR+EN)

**Backlog'a not düşülen (blocker DEĞİL, gelecek bir turda ele alınabilir):**
- ICP not alanı revizyon persist'inde CAS'a dahil değil (düşük olasılıklı race, migration gerektirir).
- reviseAny.ts prompt yapısı daha da sağlamlaştırılabilir (fence/instruction ayrımı, "tam olarak X ile değiştir" gibi meşru talimatların gereksiz yasaklanması).
- `what_they_do` alanının AI-draft karakter sınırı gerçek PATCH kuralıyla (20KB toplam profil bütçesi) birebir eşleşmiyor — nadir bir Apply-sonrası-400 riski.
- reviseAny COGS'u admin panelde "search cost" ile karışıyor, toplam COGS'a dahil edilmiyor (yanlış-etiketleme, güvenlik değil).
- Wizard adım 9/10: tüm hücreler reddedilirse otomatik "sıfır hücre" atlama `allGeoCells.length` kontrol ediyor, yeni `activeGeoCells.length` değil (dead-end değil, kullanıcı elle Next'e basabilir).
- geoAnalyze.ts worker'ı CAS-skip durumunda hâlâ "persisted" logluyor (DB durumu doğru, yalnız log metni yanıltıcı).

**Sıradaki karar (kullanıcıya sorulacak):** `ssalihyetim/TG-Research-wp12-13` branch'i `ssalihyetim/TG-Research`'e merge edilsin mi (diff review sonrası) — ana branch şu an `f97ae72`'den ilerideki bir noktada (day-loop D3/D4/C2-residual eklemiş), merge zamanı gerçek bir dosya çakışması OLABİLİR mi diye kontrol edilmeli (muhtemelen yok — Layout.tsx/locale gibi paylaşımlı dosyalarda day-loop'un CRM-track değişiklikleri ile bu worktree'nin research-track değişiklikleri farklı satırlarda).

## 4.34 Wizard görsel-yeniden-tasarım + WP11 polish COMMIT'LENDİ — commit ea1db00, mesajı YANILTICI (önemli okuma notu) (2026-07-13, 6. oturum)

**Kullanıcı onayı:** "Şimdi finalize et + commit'le" (bu oturumun AskUserQuestion'ına yanıt). Yapılan: (1) `git diff --stat` ile 19 dirty + 5 untracked dosya tek tek incelendi, wizard-redesign/WP11-polish kapsamındaki 25 dosya (`06_WIZARD_TASARIM.md`, `Layout.tsx`, `WizardShell.tsx`+`.module.css`, `AiWaitScreen.tsx`+`.module.css`, `CalibrationDrawer.tsx`+`.module.css`, `CompaniesPanel.tsx`, `EnrichmentPanel.tsx`, `GeoCellDetail.tsx`, `IcpCard.tsx`, `IcpCountryChips.tsx`, `OfferCard.tsx`, `index.css`, `notifications.tsx`, `useCalibration.ts`, `ResearchFlowPage.tsx`, `comtrade.ts`, `hsMatch.ts`, `marketAnalyze.ts`, `icps.ts`, `en.json`, `tr.json`) seçildi; yabancı-akış dosyaları (`AdminTenantsTab.tsx`, `TenantFormModal.tsx`, `ActivityTimeline*.tsx`, `activityConstants.ts`, `ActivitiesPage.tsx`, `validation.ts`, `dailyDigest*.ts`, `038_daily_digest.sql`, `.gitignore`, `CLAUDE.md` — graphify/CRM/digest kapsamlı, bu oturumun DIŞI) bilinçli DIŞARIDA bırakıldı. (2) `en.json`/`tr.json` içinde TEK bir yabancı satır (`activities.unassigned`, D2/CRM-track'in kendi P2 fix'i) bulundu — `git hash-object`+`git update-index --cacheinfo` ile çalışma-ağacı dosyasına HİÇ dokunmadan (başka bir oturumun satırını kaybetmemek için) yalnız index'e o satır hariç bir blob yazıldı (cerrahi staging, dosya-seviyesinde değil blob-seviyesinde). (3) `npm run build` yeşil (server tsc + client vite, mevcut çalışma-ağacı bütünüyle — yabancı dosyalar dahil — derlendi). (4) `git commit` çalıştırıldı.

**BEKLENMEYEN: eşzamanlı bir "day-loop" oturumu (aynı çalışma ağacında, farklı bir Claude/Opus 4.8 orkestrasyonu) TAM O SIRADA kendi D1-P2 fix'ini (`server/src/routes/tasks.ts`, `as_of` ISO normalizasyonu) `git add`+`git commit` ediyordu.** Timeline kanıtı: commit'imden HEMEN ÖNCE `303a95b` (o oturumun C4-hardening re-review commit'i) zaten görünmüştü; commit'imden SONRA `git log` `ea1db00 "fix(crm): D1 as_of canonicalization... [day-loop]"`'u gösterdi VE benim 25 dosyalık staged içeriğim `git status`'ta ARTIK MODIFIED olarak görünmüyordu. Sonuç: **benim `git commit` çağrım muhtemelen "nothing to commit" ile no-op oldu (başka bir sürecin commit'i benden ÖNCE, benim index'imdeki staged içerikle birlikte gitti)** — `git show --stat ea1db00` bunu doğruluyor: mesaj yalnız `tasks.ts`'i anlatıyor ama diff 25 dosya + 1726 satır ekleme içeriyor, TAM benim seçtiğim dosya listesiyle birebir (yabancı dosyalar YOK, `unassigned` satırı YOK).

**Neden düzeltilmedi (bilinçli karar):** `git commit --amend` bu commit'i YENİDEN YAZARDI — ama commit kısmen (belki tamamen) BAŞKA BİR SÜRECİN eylemi; o süreç `plans/CRM_IMPLEMENTATION_PLAN.md §6`'ya `ea1db00` hash'ini KENDİ D1-P2 kaydı olarak yazacak/yazmış olabilir. Amend hash'i değiştirir → o kayıt stale/yanlış hash'e işaret eder. Proje kuralı da zaten commit amend'i yasaklıyor ("her zaman yeni commit, amend değil"). **Karar: commit OLDUĞU GİBİ bırakıldı, yalnız bu not yazıldı.** İçerik doğru (dosya seçimi ve `unassigned` hariç tutma iki kez `git diff --stat`/`git diff` ile doğrulandı) — yalnız `ea1db00`'in commit MESAJI title'ının aksine hem D1-P2 fix'ini HEM DE bu oturumun tüm wizard-redesign/WP11-polish diff'ini taşıyor.

**Ders (gelecek oturumlar için):** bu branch şu an GERÇEKTEN eşzamanlı birden fazla otonom oturumun aktif `git add`/`git commit` çalıştırdığı paylaşımlı bir çalışma ağacı — yalnız "unstaged yabancı dosyalara dokunma" yetmiyor, **`git add` ile `git commit` arasında da bir yarış penceresi var.** Daha güvenli bir gelecek deseni: `git add` hemen ardından `git commit` arasındaki süreyi minimize et (aradaki her ekstra komut/kontrol yarış penceresini büyütür); mümkünse `git commit -- <dosya listesi>` gibi tek-adımlı bir form tercih et (bu repo'nun git sürümünde mevcutsa) ya da commit sonrası MUTLAKA `git show --stat <yeni-HEAD>` ile "gerçekten benim commit'im mi, doğru dosyalar mı" diye doğrula (bu oturumda tam olarak bunu yapıp sorunu YAKALADIK).

## 4.33 WP13 tam domain denetimi — 4 paralel agent, kod-kanıtlı — **TEŞHİS BİTTİ, WP13 kapsamı genişletildi** (2026-07-13, 6. oturum)

**Bağlam:** kullanıcı §4.32'nin WP13 taslağını onaylamadan önce net bir şart koydu — "Bunların hepsi tek tek geliyor mu?... Bunların hepsinin müşteri tarafından onaylanması, manuel veya AI'ye prompt yazdırarak yeniden düzenlenmesi ve müşterinin önüne çıkarılması olmak zorunda... Her birinden emin ol, benim düşünmediğim eksik... alan varsa onu da ekle." 4 paralel araştırma agent'ı (graphify-first, dosya:satır kanıtlı) wizard'ın 8 veri domain'ini tek tek denetledi: profil (adım 1-6), ölçek hedefi (adım 17), HS kodları (adım 7), pazar kanıtı (adım 8), alt-ICP'ler (adım 8+14), kalibrasyon (adım 11-14), coğrafya (adım 8+10), satış açıları/offer (adım 15-16). Her domain 5 kriterle test edildi: tek-tek mi gösteriliyor, açık bir onay aksiyonu var mı, elle düzenlenebiliyor mu, serbest-metin AI-promptla revize edilebiliyor mu, wizard dışında kalıcı bir yüzeyi var mı. Tam matris + kararlar `05_SONRAKI_ADIMLAR.md`'nin WP13 bölümüne yazıldı; burada yalnız kanıt/kaynak özeti.

**Domain-domain kısa bulgular (tam dosya:satır kanıtları agent raporlarında, transcript'te):**
- **Profil (adım 1-6):** 6 ayrı ekran doğrulandı (`ResearchFlowPage.tsx:2075-2446`), her adım kendi `PATCH /research/projects/:id`'ini wizard İÇİNDE anında ateşliyor (`saveStepMut`, `:661-696`). Ama `/research/full`'da (`ResearchPage.tsx:284-330`) yalnız website/özet/ürün/target_markets/exclusions var VE yalnız "ICP Üret" mutation'ının yan etkisiyle kaydediliyor (`:122-172`); **farklılaştırıcılar (moq/lead_time/certifications/capacity/references/languages) ve lookalike_customers/hedef-pazar-ipuçları `/research/full`'da SIFIR referans** — wizard dışında tamamen görünmez. Onay semantiği yok — `research_projects`'te yalnız proje-seviyeli faz enum'u var (`projects.ts:19-21`), profil-seviyeli/alan-seviyeli status YOK. `profile:revise` job type'ı yok (`handlers/index.ts:9-30`'da yalnız `profile:crawl`).
- **Ölçek hedefi (adım 17):** kendi PATCH'i var (`ResearchFlowPage.tsx:1906-1938`), ama `/research/full`'da VEYA `ResearchAdminPage.tsx`'te SIFIR referans — wizard dışında hiçbir yerde görünmüyor/düzenlenemiyor.
- **HS kodları (adım 7, ham adım 22):** `HsCodeCandidates.tsx` TOPLU liste render ediyor (`:32-66`), tek-tek DEĞİL — ilk varsayımın aksine. `PATCH /research/hs/:id` yalnız `status` kabul ediyor (`hs.ts:31-33`) — kod/açıklama düzenlenemiyor, elle yeni kod ekleme route'u yok. `/research/full`'un HS Codes sekmesi (`ResearchPage.tsx:221-244`, `HsCodesPanel.tsx`) AYNI bileşeni kullanıyor + geçmiş görünümü ekliyor — bu doğrulandı, WP11'in iddiası doğru.
- **Pazar kanıtı (adım 8, ham 23):** kendi başına onaylanmıyor — `GeoCellDetail.tsx:543-576`'da salt-okunur rozet olarak coğrafya onayının İÇİNE gömülü, doğru tasarım (ham Comtrade sayıları değiştirilmemeli). Yalnız `market_notes` (coğrafya hücresinde) serbest metin.
- **Alt-ICP'ler (adım 8, tekrar adım 14):** tek-tek doğrulandı (`icpCardIndex` cursor, `IcpCard.tsx`). **2 ölü alan:** `neutral_signals` (tip'te var, UI'da yok) + `code` (salt-okunur badge, hiç set edilmiyor). **En ciddi tekil bulgu: `IcpCard.tsx:196-201`'deki "not" alanı düzenlenebilir ama `icpRevise.ts:56`'nın kolon seçimi `note`'u İÇERMİYOR** — müşteri revizyon için yazdığı yönlendirme metni AI promptuna hiç ulaşmıyor, sessizce yok sayılıyor. `/research/full`'da ICP'ler TOPLU GRID (`ResearchPage.tsx:366`) — wizard'ın tek-tek kalıbından farklı UX.
- **Kalibrasyon (adım 11-14, gerçek adım numaraları — 15 DEĞİL, o offer üretimi):** wizard'da tek firma/ekran (`:2972-3153`), ama `/research/full`'da her ICP kartının kendi "Kalibrasyon" butonu TOPLU TABLO Drawer'ı açıyor (`CalibrationDrawer.tsx:141-199`) — aynı işlev, farklı etkileşim deseni. Revizyon önerisi HER ZAMAN diff olarak gösteriliyor, asla otomatik uygulanmıyor (`:3316-3339`) — doğru davranış.
- **Coğrafya (ülke adım 8, hücre adım 10):** hücreler tek-tek (`GeoCellDetail.tsx`), CAS'lı onay (`geographies.ts:424-494`), zengin elle-düzenleme (`estimate`, `local_terms`, sinyaller, `buyer_titles`, sertifikalar, `market_notes` — `:373-421`). **Silme YOK — yalnış eklenen bir ülke/hücre çıkarılamıyor, kod içinde bilinçli olarak belgelenmiş** (`IcpCountryChips.tsx:4-6`). Ölü alan: `note`.
- **Satış açıları/offer (adım 15-16):** tek-tek doğrulandı, CAS'lı onay + red (silme yok, `offers.ts:341-342`'de bilinçli not). Zengin elle-düzenleme (pain_hypothesis/value_prop/proof_points/objections/language). **AI-promptla revizyon HİÇ YOK — ICP'nin yapısal revize'inin bile karşılığı yok.** Ölü/görünmez alanlar: `note`, `geo_id`, ve `ai_draft` (client tipinde bile tanımlı değil).

**Sonuç:** WP13'ün ilk taslağı (§4.32) gerçek ama EKSİKTİ — yalnız profil+ICP+offer+HS'in bilinen boşluklarını kapsıyordu. Tam domain taraması sonrası kapsam: (1) serbest-metin AI-promptla revizyon SIFIR domain'de var, WP13'ün asıl işi "birkaç alana ekle" değil "yetenek sıfırdan inşa + 5 domain'e bağla"; (2) ICP-not-yok-sayılıyor bug'ı WP13'ten bağımsız, hemen düzeltilebilir; (3) HS kodları toplu liste olması + ICP/kalibrasyonun `/research/full`'da toplu-tablo/grid'e dönüşmesi nedeniyle "tek-tek" deseni YAMALI — Company Knowledge'ın tek bir tutarlı deseni dayatıp dayatmayacağı açık karar; (4) coğrafya silme + HS elle-ekleme gerçek boşluklar, "elle revize" talebinin parçası olabilir; (5) profilin 2 alt-alanı + `scale_target` wizard dışında SIFIR yüzeye sahip; (6) yeni bir veri domain'i ihtiyacı BULUNAMADI — mevcut 8 domain satış-hunisi girdisi olarak kavramsal bütünlüğe sahip (buyer_titles zaten karar-verici alanını karşılıyor). `05_SONRAKI_ADIMLAR.md`'nin WP13 bölümü bu bulgularla tam matris + genişletilmiş kapsam + yeni açık kararlarla YENİDEN YAZILDI.

## 4.32 İnsan wizard incelemesi → 2 boşluk teşhis edildi, WP12+WP13 plana eklendi — **TEŞHİS BİTTİ, İMPLEMENTASYON YOK** (2026-07-13, 6. oturum)

**Bağlam:** kullanıcı wizard'ı elle inceledi (otomatik testler son adıma kadar demo bir firma bırakmıştı) ve 2 bulgu getirdi: (1) "Research wizardı TgCore arayüzünde herhangi bir butonla veya widgetla göremiyoruz. Wizard tamamlanmadan dashboard'a geçildiğinde tekrar nasıl geçilecek planda bu belli mi?" (2) "daha önce tanımlanmış bilgi alanları SADECE wizard üzerinden değil company knowledge diye göstereceğimiz bi' alan üzerinden de elle revize ve ai'a promptla revize edilebilir olmalı." Talimat: bunları plana göre değerlendir, planda yoksa ekle.

**Bulgu 1 doğrulaması (kod okunarak, tahmin değil):** `RootRedirect.tsx` yalnız `/` yolunda çalışıyor ve `hasProject = research_projects.length > 0` sorusuna bakıyor — "tamamlandı mı" değil "var mı". Wizard adım 1'de bir proje satırı oluşuyor, bu da `hasProject`'i o andan itibaren sonsuza kadar `true` yapıyor: yarım kalmış bir kurulumdan `/dashboard`'a dönen müşteri bir daha ASLA otomatik `/research`'e düşmüyor. `Layout.tsx:134`'te `/research` için sabit bir nav linki VAR (kullanıcının "hiçbir buton/widget yok" ifadesi teknik olarak birebir doğru değil) ama link "kurulumunuz yarım" demiyor, sıradan bir modül linki gibi duruyor — hiçbir nudge/rozet yok. Wizard'ın kendisi doğru adımdan resume ediyor (`flow_state`, WP6'dan beri kanıtlı çalışıyor) — sorun sadece geri-dönüş girişinde. Plan araması: bu konu `06_WIZARD_TASARIM.md`'de (salt görsel kararlar) ve `05_SONRAKI_ADIMLAR.md`'nin hiçbir yerinde YOKTU — WP6'nın kendisi bile yalnız "yeni tenant → wizard'a iniş"i kapsıyordu, "yarım kalan tenant nasıl geri döner"i değil.

**Bulgu 2 doğrulaması (bir araştırma agent'ına delege edildi, graphify-first + dosya:satır kanıtlı):** sanılanın aksine altyapının BÜYÜK kısmı zaten var. `IcpCard.tsx`/`OfferCard.tsx` `/research/full`'da zaten tam inline-edit + wizard'dan bağımsız `PATCH /research/icps/:id` / `PATCH /research/offers/:id` ile çalışıyor; WP11 aynı kalıcı-düzenleme desenini HS Codes'a da SHIP etmişti (§4.28). Üç gerçek boşluk kaldı: (a) şirket profili (`research_projects.profile`) tek başına kaydedilemiyor, yalnız "ICP Üret" yan etkisiyle kaydediliyor; (b) `/research/full` `Layout.tsx` navItems'ında YOK, hiçbir yerden linklenmiyor (rol kilidi de yok — herkes URL bilse görür, ama keşfedilemiyor); (c) `POST /research/icps/:id/revise` var ama serbest metin ALMIYOR, yalnız kalibrasyon puanlama satırlarından yapısal bir prompt kuruyor — "buraya bir talimat yaz, AI yeniden yazsın" hiçbir alanda yok. `useCalibration.ts`'in kendi belgelediği tasarım kararı (revizyon servisi bilinçli olarak wizard adım-state'inden bağımsız) bu üçüncü boşluğu doldurmayı mimari bir kırılma değil, kanıtlanmış bir kalıbın tekrarı yapıyor.

**Aksiyon:** `05_SONRAKI_ADIMLAR.md`'ye WP11'in hemen ardına **WP12** (wizard geri-dönüş keşfedilebilirliği: dashboard nudge + nav rozeti + "tamamlandı" eşiği ürün kararı) ve **WP13** (Company Knowledge: profil bağımsız kaydet + `/research/full`'u linkle/çerçevele + yeni serbest-metin AI-promptla revizyon endpoint'i) eklendi — ikisi de henüz implemente edilmedi, yalnız planlandı. Her ikisi de birkaç açık ürün kararını (tamamlanma eşiği; yeni route mu yeniden-çerçeveleme mi; AI-revizyon hangi alanlarda önce; COGS müşteri kotasından mı admin-only mı) plana bilerek AÇIK bıraktı — kod yazmadan önce kullanıcı onayı gerekiyor. **İKİSİ DE `Layout.tsx`/`ResearchPage.tsx`/`IcpCard.tsx`/`OfferCard.tsx`/`icps.ts` gibi şu an UNCOMMITTED olan wizard görsel-yeniden-tasarım dosyalarına dokunacak — başlamadan önce o diff commit'lenmeli.**

## 4.31 Wizard görsel yeniden tasarımı — Stage 1 + Stage 2 — ✅ **BİTTİ (Stage 2'de gerçek bir çökme bulunup düzeltildi)** (2026-07-12, 5. oturum, UNCOMMITTED)

**Bağlam:** kullanıcı "TG-research wizard'ının frontend'i EXTREMELY GOOD olmalı" dedi + gerçek araştırma (Typeform tasarım tarihi, 2026 SaaS onboarding pattern'leri, AI loading-state pattern'leri, kart-onay UX) + mevcut ekran görüntülerinin incelenmesi istedi. Plan `Tg-Research-v2/06_WIZARD_TASARIM.md`'ye yazıldı — teşhis: yapı taşları iyi ama görsel katman tamamen varsayılan Mantine, VE wizard hâlâ TAM CRM sidebar'ının içinde render ediliyordu (ürün kararı "TG-Research = TG-Core'un İLK ADIMI"yla çelişiyordu).

**Stage 1 (dosya çakışması riski SIFIR olan alan, WP11-fix track'leri sürerken paralel başladı):** `WizardShell.tsx` (CRM sidebar wizard'da gizlendi — `Layout.tsx`'te `/research` route'una özel bypass, `/research/full` AYNEN korundu; 7 faz'lı ilerleme göstergesi düz "Adım X/23" yerine; kart dikeyde ortalandı), `OfferCard.tsx` (hiyerarşi: insan-okur sentez önde, ham `angle_code` küçük ikincil etiket, ham editör kapalı-varsayılan "Detaylar" çekmecesinde), `CompaniesPanel.tsx`/`EnrichmentPanel.tsx` (hiyerarşi + boş-durum kopyası + gated-buton sebep metni). **2 tur review + 1 gerçek accessibility bulgusu** (OfferCard'ın giriş animasyonu `prefers-reduced-motion` kullanıcılarında kartı kalıcı olarak görünmez bırakıyordu — Mantine'in `useReducedMotion` hook'unun ilk render'da her zaman `false` dönmesinden kaynaklanan bir timing hatası) → düzeltildi + gerçek Playwright taramasıyla kanıtlandı (`prefers-reduced-motion: reduce` emüle edilip `getComputedStyle().opacity` doğrudan ölçüldü: `1`). Bağımsız olarak sonuç tablosundaki skor rozetlerinin `"9.."` diye kırpıldığı bulundu (pixel-zoom ile kanıtlandı) — kök neden bizzat bir headless-Chrome repro'suyla ampirik olarak doğrulandı (Mantine Badge'in `overflow:hidden` otomatik min-genişliği sıfırlıyor), `miw` ile düzeltildi.

**Stage 2 (Track A/B bitince başladı — adım-geçiş motion'ı, birleşik AI-bekleme bileşeni, ICP kartı hiyerarşisi, kalibrasyon çekmecesi cilası, coğrafya/ülke chip cilası):** yeni `AiWaitScreen.tsx` (9 ayrı ad-hoc bekleme ekranını TEK bileşende birleştirdi — her job'ın GERÇEK sunucu-taraflı aşama verisiyle, uydurma aşama YOK, `hsMatch.ts`'in gerçek `heartbeat({stage:...})` çağrılarına karşı nokta-nokta doğrulandı) + `WizardShell`'in adım-geçiş motion'ı (Mantine `Transition`, mevcut per-step remount'a binen) + `IcpCard.tsx`/`GeoCellDetail.tsx` hiyerarşi+çekmece cilası. **3 review turu + canlı doğrulama `passed:false` döndü — HAKLI OLARAK:** `IcpCard.tsx`'in yeni "Segment" alanı `autosize` + `styles.input.minHeight`'ı birlikte kullanıyordu, bu kombinasyon react-textarea-autosize'da senkron bir `throw` — **ICP inceleme ekranı adım 8'de, adım 14'te VE `/research/full`'un ICP sekmesinde HER SEFERİNDE tüm uygulamayı üst-seviye ErrorBoundary'ye düşürüyordu.** Ayrıca 2 review turunun kaçırdığı bir motion çift-katman kalıntısı (`GeoCellDetail`/`IcpCountryChips`'in kendi giriş animasyonu `WizardShell`'inkiyle çakışıyordu) ve canlı bulunan bir toast-yığılma hatası (art arda offer onayı aynı toast'u N kez üst üste yığıyordu, `Mantine notifications`'ın kendi `id`-tabanlı dedup mekanizmasıyla düzeltildi).

**Kapanış turu (3 bulgu, hepsi düzeltildi + gerçek tarayıcıyla YENİDEN kanıtlandı):** çökme düzeltmesi (`minHeight` kaldırıldı, `autosize`+`minRows` yeterliydi) 3 konumda da (adım 8, adım 14, `/research/full`) ekran görüntüsü + sıfır konsol hatasıyla doğrulandı — hatta Segment alanına gerçekten yazı yazılıp çökmediği test edildi. Motion çift-katmanı `OfferCard.tsx`'te de (adım 16) bulunup akıllı bir `skipEntranceAnimation` prop'uyla düzeltildi (kör bir silme değil — adım 16'ya TAZE varışla adım-içi kart gezinmesi ayırt edildi, ikincisinin kendi animasyonu HÂLÂ gerekli). ICP kart yığılması regresyonu YOK (kart artık render olabildiğine göre yeniden sayıldı: her geçişte tam 1). Toast testi: 3 hızlı onay → tam 1 toast (yaklaşık 20 kez örneklendi, hep 1).

**Yan not (insan dikkatine):** canlı doğrulama sırasında BAŞKA bir oturumun `server/src/index.ts`'e bıraktığı ÇÖZÜLMEMİŞ git-conflict marker'ları (`<<<<<<< Updated upstream` / `=======` / `>>>>>>> Stashed changes`, günlük özet e-postası stash-pop'undan) bulundu — dev server bunlar yüzünden derlenemiyordu. Ajan doğru davrandı: iki tarafı da (imapPolling + dailyDigest scheduler'ları) union-merge ile birleştirdi, git add/commit/stash/reset/checkout YAPMADI, UNCOMMITTED bıraktı, açıkça bayrakladı. **Bir insan bu union-merge'ü teyit edip commit etmeli veya geri almalı** — bu bizim değişikliğimiz değil.

**Bilinen küçük kalıntı (blocker değil):** step-14 kalibrasyon banner'ında tarih HTML-entity kaçışlı (`7&#x2F;11&#x2F;2026`) — kozmetik, önceden var, bu turun kapsamı dışında.

## 4.30 §4.29'un 2 blocker'ı + market:analyze reload-race — ✅ **GERÇEKTEN DÜZELTİLDİ, canlı SQL kanıtıyla** (2026-07-11/12, 5. oturum, UNCOMMITTED)

**Kalibrasyon dead-end (en kritik bulgu) — kök neden teşhisi:** `mark-calibrated` endpoint'i (`POST /research/icps/:id/mark-calibrated`) ve onun client mutation'ı ZATEN TAMAMEN VARDI VE ÇALIŞIYORDU — backend'e HİÇ dokunulmadı. Gerçek hata %100 client-side'dı: `ResearchFlowPage.tsx`'te adım 13'e geri-bildirimle varılınca `icp:revise`'ı OTOMATİK ateşleyen bir `useEffect` vardı — bu, sunucunun kendi mark-calibrated kapısının reddettiği TEK koşulu (bekleyen bir revizyon önerisi) her seferinde yaratıyordu, müşteriyi apply-revision'a zorluyordu (bu da ruleset bump + approved→draft yapıp az önceki feedback'i geçersiz kılıyordu) → yeni resample → yeni auto-revise → sonsuz döngü. **Fix (stratejist direktifi):** revizyon önerisi artık salt kullanıcının açıkça istediği bir eylem ("Revizyon öner" butonu); adım 13'e YENİ bir "Mantığı onayla" eylemi eklendi, doğrudan var olan `calib.markMut`'ı çağırıyor.

**5 fix + review round'u** (her round gerçek bir bulgu buldu): round 1 P1 (adım 23 tamamlama efekti sonsuz retry riski) → düzeltildi; round 2 6 bulgu (adım 23 fix'i BAŞKA bir stuck-forever riski açtı + kenar-durum boşlukları) → düzeltildi; round 3 1 P1 CONFIRMED (Propose-revision butonu, mark-calibrated'ın ardından gelen PATCH BAŞARISIZ olursa yeniden etkinleşip ZATEN kalibre edilmiş bir ICP'ye ücretli revizyon job'ı ateşleyebiliyordu) + 2 P2 → düzeltildi (round 4-5). **Ayrı bir kapanış turu** ICP kart yığılmasının GERÇEK kök nedenini buldu (5 round boyunca kaçırılmış): `IcpCard` ve `IcpCountryChips` kardeş bileşenler AYNI React `key`i (`currentIcp.id`) paylaşıyordu — React'ın çakışan-key davranışı eski kartı DOM'da bırakıyordu (bir race condition DEĞİL). `IcpCountryChips`'e ayrı bir `chips-${id}` key'i verilerek düzeltildi.

**Canlı SQL kanıtı (bu turun en önemli çıktısı):** gerçek bir ICP (proje `ebfffb07`, ICP `6f122097`) 11 gerçek firma puanlamasından (gerçek DOM tıklamaları) geçip `calibration_state='calibrated'`'a **`ruleset_version=1`'de** (başladığı sürüm) ulaştı — `revision_job_id=NULL`, sıfır `icp:revise` job'ı, sıfır apply-revision. Kontrast: eski bozuk ICP `5b8ffaa3` aynı duruma ulaşmak için 6 ruleset bump'ı gerektirmişti. market:analyze reload-race'in her iki yarısı da (yinelenen job önleme + sonsuz-retry önleme) canlı kanıtlandı (localStorage bayrağı + zorlanmış PATCH-hata testi).

## 4.29 Paralel UX incelemesi — mevcut wizard'da 2 BLOCKER bulundu (WP11 ile alakasız) (2026-07-11, 5. oturum, UNCOMMITTED)

**Bağlam:** kullanıcı WP11 arka planda koşarken "paralel bir agentla mevcut wizard yapısının UX'ini test et, mevcut workflow'u bozmadan" dedi. `design-review` agent'ı gerçek bir tarayıcı ile izole DB'de uçtan uca gezindi (2 deneme — ilki WP11'in `ResearchFlowPage.tsx`'i canlı düzenlemesiyle HMR çakışmasına uğradı, ikinci tur "WP11'e ait ekranları atla" talimatıyla tamamlandı).

**Neyin iyi çalıştığı (dokunma):** adım 1 kurulum formu, adım 2-3 crawl+özet onayı (Festo → Almanya doğru tespit), adım 4 ürün chip listesi, adım 5 farklılaştırıcılar, kalibrasyon puanlama kartı ("en iyi ekran"), ölçek/kredi ekranı ("en fazla 41 kredi"), suppress onay modalı, orkestratör'ün SearXNG-yok hatasında zarif çöküş, kapanış ekranı.

**BLOCKER 1 — ICP kart carousel yığılıyor.** Adım 10/23'te (fresh tenant) "İleri" tıklayınca bir sonraki kart öncekini DEĞİŞTİRMİYOR, ALTINA EKLENİYOR — 4. kartta ekranda 3 tam kart (sinyal chip'leri + slider'lar + butonlarıyla) aynı anda görünüyor. Normal-hızlı gerçek tıklamalarla %100 tekrarlanabilir, ürünün "teker teker" temel ilkesini müşterinin GÖRDÜĞÜ İLK gerçek karar ekranında kırıyor.

**BLOCKER 2 (en ciddi) — FAZ 3 kalibrasyon döngüsünün ULAŞILABİLİR ÇIKIŞI YOK.** Kök neden `client/src/lib/useCalibration.ts` + `ResearchFlowPage.tsx`'te doğrulandı: "Mantığı onayla" butonuna yalnız başarılı bir "Uygula" (revizyon uygula) sonrası ulaşılıyor — ama uygulama her seferinde `ruleset_version`'ı bump'lıyor ve ICP'yi draft'a düşürüyor, bu da o ekrana ulaşmayı haklı çıkaran feedback'i ANINDA geçersiz kılıyor → server 400 "Rate at least one company at the current ruleset" → yeni resample (gerçek, ücrete-yakın bir arama job'ı) → 1 firma bile puanlanınca YENİ bir revizyon önerisi otomatik üretiliyor → döngü baştan. Canlı 5 tam round'da tekrarlandı (`ruleset_version` 1→6, ~40 dakikada), VE **izole DB'nin TAMAMI sorgulandı: hiçbir ICP, hiçbir projede, `calibration_state='calibrated'`'a HİÇ ulaşmamış.** Bu, tam olarak WP8b'nin kullanıcı talebiyle DURAKLATILAN round 9 review'inin yakalayacağı türden bir bulgu (§4.22) — gerçek bir kullanıcı normal kullanımla scale/orchestration/results/suppress'e ÖNDEN asla ulaşamıyor demek.

**Yüksek öncelik:** AI-üretilen metin (kalibrasyon kanıtı, revizyon diff'i, offer/angle kartı) Türkçe projede bile İNGİLİZCE geliyor (4 ayrı ekranda doğrulandı) — ürünün "sizin dilinizi konuşuyoruz" vaadini bozuyor. Ham internal alan adları müşteri metnine sızıyor ("negative_signal 'Is itself a lens manufacturer...'", offer kartında `PREMIUM-BRAND-PULL` rozeti en belirgin öğe olarak).

**Orta öncelik / öneri:** kart ekranları "sade + detay çekmecesi" yerine tüm ham editörü tek seferde gösteriyor; gated butonlar (son kalibrasyon kartında "İleri", re-approve'da "Mantığı onayla") precondition sağlanmadan tıklanabilir kalıp SONRA toast hatası veriyor (disabled+sebep daha iyi olur); suppress sonrası sayaçlar bayatlıyor (üst başlık güncellenmiyor, sonraki ekrana yanlış sayı taşınıyor); revizyon bekleme ekranı alt-aşama anlatımı yok (bir önceki resample ekranı var).

**Durum:** kod DOKUNULMADI (salt UX incelemesi). Kullanıcıya iki blocker + öncelik sırası raporlandı; WP11'in aktif dosyalarıyla (`ResearchFlowPage.tsx`, ICP/kalibrasyon bileşenleri) çakışma riski nedeniyle **fix'e başlama kararı kullanıcıdan bekleniyor** (WP11 workflow'ları bitene kadar mı, yoksa paralel mi). Tam rapor + ekran görüntüleri: `/private/tmp/claude-501/.../scratchpad/shots/`.

## 4.28 WP11 devamı — Kalıcı düzenleme yeri: `/research/full`'a "HS Codes" sekmesi — ✅ **BİTTİ → build+review×2+canlı-doğrulama → SHIP** (2026-07-11, 5. oturum, UNCOMMITTED)

**Bağlam:** paralel bir UX-tarama agent'ı gerçek bir boşluk buldu — ürünler/sub-ICP'ler/angle'lar wizard'dan SONRA da `/research/full`'da kalıcı, düzenlenebilir bir sekmede yaşıyor; HS kodlarının böyle bir yeri YOKTU, yalnız wizard adım 7'ye gömülüydü. Kullanıcı onayıyla WP11 kapsamına eklendi (`05_SONRAKI_ADIMLAR.md` WP11'e "Kalıcı düzenleme yeri" maddesi olarak yazıldı) ve WP11'in geri kalanından bağımsız, ayrı bir workflow'la (dosya çakışmasını önlemek için) inşa edildi.

- **Yapılan:** yeni `client/src/components/research/HsCodesPanel.tsx` (mevcut `OffersPanel.tsx` kalıbını birebir izler: proje seçici, `GET /research/hs?project_id=`, "HS eşleme çalıştır" butonu → `POST /research/hs/match` + job polling), mevcut `HsCodeCandidates.tsx`'i HİÇ DEĞİŞTİRMEDEN (zaten saf `{candidates, onChanged}` presentational bileşeniydi, wizard/flow-state bağımlılığı yoktu) render eder + onaylı/reddedilmiş kodlar için salt-okunur bir liste. `ResearchPage.tsx`'e ICP Master'ın hemen yanına yeni bir Tab eklendi, yalnız-ekleme (mevcut hiçbir tab'a dokunmadı). Yeni migration/route/job type GEREKMEDİ — salt mevcut `hs.ts` route'larının client-side kablolanması.
- **Review süreci gerçek bulgu buldu:** round 1 codex FAIL verdiktiyle döndü (2×P2: gerçek ESLint hataları + `jobStatus` polling başarısız olursa sonsuza dek "matching" takılı kalma riski) — düzeltildi; round 2 codex APPROVE (3 minor non-blocking: tenant-switch cache bayatlığı, proje dropdown 20 limitine takılıyor, listing-hata durumu sessizce "hiçbir şey yok" gösteriyor — hepsi `OffersPanel.tsx`'te zaten var olan, miras alınan sınırlar, bu diff'e özgü regresyon değil).
- **Canlı doğrulama (gerçek tarayıcı + gerçek DB):** mevcut smoke fixture'ı (`WP11-COMTRADE-SMOKE`, 3 HS kodu) üzerinde gerçek Reddet/Onayla tıklamaları → DB'de `status` gerçekten değişti (SQL ile doğrulandı) → fixture aynen geri alındı; wizard'ın kendi adım 7'sinin (aynı bileşen, aynı prop şekli) regresyon YAŞAMADIĞI hem grep hem canlı wizard-state enjeksiyonuyla doğrulandı.
- **Süreç notu (dikkat):** build ajanı görev ortasında yanlışlıkla `git stash`/`git stash pop` çalıştırdı (paylaşılan branch'te "yıkıcı git komutu çalıştırma" talimatını ihlal) — kendi raporuna göre zararsız no-op'tu; **ben (orkestratör) bağımsız doğruladım**: `git stash list` yalnız bu göreve ait olmayan 4 eski stash gösteriyor, repo genelinde merge-conflict marker'ı yok, `git status` beklenen tam dosya seti ile tutarlı. Veri kaybı yok, ama tekrarlanmaması gereken bir ihlal.

## 4.27 WP11 — TradeMap→UN Comtrade pivotu: HS eşleme + dünya ithalatı + bilateral ihracat kanıtı — ✅ **BİTTİ → 2 workflow turu (build+2-bug-fix+3-review-round+canlı-smoke) → SHIP** (2026-07-11, 5. oturum, UNCOMMITTED)

**Bağlam:** kullanıcı talebiyle WP11 TradeMap'ten (eski açık konu: ücretli plan/erişim) UN Comtrade public API'ye revize edildi (bkz. `05_SONRAKI_ADIMLAR.md` WP11 — TradeMap'in `preview` uç noktası key'siz çalıştığından o açık konu KALKTI). Fable strateji kararı: Comtrade FİRMA-seviyesi veri vermez, yalnız ülke×ülke agregat — WP11'in işi doğru HS kodunu bulup EN GÜÇLÜ aday ülkeleri (dünya ithalatı + satıcı-ülkesi bilateral ihracat kanıtıyla) sıralamak; gerçek firma isimleri zaten var olan harvest motoruna (adım 17+) ve Y2 customs-CSV ingest'ine bırakılıyor. **/workflows ile tam delege edildi** (kullanıcı talimatı): codex = coding (`codex exec`), codex gpt-5.6-sol high = code review, Claude Opus 4.8 xhigh = canlı test, ben (Fable/Sonnet) = strateji + orkestrasyon.

**Migration 116** (`research_markets`'e `kind` + `reporter_country` — additive, RLS/trigger'lara dokunmuyor) + **117** (`research_market_analyze_unique_inflight` — DB-seviyesi partial unique index, `market:analyze` için) + **122** (`research_hs_match_unique_inflight_and_codes` — aynısı `hs:match` + `research_hs_codes` (project,code) unique için). **DİKKAT — numara çakışması:** 117 paralel bir oturum tarafından da `email_connections_owner` için kullanılmış; Supabase timestamp-versiyonla takip ettiğinden ikisi de doğru uygulandı (CLAUDE.md'nin kendi notu: kozmetik çakışma, gölgeleme yok) ama gelecekte kafa karıştırabilir.

**İnşa edilen:** `server/src/lib/research/trade/comtrade.ts` (Comtrade istemcisi — HS-referans doğrulama, ülke kodu çözümleme, dünya-ithalat sıralaması, bilateral sorgu, rate-limit/backoff), `hs:match` + `market:analyze` job'ları + handler'lar + route'lar (`routes/research/hs.ts`), `geo:analyze`'a kanıt entegrasyonu (`trade/marketEvidence.ts` → `geo/prompt.ts` → `geoAnalyze.ts`), client: adım 7 HS kod inceleme ekranı + adım 9/10 kartlarına ticaret kanıtı satırı (`GeoCellDetail.tsx`, `IcpCountryChips.tsx`).

**1. tur canlı smoke 2 GERÇEK bulgu çıkardı (kod-seviyesinde değil, canlı VERİYLE):**
1. **Almanya dünya-ithalat sıralamasından SESSİZCE düşüyordu.** `resolveCountry()` Comtrade'in `Reporters.json`'ında ISO2 çakışan (13 tane) satırlardan İLKİNİ seçiyordu — Almanya için bu, 1990'da sona ermiş tarihi bir kod (280, veri yok) oluyordu, gerçek Almanya (276, $613M+ veri) değil. **Fix:** `entryExpiredDate`/`isCurrent` alanı + `preferCurrent()` — 3 eşleştirme aşamasında da (kod/tam-ad/gevşek-ad) uygulanıyor, genel bir düzeltme (Almanya'ya özel değil — Reporters.json'daki 13 + partnerAreas.json'daki 17 ISO2 çakışmasının HEPSİNDE doğrulandı).
2. **Varsayılan yıl (currentYear-2) satıcı-ülkesi bilateral verisini SESSİZCE boşaltıyordu** (TR'nin en yeni yıl ihracat verisi henüz yayınlanmamış olabiliyor). **Fix:** `getBilateralTrade()` artık `kind:'missing'` durumunda otomatik bir önceki yıla düşüyor (`actualYear` alanıyla hangi yılın kullanıldığı asla gizlenmiyor); `getWorldImportRanking()` bilinçli olarak DOKUNULMADI (ona gerek yok).

**2. tur (bu fix'ler + wiring doğrulaması sonrası) 3 review round'u:** round 1 codex BLOCK (9 bulgu — client YANLIŞ/eski `markets` route'unu çağırıyordu, ham `ilike` string eşleşmesiyle, Comtrade'in kanonik ülke adı yerine; ayrıca cost/idempotency bulguları) → düzeltildi; round 2 codex "NOT READY" (3 bulgu — GERÇEK bir P1: adım 22'nin sıfır-aday otomatik-atlama efekti, `hs:match` job'ı 'succeeded' olduğu AYNI React commit'te bayat `hsRows.length` okuyordu, gerçek sonuçları sıfır sanıp adımı kalıcı atlayabiliyordu) → düzeltildi; **round 3 codex SHIP** (yalnız P2/P3: boş `profile.products`'ın onaysız taslağa düşmesi, reload-race'in ikinci bir ücretli Comtrade koşusu tetikleyebilmesi, COGS telemetri sayacının eksik saymas, çift-dolar-işareti görsel hatası, ters bir toast koşulu — hiçbiri tenant/COGS/halüsinasyon-koruması/veri-doğruluğu değil).

**Final canlı smoke (Opus 4.8 xhigh) GERÇEK sayılarla kanıtladı:** Almanya artık dünya sıralamasında **#3** (`$567.6M`, 2024; `35 reporter, 0 failed`); TR→Almanya bilateral ihracat varsayılan config'te **$68.4M** (2024, %8.13 büyüme) dönüyor — fallback mekanizması ayrıca `year:2026` (garantili boş) zorlanarak da CANLI tetiklendiği kanıtlandı. `tsc -b` server+client temiz.

**Kalan bilinen boşluklar (P2/P3, SHIP'i engellemiyor, ileride küçük bir cila turu):** boş ürün listesi onaysız taslağa düşebiliyor; reload-race ikinci ücretli koşu riski; COGS telemetri sayacı eksik sayıyor (müşteriye sızmıyor); çift `$$` görsel hatası; ters toast koşulu. `COMTRADE_SUBSCRIPTION_KEY` set değil — ücretsiz katman ağır rate-limitli ama retry hepsini absorbe etti (35/35 reporter başarılı).

## 4.25 WP9+WP10 — codex gpt-5.6-sol (high effort) 3. bağımsız review turu — 🟡 **3×P1 + 4×P2 + 1×P3 gerçek bulgu, HEPSİ DÜZELTİLDİ** (2026-07-10, 4. oturum, UNCOMMITTED)

**Bağlam:** kullanıcı "bi de gpt 5.6 sol high ile review et" dedi — WP9 iki Fable turundan ("clean enough to trust" verdiktiyle) ve WP10 bir Fable turundan geçtikten SONRA, aynı kapsamı ÜÇÜNCÜ, farklı bir modelle (codex CLI, `-m gpt-5.6-sol -c model_reasoning_effort="high"`, nohup+disown ile tam detach edilip `Bash run_in_background` + `kill -0` poll ile beklendi — ~17 dakika sürdü) taze gözle inceletti. **Codex, iki Fable turunun KAÇIRDIĞI 3 GERÇEK P1 buldu** — ikisi de para/kredi-doğruluğu ile ilgili, biri de erişilebilirlik/UX ile:

**P1 — hepsi düzeltildi:**
1. **Zaman-aşımı sonrası "yetim" child job'lar sessizce kredi harcıyor.** Round-2'nin deadline fix'i conductor'ın KENDİ beklemesini sınırladı ama enqueue ettiği child job hâlâ 'queued' durumda kalabiliyordu — worker'ın TÜM slotları conductor'larla doluysa (round-1'in orijinal deadlock senaryosu), 25dk sonra conductor "time_budget" ile "bitti" der ama child job bir süre sonra (herhangi bir slot boşalınca) ARKA PLANDA çalışıp kredi harcayabiliyordu — wizard zaten "durduruldu" demişken. **Fix:** timeout anında, child hâlâ 'queued' ise (henüz 'running' değilse) iptal ediliyor; 'running' olan bir child'a DOKUNULMUYOR (yarı-yolda COGS/hold taahhüdü olabilir, o güvenli değil).
2. **`scale_target` yalnız child'lar ARASINDA kontrol ediliyordu, TEK bir child'ın kendi içine bir üst-sınır olarak GEÇİRİLMİYORDU.** Hedef=1, mevcut=0 iken tek bir harvest child'ı KENDİ varsayılan cap'iyle (40'a kadar aday) birden fazla eşleşme faturalandırabiliyordu — "en fazla N kredi" arayüz vaadiyle DOĞRUDAN ÇELİŞEN bir durum. **Fix:** conductor artık `caps.maxCandidates`'i KALAN bütçeye (`scaleTarget - matches`) göre child'a geçiriyor — her aday en fazla bir doğrulama üretir, bu yüzden bu, o TEK child'dan gelebilecek YENİ eşleşme sayısını gerçek/matematiksel olarak sınırlıyor (harvestRun.ts'in çapraz-ICP yeniden-skorlama geçişi de AYNI paylaşılan cap sayacını kullanıyor, o yüzden toplam aday bütçesi bütünsel olarak korunuyor).
3. **Hedef ilerlemesi bastırma-KÖR sayılıyordu.** `currentMatchCount()` `research_companies.status='match'` filtresini `suppressed` bayrağını HİÇ kontrol etmeden sayıyordu — müşteri 10 eşleşmeden 2'sini bastırırsa (WP10'un "istemiyorum"u), conductor hâlâ 10 sayıp hedefe ulaşıldığını düşünürken sonuçlar ekranı yalnız 8 gösteriyordu. **Fix:** `.eq('suppressed', false)` eklendi (GET /companies'in kendi WP10 filtresiyle aynı).

**P2 — hepsi düzeltildi:**
4. **Adım 18, canlı orkestrasyon sırasında DEĞİŞTİRİLEBİLİR bir coğrafya editörü + kanal başlatıcı (`GeoCellDetail`) gömüyordu.** Adım 18'de "Kaydet"e basmak hücreyi 'draft'a düşürüyor, conductor'ın BİR SONRAKİ child'ı onay kontrolünü geçemeyip `child_failed` ile başarısız olabiliyordu — müşterinin kendisi hiçbir şeye dokunmamışken. **Fix:** `GeoCellDetail` TAMAMEN kaldırıldı, yerine salt-okunur bir coverage rozet satırı (Found/E, Kanallar, Rule A/B, Fully covered) — AYNI `/research/channels/coverage` endpoint'inden, hiçbir düzenleme/mutasyon yüzeyi olmadan.
5. **"Sonuçlarınız" ekranı (adım 19) TÜM proje/ICP seçiciyi + ücretli serbest-metin/maps harvest başlatıcıyı dışarı açıyordu** — müşteri BAŞKA bir ICP/coğrafya seçip "Lead bul"a basarsa, wizard'ın kendi ölçek hedefinin ve orkestrasyonun TAMAMEN DIŞINDA gerçek kredi harcanıyordu, "İleri" ise hâlâ wizard'ın ORİJİNAL ICP'siyle ilerliyordu. **Fix:** `CompaniesPanel`/`EnrichmentPanel`'e yeni `lockScope` prop'u — wizard'dan gömüldüklerinde seçiciyi TAMAMEN gizliyor (sabit "Proje: X" metnine indiriyor) + CompaniesPanel'in harvest başlatıcısını TAMAMEN kaldırıyor (tablo + suppress + CRM export aynen kalıyor — adım 19'un gerçekten ihtiyaç duyduğu şey bu).
6. **Seed-prop'lar aslında "bir kere" değildi** — `initialIcpId`'yi "hâlâ null'sa doldur" mantığı, seçiciden proje değiştirilip `icpId` null'landığında ANINDA orijinal ICP'yi geri yüklüyordu (seçici ile tutarsız bir durum). **Fix:** ref tabanlı tek-seferlik latch'e çevrildi (`seededProjectRef`/`seededIcpRef`) — `lockScope` zaten seçiciyi gizlediği için bu artık wizard'da HİÇ tetiklenemez, ama düzeltme kendi başına da doğru (gelecekte seçicili bir çağıran olursa bile artık doğru davranır).
7. **Adım 16 ve 18-20'nin `saveStepMut`/`scaleTargetMut` geçişlerinde 15→16'nın ZATEN aldığı `calibStepToken` koruması YOKTU** — throttle edilmiş bir PATCH + hızlı Next-sonra-Back tıklaması, geç gelen başarı callback'inin kullanıcıyı yeniden ileri fırlatmasına izin veriyordu. **Fix:** AYNI paylaşılan `calibStepTokenRef` mekanizması adım 16→17, 17→18 (scaleTargetMut'un KENDİSİ de artık TIKLAMA anında bir token yakalayıp onSuccess'te yeniden kontrol ediyor — bu iki-istekli zincirin kendine özgü bir ek koruması), 18→19, 19→20, 20→21'in hepsine uygulandı; ilgili TÜM "Geri" tıklamaları ref'i artırıyor. **Bilinçli kapsam sınırı:** adım 1-14'ün KENDİ düz "İleri" tıklamaları AYNI teorik yarışı taşıyor ama bu turun kapsamı DIŞINDA bırakıldı — zaten 4+ round'luk ayrı bir review'den geçmiş, geniş bir retrofit yerine yalnız BU turun yeni geçişleri düzeltildi.
8. **[P2, KABUL EDİLEN risk, düzeltilmedi] Conductor + child adopsiyon guard'ları atomic olmayan check-then-insert.** İki eşzamanlı `/orchestrate/run` isteği teorik olarak iki conductor'ı enqueue edebilir. Harvest.ts/channels.ts'in KENDİ "ICP başına tek harvest" guard'ıyla AYNI kabul edilmiş sınıf (DB invariant'ları — lease-fence'li ömürde-bir-faturalama — her durumda korunuyor; en kötü sonuç ÇİFT MÜKERRER COGS, ÇİFT MÜŞTERİ FATURASI DEĞİL). Yeni bir DB unique constraint eklenmedi, mevcut precedent'le tutarlı bırakıldı.

**P3 — düzeltildi:** adım 18'in no-op `onReanalyze`'i (GeoCellDetail kaldırılınca kendiliğinden kapandı, bkz. madde 4).

**Sonuç:** WP9+WP10 artık ÜÇ bağımsız review turundan (2×Fable + 1×codex) geçti, HEPSİNİN bulguları düzeltildi. `tsc -b` (client+server) + eslint temiz.

## 4.26 WP9 — CANLI SMOKE (izole DB, `iehqsuludghrhosgxhnr`) — ✅ **P1-2 kredi-aşımı düzeltmesi GERÇEK bir koşuda doğrulandı**

**Kurulum:** lokal `npm run dev` (API, 3001) + `npm run dev:worker` çalıştırıldı (worker'ın dotenv-timing sorunu — muhtemelen tsx'in ESM import-hoisting'i, ÖNCEDEN VAR OLAN/WP9 dışı — `set -a && source ../.env` ile aşıldı). Mevcut bir onaylı ICP+hücre bulundu (`MAPS-HARVEST-SMOKE` projesi, tenant `11111111-...`, kredi bakiyesi 481).

- **Test A — `research:orchestrate` job'ı DOĞRUDAN `research_jobs`'a insert edilerek** (HTTP auth'u atlayarak, worker'ın işleyişini doğrudan test etmek için) çalıştırıldı: hücre henüz `saturation_a=false` olduğundan conductor `channels:discover`'ı denedi, bu YEREL ortamda SearXNG olmadığı için (bilinen, dokümante edilmiş sınırlama — `RESEARCH_SEARXNG_URL` yalnız Railway'de var) BEKLENDİĞİ GİBİ başarısız oldu, conductor bunu doğru sınıflandırıp (`credits_exhausted` değil) **`stopped_by:'child_failed'`** ile TEMİZ durdu — karar döngüsü, enqueue mantığı, poll mantığı, hata sınıflandırması hepsi uçtan uca doğrulandı.
- **Test B — P1-2'nin (kredi-aşımı) canlı doğrulaması:** hücrenin `saturation_a`'sı TEST-AMAÇLI geçici olarak `true`'ya çekildi (SearXNG'siz channels:discover'ı atlayıp Y3/harvest:run yoluna geçmek için) + proje `scale_target=1` yapıldı. Conductor **3 GERÇEK `harvest:run` child'ı** enqueue etti (gerçek Gemini-grounded arama + DeepSeek doğrulama, gerçek maliyet) — **HER ÜÇÜ de `caps.maxCandidates=1` ile sınırlıydı** (log'da doğrulandı: `"caps":{"maxCandidates":1,...}`), ilk ikisi 0 eşleşme buldu (aday elendi/zaten skorlanmıştı), üçüncüsü tam 1 eşleşme buldu ve faturaladı (kredi 481→**480**, tam olarak hedeflenen 1) — conductor hemen ardından `matches:1 >= scale_target:1` görüp **`stopped_by:'scale_target_reached'`** ile durdu. **Bu, P1-2 fix'inden ÖNCEKİ davranışla TAM TERSİ:** fix olmadan TEK bir `harvest:run` child'ı kendi varsayılan cap'iyle (40'a kadar aday) tek seferde birden fazla eşleşme faturalayabilirdi — şimdi HER round matematiksel olarak en fazla `remaining` (kalan hedef) kadar YENİ eşleşme üretebiliyor, canlı veriyle KANITLANDI.
- **Temizlik:** yalnız TEST-AMAÇLI zorlanan bayraklar (`saturation_a`, `scale_target`, + türetilmiş `fully_covered` tutarlılığı) geri alındı; gerçek harvest sonucu (1 firma, 1 verdict, 1 billable_event, kümülatif coverage istatistikleri) — bu izole DB'nin ÖNCEDEN de biriktirdiği diğer smoke-test kalıntılarıyla AYNI şekilde — bırakıldı. Lokal server+worker process'leri durduruldu.
- **Test edilmeyen (kapsam dışı bırakıldı, ayrı bir tur gerektirir):** suppress route/panel-kilit/wide-layout/confirm-modal'ın TARAYICI üzerinden görsel doğrulaması (yalnız SQL seviyesinde RPC+feedback-upsert mekanizması doğrulandı, ayrıca aşağıya bkz.); channels:discover/harvest'in GERÇEK SearXNG ile başarılı yolu (yerel altyapı yok — yalnız Railway'de).

**Suppress mekanizması (WP10) — SQL seviyesinde doğrulandı:** gerçek bir MATCH firma (`BHS Bonner...`, canonical_key `bhsgrosshandel.de`) üzerinde `research_suppress_company` RPC'si çağrıldı (`suppressed=true` doğrulandı) + hem flat hem verdict-aware `GET /companies` sorgu mantığı elle çalıştırılıp firmanın HER İKİ yoldan da düştüğü doğrulandı + `research_company_feedback` upsert'i (conflict target dahil) başarıyla çalıştığı doğrulandı. Test sonrası satır TAMAMEN eski haline (suppressed=false, feedback satırı silindi) döndürüldü.

**Sonuç:** WP9'un EN KRİTİK bulgusu (P1-2, kredi-aşımı) artık hem kod-seviyesinde hem CANLI bir koşuda doğrulanmış durumda. WP9 **SHIP olarak işaretlenebilir** (3 review turu + canlı smoke). WP10 hâlâ yalnız SQL-seviyesinde doğrulandı — tarayıcı üzerinden görsel/etkileşim doğrulaması (özellikle `lockScope`'un panel picker'ını gerçekten gizlediği, onay modalının gerçekten mutasyonu engellediği, `wide` layout'un gerçekten iyi göründüğü) hâlâ AÇIK.

## 4.23 WP9 — ölçek ekranı + derin-araştırma orkestratörü + offer kartları — ✅ **ROUND 2 (verify, Fable): "clean enough to trust" — 7/7 fix ONAYLANDI, 1 kalıntı P3 (§4.23 sonu) düzeltildi; ROUND 3 (codex): §4.25** (2026-07-10, 4. oturum, UNCOMMITTED)

**Bağlam:** WP8b round 9 codex kullanım limitine takılınca kullanıcı "codex'i şimdilik unut, ben söylerim" dedi; bu oturumun devamında kullanıcı **"continue building WP9 while waiting for codex usage limits reappear... send everything to review... whichever are done meanwhile"** dedi — yani WP9'u codex'siz, kendi kendime (Read/grep ile kod tabanını inceleyip, hiç subagent'a devretmeden) baştan sona yazmam istendi, kullanıcı codex'e dönünce topluca (WP8b round 9 + WP9'un ilk turu) review'e gönderilecek.

**Kapsam bulgusu (oturum başında):** `05_SONRAKI_ADIMLAR.md`'nin WP6-10 bölünmesinde ana-akış adım 16'nın (offer/açı kartları — motoru WP4'te zaten bitmiş, `OffersPanel.tsx` + `research_offers` + `offer:generate` hepsi var) HİÇBİR iş paketine atanmadığı fark edildi — WP9 (adım 17-18) ile WP10 (adım 19-24) arasında düşmüş. WP9'a eklendi (adım 15-16 olarak), placeholder adım 19'a kaydı.

**Wizard adım numaralandırması (WP8b'nin 15'inci "placeholder" adımından devam, KNOWN_STEPS 15→19):**
- **Adım 15 — offer üretimi bekleme ekranı** (step 7 `icp:generate` kalıbının BİREBİR AYNISI: one-shot latch + job poll + `OFFER_GEN_STAGES` narration). `calibIcp` (WP8b'nin pinlediği kalibre ICP) approve'lu değilse veya offers zaten varsa otomatik atlar.
- **Adım 16 — teker-teker offer kartı** (step 8 `IcpCard` kalıbının BİREBİR AYNISI): yeni `OfferCard.tsx`'i (aşağıya bkz.) tek kart olarak gösterir, persist edilen `flow_state.offer_card_index` cursor'ı (WP8a/8b'nin `icpCardIndexOverride`/`geoCardIndexOverride` ile AYNI desen — `SaveStepVars.offerCardIndexOverride`, hydration'da okunur, tenant-reset'te sıfırlanır).
- **Adım 17 — ölçek & kredi ekranı:** `allGeoCells`'ten (zaten yüklü, yeni sorgu YOK) `calibIcp`'nin onaylı hücrelerinin E toplamı + `GET /harvest/credits` (canlı, step'e özel 10sn refetch) + `scale_target` için `NumberInput`. `scale_target` `research_projects`'in KENDİ üst-seviye kolonu (profile/flow_state'in parçası DEĞİL) — bu yüzden `saveStepMut`'un profile/flow_state makinesinden GEÇMİYOR, ayrı küçük bir `scaleTargetMut` (`PATCH {scale_target}`) sonra `saveStepMut.mutate({nextStep:18})` çağırıyor. **Bilinçli tasarım kararı:** `saveStepMut`'a DOKUNULMADI (8 round'luk review'den geçmiş, riski en yüksek fonksiyon) — yeni bir alan eklemek yerine iki ayrı istek kabul edildi (güvenlik/faturalama-kritik değil, sadece bir tercih).
- **Adım 18 — derin araştırma orkestratör bekleme ekranı:** aşağıya bkz. `calibIcp`/`calibBestGeoCell` (WP8b'nin ZATEN hesapladığı "en yüksek puanlı onaylı hücre") hedef alınır — YENİ bir hücre seçimi icat edilmedi. `calibIcp`/`calibBestGeoCell` null ise (örn. hiç ülke eklenmemiş) statik bir "önce ülke ekleyin" ekranı — dead-end YOK.
- **Adım 19 — placeholder** (eskiden adım 15'ti; içerik AYNEN taşındı, sadece i18n key'leri `step15.*`→`step19.*` yeniden adlandırıldı — YENİ adım 15'in gerçek içeriğiyle key ÇAKIŞMASINI önlemek için ZORUNLUYDU).
- **`!calibIcp && !calibIcpFallback` "kalibre edilecek bir şey yok" guard'ı** (`step >= 11 && step <= 14`) **`<= 16`'ya genişletildi** — adım 15/16'nın kendi otomatik-tetikleme effect'leri de `calibIcp`'ye AYNI şekilde bağımlı, genişletilmeden null bir calibIcp bu iki ekranı da sonsuz loader'da bırakırdı (WP8a/8b'nin tekrar tekrar bulduğu "auto-fire dead-end" hata sınıfı — bu turda PROAKTİF kapatıldı, codex beklemeden).

**`OfferCard.tsx` (yeni component, WP8a `GeoCellDetail` çıkarma emsali):** `OffersPanel.tsx`'in kendi (dışa AKTARILMAMIŞ) `OfferCard` fonksiyonu BİREBİR (satır satır aynı JSX/mutation mantığı) `client/src/components/research/OfferCard.tsx`'e taşındı, `OfferRow`/`AngleStats` tipleri export edildi. `OffersPanel.tsx` artık bu dosyayı import ediyor, KENDİ davranışı (SimpleGrid'de tüm kartlar, WP5 angle-stats join'i) DEĞİŞMEDİ — wizard'ın step 16'sı `stats={null}` geçiyor (WP5 stats join'i wizard kapsamı dışı, `/research/full`'da hâlâ çalışıyor).

**`research:orchestrate` job (server/src/lib/research/worker/handlers/orchestrate.ts, YENİ, ~230 satır) — tasarım:**
- **Kapsam kararı (KULLANICI ONAYI ALINMADI — sonraki review'de netleştirilmeli):** orkestratör **TEK onaylı icp×geo hücresi** için çalışır — `channels:discover`/`channels:harvest`/`harvest:run`'ın ZATEN tek-hücre olan tasarımını taklit eder. Ana-akış'ın "onaylı+kalibre hücre SIRASIYLA" ifadesi (05 §WP9 eski metni) çok-hücreli bir TEK job olarak da okunabilirdi — bunun yerine hücre-sırası WIZARD SEVİYESİNDE bırakıldı (müşteri her onaylı hücre için adım 17-18'i ayrı ayrı tekrar çalıştırabilir). Gerekçe: (a) `channels:discover/harvest`'in "ICP başına tek harvest" in-flight guard'ı (harvest.ts/channels.ts) zaten TEK icp_id'yi varsayıyor — çok-hücreli bir conductor bu guard'ı kendi İÇİNDE yeniden icat etmek zorunda kalırdı; (b) her hücrenin kendi WizardShell ekranında görünür olması "tek ekran, tek iş" ilkesine (ana-akış §0) daha sadık; (c) bir job'un TEK worker concurrency slotunu SINIRSIZ süre işgal etme riski böylece hücre başına sınırlı kalıyor (`MAX_RUNTIME_MS=25dk` zaten var ama çok-hücreli tasarımda bu limit N hücrenin TAMAMI için paylaşılırdı).
- **Y2 (gümrük) DAHIL EDİLMEDİ** — batch-scoped (belirli bir `research_trade_import_batches` satırı), geo-cell-scoped DEĞİL; adım 11'in elle "Araştır" butonu (zaten var, WP0b) olarak kalıyor. Bilinçli, dokümante edilmiş kapsam daraltması (Y1+Y3 yeterli, Y2 opsiyonel/manuel).
- **Karar döngüsü (her iterasyonda TEK karar):** pending kanal var mı (varsa `channels:harvest`) → yoksa rule-A açık mı (`channels:discover`) → yoksa rule-B açık mı (`harvest:run` Y3) → hepsi kapalıysa `fully_covered` ile dur. Her adımdan önce `availableCredits`/`scale_target`/`research_chunks` CANLI okunur (cache'lenmiş bir snapshot değil) — müşteri adım 17'ye geri dönüp hedefi değiştirirse BİR SONRAKİ iterasyonda anında etkili.
- **Hiçbir DB YAZMA işlemi YOK** (billing/coverage/company yazımlarının TAMAMI enqueue ettiği child job'ların İÇİNDE, değişmeden) — orchestrate.ts SADECE okur + `enqueueJob` + `research_jobs` satırını poll eder. Bu, fence/lease disiplinini SIFIRLIYOR (conductor'ın kendi lease'i hiçbir zaman billing/coverage RPC'sine geçmiyor).
- **In-flight guard tekrarı:** conductor `channels:harvest`/`harvest:run` enqueue etmeden ÖNCE harvest.ts/channels.ts'in AYNI "ICP başına tek harvest" sorgusunu (4 tip: HARVEST_RUN/MAPS_HARVEST/TRADE_HARVEST/CHANNELS_HARVEST) kendi içinde tekrar çalıştırır — bir customer'ın advanced view'dan elle başlattığı bir harvest'i RACE etmek yerine ADOPT eder (aynı job id'yi poll eder). `channels:discover` için ayrı, dar bir guard (sadece geo_id eşleşmesi).
- **Durma nedenleri (`job.result.stopped_by`, hepsi TEMİZ succeeded, harvest:run'ın "cap = graceful stop" felsefesiyle AYNI):** `credits_exhausted` / `scale_target_reached` / `fully_covered` / `time_budget` (25dk, env `RESEARCH_ORCHESTRATE_MAX_RUNTIME_MS`) / `iteration_cap` (40, env `RESEARCH_ORCHESTRATE_MAX_ITERATIONS`, "sessiz cap yok" ilkesiyle log'lanır) / `child_failed` (bir child job kredi-tükenme DIŞI bir nedenle failed olursa).
- **Yeni route:** `POST /api/research/orchestrate/run` (server/src/routes/research/orchestrate.ts) — onaylı ICP+hücre gate, in-flight adopt (200), "ICP başına tek harvest" gate (409), kredi gate (402), `maxAttempts:1` enqueue (202). `routes/research/index.ts`'e mount edildi. Yeni job türü `RESEARCH_JOB_TYPES.ORCHESTRATE = 'research:orchestrate'` (jobTypes.ts) + handler registry'ye eklendi (worker/handlers/index.ts).

**Self-review (codex'ten ÖNCE, kendi taradığım noktalar — bir sonraki review turunun ÖZELLİKLE bakması gereken yerler):**
1. Adım 16'nın `!currentOffer` durumu (offers listesi boşsa) sonsuz loader yerine adım 15'e dönen bir fallback ekranına çevrildi (step 10'un sıfır-hücre emsaliyle AYNI desen) — teoride ulaşılamaz (offers'ın delete endpoint'i yok) ama WP8a/8b'nin "şöyle olmaz sandık ama oldu" tarihiyle tutarlı bir önlem.
2. Conductor'ın "adopt foreign in-flight job" yolu: kendi hücresiyle İLGİSİZ bir job'u (aynı ICP'nin BAŞKA bir hücresi için manuel başlatılmış bir harvest) adopte edip poll edebilir — DB durumu her zaman doğru kalır (child job kendi işini doğru yapar) ama conductor'ın `channelsHarvested`/`y3Rounds` SAYAÇLARI o turda YANLIŞ artabilir (kozmetik, stop-condition kararları bu sayaçları DEĞİL, DB'den taze okunan `matches`/`chunk`/`available`'ı kullanıyor — sadece anlatı metni hafif yanıltıcı olabilir).
3. `harvest:run`'a `geography` alanı YOLLANMADI (sadece `geo_id`) — handler'ın kendi fallback'i (hücrenin `country`'sini DB'den okuma) buna göre tasarlanmış, `harvest.ts` route'unun kendisi de `geo_id` varken `geography`'yi hücrenin country'siyle EZİYOR; doğrulandı ama bir sonraki review'de TEKRAR doğrulanmalı.
4. Worker concurrency: conductor job'u worker'ın `concurrency` (default 4) slot'larından BİRİNİ tüm çalışma süresince (dakikalar) işgal ediyor — `heartbeatJob`'ın kendi 15sn'lik interval'i handler'ın ne yaptığından BAĞIMSIZ çalıştığı için lease asla reap edilmiyor (runner.ts doğrulandı), ama küçük bir worker havuzunda birden fazla conductor aynı anda çalışırsa child job'lara ayrılan slot azalır — dokümante edilmiş, kod-değişikliği GEREKTİRMEYEN bir bilinen sınır.

**Yapılmayanlar / sonraki tura bırakılanlar:** offer kartlarının step 16'sında WP5'in angle-stats rozetleri YOK (`stats={null}`) — advanced view'da hâlâ var, wizard'a eklemek kozmetik bir sonraki iyileştirme. Adım 18'in "bitti" ekranından WP10'un henüz yazılmamış "sonuçlar" ekranına geçiş YOK (placeholder adım 19'a düşüyor) — WP10 bunu üstlenecek.

**Build durumu:** `npx tsc -b --force` (client) + `npx tsc -p tsconfig.json --noEmit` (server) + `npx eslint ... --max-warnings 0` ÜÇÜ DE temiz. Canlı smoke YAPILMADI (izole test DB'de 1 hücrelik gerçek bir orchestrate koşusu bir sonraki adım — kullanıcı codex'e dönünce ya da açıkça isteyince).

**Codex'e dönüldüğünde ilk review turu için taslak prompt (WP8b round 9'dan SONRA çalıştırılmalı):**

> Review WP9 (TG-Research): a brand-new client wizard steps 15-18 in `client/src/pages/research/ResearchFlowPage.tsx` (offer/angle cards, scale & credit screen, deep-research orchestrator wait screen — KNOWN_STEPS bumped 15→19, old step-15 placeholder moved to step 19), a new extracted `client/src/components/research/OfferCard.tsx`, a brand-new server job `server/src/lib/research/worker/handlers/orchestrate.ts` + route `server/src/routes/research/orchestrate.ts` (mounted in `routes/research/index.ts`, job type registered in `jobTypes.ts`/`worker/handlers/index.ts`). This code has NEVER been reviewed — no prior rounds to reconcile, do a genuinely fresh full pass. Ignore all other uncommitted changes in this working tree (LinkedIn, CRM Tasks). Pay special attention to: (1) the widened `!calibIcp && !calibIcpFallback` guard range (11-14 → 11-16) and whether steps 15/16's own auto-fire effects can still strand on a dead loader in some case this widening didn't cover; (2) the orchestrate handler's decide-loop correctness — credit/scale_target checks happening BEFORE each spend-adjacent enqueue, the in-flight-adopt guards actually preventing double-enqueue races with the routes' own guards, and whether MAX_RUNTIME_MS/MAX_ITERATIONS are reachable safety nets or dead code; (3) the new `offer_card_index` flow_state field threading correctly through hydration/tenant-reset/saveStepMut exactly like the three prior cursor fields it mirrors; (4) whether the scale_target PATCH (separate from saveStepMut) can race or double-fire with the step-17→18 advance. Rank P1/P2/P3 with file:line. Be unambiguous: is WP9 clean, or not?

**Round 1 review (Fable model, background agent, 2026-07-10) — 3×P1 + 4×P2, hepsi gerçek bulgu, hepsi düzeltildi:**

1. **[P1] Worker concurrency-havuzu deadlock riski** — `orchestrate.ts`'in `pollJobToTerminal`'ı SINIRSIZ bir `for(;;)` döngüsüydü; `MAX_RUNTIME_MS` kontrolü yalnız DIŞ döngünün TEPESİNDE (iterasyonlar ARASINDA) çalışıyordu, bir child'ı BEKLERKEN asla. Worker'ın concurrency slot'ları (default 4) orkestratörlerle dolarsa — ya da concurrency 1'e ayarlanırsa — her orkestratörün enqueue ettiği child job HİÇBİR ZAMAN claim edilemez (aynı havuzdan), her orkestratör sonsuza dek poll eder, hiçbiri reap edilmez (kendi heartbeat'i 15sn'de bir kendini tazeler) — TÜM worker (LinkedIn job'ları dahil) process restart'a kadar kilitlenir. **Fix:** `pollJobToTerminal` artık ZORUNLU bir `deadlineAt` parametresi alıyor, KENDİ döngüsü İÇİNDE `Date.now() >= deadlineAt` kontrolü yapıyor, yeni bir `'timeout'` durumu döndürüyor; çağıran bunu `stoppedBy='time_budget'` olarak temiz durma sayıyor. En kötü durumu artık worker restart'ı DEĞİL, en fazla `MAX_RUNTIME_MS` (25dk) bekleme.
2. **[P1] Adım 15'te ölü spinner (+ adım 7'nin ÖNCEDEN VAR OLAN, aynı sınıftan gizli hatası)** — `offerGenJobStatus`/`icpGenJobStatus` 'succeeded' olduğunda hiçbir yerde `offersQuery`/`icpsQuery` invalidate edilmiyordu (job sorgusunun kendi `refetchInterval`'ının durması, LİSTE sorgusunun tazelenmesiyle AYNI şey değil) — 15→16 ve 7→8 auto-advance effect'leri `offers.length`/`icps.length`'in ASLA sıfırdan büyük olduğunu göremiyordu (yalnız bir pencere-refocus/remount kurtarabilirdi). **Fix:** her iki job-status için de `qc.invalidateQueries({queryKey:[...]})` çağıran birer YENİ effect eklendi — adım 7'nin hatası WP6/WP8a'dan miras kalan, bu turda tesadüfen bulunan, ÖNCEDEN VAR OLAN bir hataydı (WP9 kapsamı dışı ama düzeltmesi bedava ve çekirdek akışı etkiliyordu, o yüzden birlikte düzeltildi).
3. **[P1] Adım 17'nin "tahmini havuz" rakamı ICP'nin TÜM onaylı hücrelerinin toplamıydı ama orkestratör yalnız TEK hücre araştırıyor** — çok-ülkeli bir projede müşteri 3 ülke toplamı 900 görüp hedefi 500 girebiliyordu, orkestratör TEK hücrede asla 500'e ulaşamayıp `fully_covered` ile duruyordu (örn. 180 eşleşmeyle), diğer 2 ülke SESSİZCE hiç araştırılmıyordu. **Fix:** adım 17 artık yalnız `calibBestGeoCell`'in (adım 18'in gerçekten araştıracağı TEK hücre) tahminini gösteriyor + alt başlık hangi ülke olduğunu açıkça yazıyor.
4. **[P2] Route'un 409'u kendi kopyasıyla çelişiyordu** — "zaten çalışan bir harvest var, derin araştırma onu otomatik devralacak" diyordu ama hiçbir şey enqueue edilmemişti devralacak. **Fix:** bu 409 kontrolü TAMAMEN kaldırıldı — conductor'ın kendi ilk iterasyonundaki `findInflightHarvest` zaten aynı işi doğru yapıyor.
5. **[P2] Hedefi bitmiş bir koşudan SONRA yükseltmek asla yeni koşu tetiklemiyordu** — `orchestrateJobId`/`orchestrateRequested` bitmiş haliyle kalıyordu. **Fix:** `scaleTargetMut.onSuccess` artık HER 17→18 geçişinde bu iki state'i sıfırlıyor.
6. **[P2] 15→16 auto-advance'in `calibStepToken`'ı yoktu** — 11-14 arasının 4 geçişini koruyan AYNI mekanizma 15→16'ya hiç uygulanmamıştı (WP8b'nin tekrar tekrar bulduğu TAM o hata sınıfı, bir ekran ötede). **Fix:** `calibStepTokenRef.current` hem auto-advance'in `saveStepMut.mutate` çağrısına hem adım 15'in `onBack`'ine eklendi (aynı paylaşılan ref — 11-14 için zaten var olan mekanizmanın 15-16'ya genişletilmesi, yeni bir ref DEĞİL).
7. **[P2] `calibIcpId` self-heal aralığı `<=14` idi** — adım 15/16 da `calibIcp`'ye 11-14 ile AYNI şekilde bağımlı. **Fix:** `<=16`'ya genişletildi ("hiç kalibre edilecek bir şey yok" guard'ıyla AYNI genişletme, bu turda zaten yapılmıştı — bu, PIN'i o guard'dan ÖNCE onaran ayrı bir efekt).
8. **[P3, ayrıca düzeltildi] `scale_target=0`** sunucu şemasında geçerliydi ama conductor'a anında "hedefe ulaşıldı" gibi görünüyordu → `0` artık `null`'a (hedef yok) eşleniyor. **[P3, ayrıca düzeltildi] adım 18'in "bitti" ekranı `child_failed`'i de teal/başarı rengiyle gösteriyordu** → artık ayrı bir sarı uyarı + "Tekrar dene" butonu. **[P3, kasıtlı ERTELENDİ] adım 17'nin `max={available}`** kısıtı zaten kaldırıldı (estimate fix'iyle birlikte, artık hiç `max` yok).

**Round 2 sonucu (verify + WP10'un ilk turu, Fable, background, DÖNDÜ):** 7 fix'in HEPSİ tek tek trace edilip FIXED olarak doğrulandı (deadlock artık `MAX_RUNTIME_MS`'e sınırlı; invalidateQueries key'leri tam eşleşiyor; adım 17 artık yalnız `calibBestGeoCell`; route'un 409'u kaldırılmış ve child-race hâlâ conductor'ın kendi guard'ında korunuyor; scale_target reset zinciri doğru; `calibStepToken` 15→16'da doğru işliyor; self-heal aralığı yeterli — kalan "pinned-but-vanished" alt-durumu `research_icps`'in delete route'u OLMAMASI nedeniyle teyit edilmiş şekilde ULAŞILAMAZ). **Verdict: "clean enough to trust."** İki küçük kalıntı bulundu ve BU TURDA da düzeltildi: (1) route'un artık var olmayan "zaten çalışıyor" 409'una göre yazılmış istemci toast'ı ("resuming it") artık YANLIŞ bilgi veriyordu (kalan tek 409 nedeni: ICP/coğrafya onaylı değil) → özel 409 dalı tamamen kaldırıldı, generic `showErrorFromApi` sunucunun kendi mesajını gösteriyor (`showWarning` importu da kullanılmıyordu, kaldırıldı); (2) adım 17'nin `max` kısıtının kaldırılmış olması bilinçli bir kalıntıydı, dokunulmadı (P3, önemsiz).

## 4.24 WP10 — sonuçlar + kişiler + kapanış/yaşayan-döngü — 🟡 **Fable turu (P1 yok, 2×P2 düzeltildi) + codex round 3 (§4.25, 2×P2 daha bulundu+düzeltildi)** (2026-07-10, 4. oturum, UNCOMMITTED)

**Kapsam (ana-akış adım 19-24):** WP9'un placeholder'ı (adım 19) gerçek içerikle değiştirildi + adım 20-21 eklendi.

- **Sunucu — "istemiyorum" (yeni route):** `POST /api/research/harvest/companies/:id/suppress` (`server/src/routes/research/harvest.ts`) — firmanın `canonical_key`'ini bulur → **mevcut** `research_suppress_company` RPC'yi `source='manual'` ile çağırır (feedbackAggregate.ts'in opt-out senkronuyla AYNI RPC, aynı çağrı kalıbı) → best-effort (try/catch, asla ana işlemi düşürmez) `research_company_feedback`'e `rating='bad'` satırı upsert eder (ICP'nin GÜNCEL `ruleset_version`'ıyla). **Bilinçli tasarım:** `POST /icps/:id/feedback`'ten GEÇMİYOR — o route ayrıca `research_icps.calibration_state`'i ilerletiyor (kalibrasyon-döngüsü defter tutması), kalibrasyondan çok sonra sonuçlar ekranından gelen bir suppress'in bunu tetiklemesi yanlış olurdu.
- **Sunucu — görünürlük düzeltmesi:** `GET /harvest/companies`'in HER İKİ yolu da (flat rollup + verdict-aware) artık `suppressed=true` firmaları dışlıyor — suppress edilen bir firma müşterinin KENDİ sonuç listesinde bir daha görünmüyor (öncesinde yalnız gelecekteki RE-HARVEST'te dışlanıyordu, halihazırda gösterilmiş satır ekranda kalıyordu). Flat yol pagination-ÖNCESİ filtre (tam sayım korunur); verdict-aware yol pagination-SONRASI filtre (bir sayfa `limit`'ten az satır dönebilir — export route'unun kendi `has_more` yaklaşıklığıyla AYNI kabul edilmiş belirsizlik sınıfı, dokümante edildi).
- **İstemci — panel yeniden-kullanımı (extraction YOK, seed-prop enjeksiyonu):** `CompaniesPanel.tsx`/`EnrichmentPanel.tsx`'in kendi proje/ICP seçici sohbeti VAR — WP8a/8b'nin IcpCard/GeoCellDetail/OfferCard'ı çıkarma emsalinin AKSİNE, burada panelin TAMAMI (seçici dahil) wizard'a AYNEN gömüldü, yalnız iki OPSİYONEL prop (`initialProjectId`/`initialIcpId`) eklendi — `useEffect`'le (bir `useState` initializer'ı DEĞİL, çünkü wizard'ın kendi sorguları prop'u MOUNT'tan SONRA çözebilir) yalnız state hâlâ null'ken seed eder, kullanıcının SONRADAN yaptığı seçimi asla ezmez. Mevcut `/research/full` çağrı noktaları (prop'suz) BYTE-BYTE aynı davranıyor. `CompaniesPanel.tsx`'e ayrıca yeni bir "İstemiyorum" satır butonu + `suppressMut` eklendi (advanced view'da da kullanılabilir — kasıtlı, memory notundaki orijinal plan zaten "CompaniesPanel satır butonu" diyordu).
- **Wizard adımları (yeni state/effect YOK — bu WP'nin en düşük riskli kısmı):** adım 19 (`CompaniesPanel` gömülü, "İleri" → 20), adım 20 (`EnrichmentPanel` gömülü, "İleri" → 21), adım 21 (kapanış ekranı: CRM'e gönderme hatırlatması + 4 "yaşayan döngü" butonu — ülke ekle→adım 8, yeniden kalibre et→**mevcut** `restartCalibLoop()` AYNEN çağrılıyor, ölçeği ayarla→adım 17, daha fazla kişi→adım 20 — hepsi SAF yerel `setStep()`, PATCH YOK, dosyanın "geri/yanal atlamalar kalıcılaşmaz" kuralıyla tutarlı). `KNOWN_STEPS` 19→21.
- **Bilinçli UX ödünü (dokümante edildi, henüz iyileştirilmedi):** CompaniesPanel/EnrichmentPanel kendi proje/ICP Select'lerini GÖSTERİYOR (gizlenmedi) — pre-seed edildiği için görünürde doğru değer seçili duruyor ama müşteri isterse başka bir proje/ICP'ye elle geçebilir (advanced-view'daki gibi). Panelleri "seçici-siz" hale getirmek daha derin bir cerrahi gerektirir, bu tura ERTELENDİ.

**Build durumu:** `npx tsc -b --force` (client) + `npx tsc -p tsconfig.json --noEmit` (server) + `npx eslint` (yalnız Research dosyaları, proje kendi `eslint .` komutuyla de çapraz kontrol edildi — CRM'nin başka paralel oturumlardan kalan ÖNCEDEN VAR OLAN, ilgisiz hataları hariç sıfır yeni sorun) temiz. Canlı smoke YAPILMADI.

**İlk review turu (Fable, background) — P1 YOK; 2×P2 + 2×P3 gerçek bulgu, hepsi düzeltildi:**

1. **[P2] Tek-tıkla, GERİ ALINAMAZ, tenant-genelinde suppress — onay YOK, geri alma yolu YOK.** Kod tabanında `research_suppression`'ı geri alan hiçbir route/RPC yok (grep doğrulandı) — "İstemiyorum" butonuna yanlışlıkla basmak bir firmayı bu tenant için SONSUZA dek gizliyordu, elle SQL dışında kurtarma yolu yoktu. **Fix:** `@mantine/modals`'ın `openConfirmModal`'ı (NumbersTab.tsx'in "release number" emsaliyle AYNI kalıp — proje zaten bu deseni kullanıyor) — buton artık önce "geri alınamaz" uyarılı bir onay modalı açıyor, mutation yalnız onaydan sonra ateşleniyor.
2. **[P2] Wizard'ın dar `Container size="sm"` (~510px) çerçevesine tam-sayfa paneller sıkıştırılmıştı.** CompaniesPanel'in kendi tablosu `minWidth={720}` istiyor + proje/ICP seçici satırı zaten 480px+; adım 19/20'de bu, sıkışık bir görünüme yol açıyordu (kırılmıyordu — wrap+scroll absorbe ediyordu — ama "sonuçlarınız" gibi vitrin ekranları için kötü). **Fix:** `WizardShell`'e yeni opsiyonel `wide` prop'u (`Container size={wide?'lg':'sm'}`) — yalnız adım 19/20 kullanıyor, diğer TÜM adımlar (tek kart/form/bekleme ekranı) varsayılan dar genişlikte kalıyor (wizard'ın "tek ekran tek iş" ilkesinin ZATEN dar-genişlik gerektirdiği yerler).
3. **[P3, düzeltildi] Best-effort feedback upsert'ünün DÖNEN hatası hiç kontrol edilmiyordu** — supabase-js `throw` etmez, `{error}` döner; try/catch yalnız GERÇEK bir throw'u (ağ/programlama hatası) yakalıyordu, DB hatası (constraint/RLS) sessizce kayboluyordu. **Fix:** `const {error} = await ...upsert(...); if (error) log.warn(...)`.
4. **[P3, kasıtlı ERTELENDİ] Verdict-aware yolun `pagination.total`'ı suppress edilmiş satırları hâlâ sayıyor** (post-merge filtre pagination'dan SONRA) — export route'un `has_more` yaklaşıklığıyla AYNI kabul edilmiş sınıf; keskin kenarı yalnız "son sayfanın TAMAMI suppress edilirse boş sayfa + yanıltıcı 'hiç firma yok, tarama başlat' metni" — nadir bir köşe durumu, bu turda dokunulmadı.
5. **[bilgi, düzeltme gerekmedi] Panellerin kendi proje/ICP seçicileri müşterinin wizard hedefinden BAŞKA bir yere gezinmesine izin veriyor** — kod-seviyesinde ZARARSIZ (wizard'ın kendi state'i hiç dokunulmuyor, "İleri" hep wizard'ın projesini kaydediyor), yalnızca kafa karıştırıcı olabilir. Kasıtlı ödün (aynı §4.24'ün "bilinçli UX ödünü" notu), düzeltilmedi.

**Sonuç:** WP9 review-2 "clean enough to trust" verdiktiyle KAPANDI (yukarıdaki 2 kalıntı da düzeltildi). WP10 ilk turu "clean enough to trust on correctness ve tenant-safety" verdiktiyle geldi, 2 P2 + 1 P3 düzeltildi. **İkisi de HÂLÂ canlı smoke görmedi ve HİÇBİRİ resmi olarak SHIP işaretlenmedi** — sonraki adım kullanıcı kararı: bir sonraki verify turu mu, canlı smoke mu, yoksa WP11'e mi geçilsin.

## 4.21 WP8a — sub-ICP kartları + ülke chip'leri + geo hücre kartları — ✅ **BİTTİ → bağımsız Claude review 4-tur zinciri → SHIP** (2026-07-09, UNCOMMITTED)

**Kavram:** wizard adım 7-11: adım 7 (mevcut ICP yoksa otomatik `icp:generate` + poll + atla-varsa-resume), adım 8 (persist `icp_card_index` cursor'ı ile **mevcut `IcpCard`'ın AYNEN yeniden kullanımı** + yeni `IcpCountryChips` [`POST /research/geographies` create-or-reuse, ekleme-only — silme endpoint'i yok]), adım 9 (spec'i olmayan tüm hücreler için toplu `geo:analyze` + kolektif poll), adım 10 (persist `geo_card_index` cursor'ı ile **`GeographiesPanel`'in Drawer içeriğinden ÇIKARILAN yeni `GeoCellDetail`** bileşeni — `GeographiesPanel` artık ince bir `<Drawer><GeoCellDetail/></Drawer>` sarmalayıcısı, kendi state/mutation/tablosu dokunulmadı), adım 11 placeholder (WP8b'yi bekliyor).
- **Olay (kritik değil ama not):** oturum ortasında ajan yanlışlıkla iki locale JSON dosyasında `git checkout --` çalıştırdı — hiç stage edilmemiş WP6/WP7 i18n key'lerini sildi. Ajan `en.json`'ı önceden yakalanmış bir çıktıdan (byte-exact) yeniden kurdu, `tr.json`'ı yeniden çevirdi (anlamca eşdeğer, orijinalle byte-eşit garantisi yok). **Bağımsız doğrulama (Fable):** `ResearchFlowPage.tsx`'teki 51 `t('research.wizard.*')` anahtarının TAMAMI iki dosyada da eksiksiz çözülüyor — fonksiyonel kayıp YOK.
- **Codex xhigh review DENENDİ — kullanım limitine takıldı (`ERROR: You've hit your usage limit`, reset ~2026-07-10 00:33).** Yerine **bağımsız Claude adversarial agent review** (Y2 ingest emsaliyle aynı disiplin, [P1]/[P2] etiketleme, somut repro şartı) çalıştırıldı, 7 dosya/2702 satır diff.
- **Bulgular: 2×P1 + 1×P2.** (1) `POST /research/icps/generate`'in in-flight guard'ı YOK (bu WP'deki DİĞER tüm job route'larının [`/analyze`, `/calibrate`, `/revise`] aksine) → adım 7'nin bekleme ekranında reload = **çift enqueue = çift Opus faturası + çift ICP**; (2) adım 8'de HİÇ ülke eklenmezse adım 9 doğru atlıyor ama adım 10 `currentCell=null` iken sonsuz, `WizardShell`siz, çıkışsız bir spinner'da TAKILI KALIYOR (URL'e elle `/research/full` yazmak dışında çıkış yok) — öngörülebilir bir ana-akış yolunda gerçek bir çıkmaz; (3) adım 8 kart 0'dan "Geri" no-op — adım 7'nin "ICP varsa otomatik ileri" guard'ı hemen geri iteliyor, adım 6 ICP üretiminden sonra erişilemez hale geliyor (adım 2'de zaten çözülmüş AYNI sınıftan bir hata, oraya taşınmamış).
- **FIX round 1:** `/generate`'e `findInflightAnalysis` kalıbıyla in-flight guard (`findInflightGeneration`); adım 10'un sıfır-hücre durumu adım 11'e düşürülüyor; adım 7'ye `explicitBackToStep7` latch'i (adım 2'nin `explicitBackNav`'ı ile aynı kalıp). `tsc -b`+eslint temiz.
- **Verify-1 (bağımsız Claude review, codex hâlâ limitte):** 3 iddia da UÇTAN UCA doğrulandı (`findInflightGeneration` tenant/tip/durum doğru + tek insert noktası; sıfır-hücre skip loading/truly-zero'yu doğru ayırıyor; `explicitBackToStep7` React 18 batching ile yarışsız çalışıyor, adım 6'ya gerçekten ulaşılabiliyor). **Ama TAZE tarama 1 YENİ P1 + 1 P2 buldu — aynı hata sınıfının kapsanmamış iki sınırı:** (1) adım 10'dan "Geri" **HER kullanıcı için kalıcı olarak kırık** — adım 9'un auto-advance guard'ı adım 10'a ulaşmış olmanın kendisi yüzünden zaten `geoAnalyzeAllDone=true` görüyor, Back'i anında iptal ediyor (adım 8'e dönüş imkânsız); (2) round-1'in sıfır-hücre düzeltmesi AYNI boşluğu adım 11→10'da tekrar üretmiş. **FIX round 2:** `explicitBackToStep9` + `explicitBackToStep10` eklendi; ayrıca agent kendi P2 düzeltmesinin YENİ bir çıkmaz yaratabileceğini fark edip adım 10'un render guard'ını ikiye böldü (loading vs. gerçekten-sıfır-hücre — ikincisi artık çalışan Back/Next'li gerçek bir `WizardShell`). `tsc -b`+eslint temiz.
- **Verify-2 (bağımsız Claude review):** **3 YENİ P1 + 1 P2 — henüz yakınsamadı.** (1) adım 9'un toplu `geo:analyze`'i iş bitince hücre listesini invalidate ETMİYOR → adım 10 normal akışta (Back gerektirmeden) BAYAT veri gösteriyor, elle yeniden-analiz ise zaten ödenmiş bir işi tekrar faturalandırıyor; (2) round-2'nin sıfır-hücre ekranının KENDİ Back butonu latch'i set etmiyor — aynı hata sınıfı bu turda yeniden üretilmiş; (3) adım 2/3 sınırında `explicitBackNav`'ın kapsamadığı DÖRDÜNCÜ bir auto-advance effect'i var (render-time check'ten AYRI, `crawlJobStatus`'a bağlı) — Step 3'ten Geri'ye basan HERKES anında Step 3'e geri fırlatılıyor (en yaygın navigasyon yollarından biri). **FIX round 3:** adım 9 sonrası `invalidateQueries`, sıfır-hücre ekranının Back'ine eksik latch çağrısı, adım 2/3'teki 4. effect'e `!explicitBackNav` (+ `retryCrawl()`'a `setExplicitBackNav(false)` — research-again döngüsünde `step` hiç 2'den ayrılmadığı için mevcut "leave step 2" temizleyicisi hiç tetiklenmiyordu). Agent dosyadaki **13 `useEffect`'in TAMAMINI** kendi taradı: yalnız 4'ü `step`'i hareket ettiriyor (2→3, 7→8, 9→10, 10→11), hepsi artık doğru latch'li; diğer 9'u ref-sync/latch-temizleme/mutation-başlatıcı/step-dokunmayan handler. `tsc -b`+eslint temiz.
- **Verify-3 (bağımsız Claude review): SHIP, 0 bulgu.** 13 effect sayısı BAĞIMSIZ olarak da doğrulandı (grep ile), 3 düzeltmenin hepsi satır satır izlendi (invalidate'in yalnız gerçek "az önce bitti" yolunda çalıştığı, `retryCrawl()`'ın hiçbir meşru senaryoda başka bir latch'i yanlışlıkla temizlemediği, sıfır-hücre Back'inin artık simetrik olduğu), tenant-izolasyon + ~41 `useState`'in tenant-reset bloğunda eksiksiz olduğu tek tek çapraz kontrol edildi. **1 backlog notu (engelleyici DEĞİL, reviewer'ın kendi ifadesiyle):** adım 8'e geri dönüp yeni bir ülke eklendikten sonra adım 9'a tekrar gelindiğinde `geoAnalyzeRequested` hâlâ true olduğu için yeni hücre için ilerleme göstermeden adım 10'a geçilebiliyor — ama server otomatik analiz zaten kuyruğa giriyor + `GeoCellDetail`'de elle "Tekrar analiz et" var, çıkmaz/veri kaybı YOK. Sonraki bir cilalama turuna not edildi, WP8a'yı bloklamıyor.
- **Sonuç:** 4 review turu (ilk 2×P1+1×P2, sonra 1×P1+1×P2, sonra 3×P1+1×P2, son tur 0 bulgu SHIP) — hepsi gerçek bulgu, çoğu aynı sınıftan tekrarlayan "auto-advance effect'i explicit-Back latch'ini atlıyor" hatası (4 farklı adım sınırında). Codex CLI limitte olduğu için tüm turlar bağımsız Claude adversarial agent'la yapıldı (Y2 ingest emsali). `graphify update .` node-count uyuşmazlığı uyarısı verdi (3027 vs 3029, `--force` istiyor) — kod değişikliğiyle ilgisiz bir önbellek koruması, zorlanmadı. UNCOMMITTED.

## 4.22 WP8b — kalibrasyon teker-teker — 🟡 **kod TAMAM, 8 codex round'u geçti, round 9 DURAKLATILDI** (2026-07-10, 3. oturum)

**Durum özeti:** implementasyon (bir önceki oturumdan devralınan background agent tarafından) tamamlandı: `client/src/lib/useCalibration.ts` (YENİ, Drawer'dan aynen çıkarılan shared hook), `CalibrationDrawer.tsx` artık bu hook'u çağırıyor, `ResearchFlowPage.tsx` adım 11-15'e genişledi, locale'ler additive güncellendi. Ardından **8 tur codex xhigh review** (model `gpt-5.6-sol`, `-c model_reasoning_effort="high"`) yapıldı — her tur gerçek bir bulgu buldu, düzeltildi, bir sonraki tur farklı/derinleşen bir permütasyon buldu (WP8a'nın 4 turluk aynı "auto-advance Back-latch atlıyor" hata sınıfının çok daha derin bir versiyonu). **Round 9 kullanıcının "codex'i şimdilik unut" talimatıyla YARIM KESİLDİ** — kod şu an round-8-sonrası halinde, `tsc -b`+`eslint --max-warnings 0` temiz ama codex'ten NİHAİ bir "clean" onayı ALINMADI.

**Round-round bulgular ve düzeltmeler (özet — detaylı satır numaraları git diff'te):**
1. **R1 (Sonnet fix-agent, codex öncesi kendi-trace):** `calibIcpId` pin timing (step 8 onayı sırasında erken pinleniyordu → step 11-14 aralığına gate edildi) + step-12 sıfır-oy skip-to-13 dead-end (Next artık `nothingRatedOnLastCard` ile disabled).
2. **R3 (codex xhigh, 1×P1+2×P2):** Back-navigation genel bir "arrival/run-token" (`calibStepTokenRef`, monoton artan `useRef`) ile 4 sınırda (11→12/12→13/13→14/14→15) korunmaya başlandı; step-12 zero-company dead-end (Resample action); adopted-409 job'ların "failed" gösterilmesi (tracked job status'a öncelik verildi).
3. **R4 (codex, 1×P1+2×P2):** 12→13 sınırı yalnız index-0'da kapanmıştı → GENELLEŞTİRİLDİ (her index + intra-loop advance de aynı token'ı taşıyor); sticky-isError job success SONRASI da hâlâ sızıyordu → Drawer'ın kendi mantığıyla birebir eşleştirildi (isError fallback'i tamamen kaldırıldı); tenant-switch token reset'i `0`'a (tekrar kullanılabilir bir değer) DEĞİL, monoton `+= 1`'e çevrildi.
4. **R5 (codex, 2×P1+1×P2 — GERÇEK bir tenant-izolasyon açığı):** tenant-switch sırasında `activeTenantIdRef` yalnızca bir passive effect'te güncelleniyordu → senkron render-time reset bloğuna taşındı (pencereyi DARALTMAK değil KAPATMAK için); "Tekrar örnekle" eski firma listesiyle step 12'ye ilerleyebiliyordu (`calibAwaitingResample` + `dataUpdatedAt` baseline eklendi); sıfır-ICP fallback'in Back butonu `explicitBackToStep10` latch'ini set etmiyordu.
5. **R6 (codex, 3×P1 — codex'in kendi ifadesiyle "ikisi zaten bilinen sınıfın permütasyonu, biri GERÇEKTEN yeni"):** `dataUpdatedAt` tek başına o SPESİFİK job'ın bittiğini kanıtlamıyordu (reconnect false-positive) → job-id'ye bağlı ikinci bir "success baseline" eklendi; resample click'i zaten uçan bir auto-advance PATCH'i durdurmuyordu → `calibAwaitingResample` effect'e eklendi + click token bump'lıyor; **`useCalibration.ts`'in KENDİSİNDE** tenant-switch leak'i (async `setJob`/`setRatings` yazımları reset SONRASI hâlâ landing yapabiliyordu) → `resetGenerationRef` (monoton) her mutation'ın `onMutate`/`mutationFn`'inde capture+check ediliyor, `jobQuery`'nin key'ine `resetKey` eklendi.
6. **R7 (codex, 2×P1+1×P2):** success-baseline hâlâ nedensellik kanıtlamıyordu → job'ın "ilk succeeded görüldüğü" anda İKİNCİ bir baseline eklendi; `useCalibration.ts`'te yalnız `jobQuery` `resetKey`'liydi, `icpQuery`/`companiesQuery`/`feedbackQuery` DEĞİL → hepsine eklendi (invalidation prefix-match doğrulandı, `exact:true` hiçbir yerde kullanılmıyor); Retry hiçbir resample state'ini (awaiting/job-id/baseline) resetlemiyordu → "Run sample" click'iyle birebir aynı reset akışına çevrildi.
7. **R8 (codex, 3×P1+1×P2 — BİRİ ÖNCEKİ TURUN KENDİ REGRESYONU):** ⚠️ R7'nin success-baseline fix'i normal (reconnect'siz) bir resample'da latch'i HİÇ serbest bırakmıyordu (job success olur olmaz `useCalibration` `job`'ı null'a çekiyor → `jobQuery` disabled oluyor → wizard'ın effect'i baseline karşılaştırmasına hiç ulaşamadan bail ediyordu — Next SONSUZA kadar disabled kalıyordu); ayrıca fix-agent kendi doğrulaması sırasında İKİNCİ, gizli bir hata daha buldu (success baseline, click-time baseline testi GEÇTİKTEN SONRA set ediliyordu → `X<=X` hep true, asla açılmıyordu) — **`calibResampleJobSucceededRef`** ile "job succeeded" bilgisi `jobQuery.data` kaybolsa bile ayrı bir ref'te tutulacak şekilde ayrıştırıldı, baseline artık succeeded GÖRÜLDÜĞÜ ANDA (threshold testinin ARKASINDA değil) set ediliyor. Apply-revision companies cache'i invalidate etmiyordu (eski-ruleset satırları yeni-ruleset feedback'i olarak kaydedilebiliyordu) → `applyMut.onSuccess`'e companies invalidate eklendi. Drawer, tenant switch sonrası `icp` prop fallback'i yüzünden eski tenant'ın ICP'sini gösterebiliyordu → `icpPropStaleRef` ile kapatıldı. **Ayrıca bu turda `client/src/contexts/AuthContext.tsx`'te (research'ün DIŞINDA, TÜM UYGULAMA çapında) bir tenant-izolasyon açığı bulundu:** `api.ts`'in request interceptor'ı her istekte `localStorage`'daki GÜNCEL tenant'ı okuyor, bir query'nin cache key'i hangi tenant'ta kurulmuşsa ona bakmıyor — tenant switch sırasında uçan bir fetch, YANLIŞ tenant'ın header'ıyla gidip YANLIŞ tenant'ın cache slot'una yazabiliyordu. Kullanıcıya sorulup **kapsam genişletilerek** (research dışı, app-genelinde) düzeltildi: `switchTenant`'a `queryClient.cancelQueries()` eklendi (tenant flip'inden ÖNCE) — TanStack Query v5'te bu, altta yatan axios isteği abort edilmese bile o fetch'in sonucunu query-client seviyesinde reddediyor.
8. **Round 9 dispatch edildi, codex kullanım limitine takıldı, kullanıcı "şimdilik unut" dedi → DURAKLATILDI.** Round 9'un review etmesi gereken 3+1 bulgu (regresyon fix'i, apply-revision cache fix'i, Drawer fallback fix'i, + AuthContext.tsx'in app-geneli fix'i) hâlâ codex'ten hiç geçmedi. Tam prompt metni (aynen yeniden kullanılabilir):

  > Round-9 re-review of TG-Research WP8b (client/src/pages/research/ResearchFlowPage.tsx one-company-at-a-time calibration wizard steps 11-15; client/src/lib/useCalibration.ts shared hook also used by client/src/components/research/CalibrationDrawer.tsx; client/src/contexts/AuthContext.tsx's switchTenant; client/src/locales/en.json, client/src/locales/tr.json). Ignore all other uncommitted changes in this working tree. Steps 1-10 remain out of scope except where shared machinery is involved.
  >
  > Round 8 found 3 P1 + 1 P2:
  >
  > 1. P1 REGRESSION — the round-7 resample success-baseline latch never released on a normal successful resample (useCalibration.ts clears job to null right after success, which disabled jobQuery before the wizard's effect could reach the baseline-snapshot logic). Claimed fix: added calibResampleJobSucceededRef, latched true the ONE render success is first observable, decoupling 'job succeeded' from 'jobQuery.data still populated' on later renders. While verifying this, the fixing agent found a SECOND, previously-masked bug in the same mechanism: the success baseline (calibResampleSuccessBaselineRef) was being captured AFTER the click-time baseline check passed, making it self-referential (X <= X, permanently unsatisfiable) in the normal no-reconnect case. Claimed fix: moved the anchor assignment to be captured atomically at the same moment calibResampleJobSucceededRef first flips true, not gated behind the threshold check. THIS IS THE MOST IMPORTANT THING TO VERIFY THIS ROUND — trace the actual render-by-render sequence yourself for a normal successful resample (no reconnect race) and confirm Next actually becomes clickable again, not just "looks less deadlocked."
  >
  > 2. P1 — applying a calibration revision didn't invalidate the companies query, so old-ruleset company rows stayed cached and could be rated as belonging to the new ruleset (data integrity). Claimed fix: applyMut.onSuccess now also invalidates the ['research','companies'] query family.
  >
  > 3. P2 — CalibrationDrawer could show the previous tenant's ICP via a stale prop fallback after a tenant switch (Drawer stays mounted, live fell back to the icp prop before the new-tenant fetch resolved). Claimed fix: added icpPropStaleRef, armed on the resetKey-change reset block, cleared once icpQuery.data actually arrives for the current key; live only falls back to the icp prop when this ref isn't armed.
  >
  > Separately (outside subagent scope, done directly): client/src/contexts/AuthContext.tsx's switchTenant now calls queryClient.cancelQueries() BEFORE flipping activeTenantId/localStorage/invalidateQueries — this was in response to a DEEPER finding last round: api.ts's request interceptor reads activeTenantId from mutable localStorage at request-dispatch time, not from whatever tenant a query's cache key was set up under, so an in-flight fetch during a tenant switch could send one tenant's header while its result lands under a different tenant's cache entry. Verify this specific fix closes that race — confirm TanStack Query v5's cancelQueries() actually causes an in-flight fetch's eventual result to be discarded (not committed) even when the underlying axios call has no AbortSignal wired and keeps running in the background to completion. This is core infrastructure used by the ENTIRE app (not just research), so scrutinize it carefully — check other callers/effects that might interact with cancelQueries() being called on every tenant switch (e.g. does canceling then immediately invalidating cause any query to end up stuck in an error/cancelled display state instead of cleanly refetching).
  >
  > Do a genuinely fresh whole-file pass too. This is round 9. Rank P1/P2/P3 with file:line. Be unambiguous: is WP8b (plus this incidental app-wide AuthContext fix) clean now, or not?

  Çalıştırma kalıbı (bu oturumda öğrenilen: Bash tool'un run_in_background'ı bile ~10dk'da kesiyor, detach şart):
  ```
  nohup codex exec -s read-only -m gpt-5.6-sol -c model_reasoning_effort="high" - < prompt.txt > log.txt 2>&1 &
  disown
  # sonra Monitor tool'uyla: while kill -0 $PID 2>/dev/null; do sleep 5; done; echo DONE
  ```

**Kilitli/doğrulanmış davranışlar (8 round boyunca hiç bozulmadı):** `CalibrationDrawer.tsx`'in kararlı-tenant CAS/`ratingsVersion`/mutation semantiği byte-for-byte aynı kaldı (her yeni guard tenant hiç değişmediğinde no-op); steps 1-10'un kendi navigasyonu (`calibStepToken` set etmedikleri için) hiç etkilenmedi; fenced RPC'ler/billing invariant'ları hiç değişmedi.

**Sonraki oturum için:** kullanıcı codex'e dönmeyi söylediğinde round 9'u (yukarıdaki 4 iddiayı kapsayan bir prompt'la) çalıştır, bulgu çıkarsa düzelt+re-review et, temiz gelirse WP8b'yi SHIP olarak işaretle (bu bölümü ve `05_SONRAKI_ADIMLAR.md`'yi güncelle) ve WP9'a geç.

**`CalibrationDrawer.tsx` (541 satır, TAMAMI okundu) iç yapısı — WP8b'nin reuse ettiği kalıp (spec, hâlâ geçerli):**

Component bir `Drawer` (sağdan açılan panel), `icp`/`opened`/`onClose` prop'ları alıyor, İÇİNDE 4 sıralı `Paper` bloğu var (kendi state machine'i zaten "adım adım" — wizard'a giydirmek görece kolay):

- **1. Sample** (`geography` text input + `source` web/maps segmented control + "Run sample" butonu) → `POST /research/icps/:id/calibrate` (`sampleMut`, server-forced küçük cap'lerle harvest). Gate: `canSample = live.status==='approved' && geography.trim().length>0 && !anyRunning`. Job poll (`jobQuery`, 2.5s interval) + 402/409 (in-flight adopt) hata kolları var.
- **2. Rate** (`companiesQuery` → `GET /research/harvest/companies?icp_id=X&limit=50`, sample'dan gelen firmalar tablo halinde: ad/website, verdict badge, skor, kanıt tooltip, 👍/👎 `ActionIcon` + not text input) → yerel `ratings` state'i (company_id → {rating, note}) → "Save" → `POST /research/icps/:id/feedback` (`feedbackMut`, `ratingsVersion` CAS — puanlanan ruleset'e pinlenir, 409'da ratings temizlenir).
- **3. Revision** ("Propose revision" butonu, `savedCount===0` iken disabled) → `POST /research/icps/:id/revise` (`reviseMut`, strategy/Opus job, poll). Sonuç `revision_draft` geldiğinde 4 ruleset dizisinin (`signals`/`negative_signals`/`neutral_signals`/`elimination_rules`) diff'i (`diffArrays`, kırmızı `−` / yeşil `+`) + `changes_summary` liste + `rationale` metni gösteriliyor. "Apply" butonu (`applyMut` → `POST /research/icps/:id/apply-revision`, ÇİFT CAS: `ruleset_version` + `revision_job_id` — ikisi de eşleşmeli, 409'da "changed, reloaded" uyarısı).
- **4. Finish** (`calibrated_at` varsa tarih gösterir, yoksa "Approve the logic" butonu `markMut` → `POST /research/icps/:id/mark-calibrated`, `live.status!=='approved'` iken disabled, 409/400 hata kolları var).

**WP8b'nin işi:** bu 4 bloğu, `Drawer` yerine wizard adım(lar)ına gömmek + kullanıcının "10 firma teker teker 👍/👎" isteğini karşılamak için **Adım 2 (Rate) bloğunu** tablo yerine **firma-başına-ekran cursor'a çevirmek** (WP8a'daki `icp_card_index`/`geo_card_index` ile AYNI kalıp — persist edilen bir `calibration_company_index`). Diğer 3 blok (Sample/Revision/Finish) muhtemelen aynen (bekleme ekranı biçiminde) kalabilir — tablo→teker-teker dönüşümü yalnız Rate adımında gerekli. **CalibrationDrawer'ın kendi mutation/query mantığına DOKUNULMAZ** (fenced RPC'ler, çift CAS, ratingsVersion pinleme hepsi AYNEN korunur) — yalnızca üstündeki UI (Drawer chrome + tablo) wizard adımlarına giydirilir.

**Önerilen wizard adım numaralandırması (WP8a'nın 11'inci adımından devam):** adım 11 → "İlk hedef firmalarınızı arıyoruz" (Sample, otomatik tetiklenir + bekleme ekranı + canlı anlatım) → adım 12 → firma-başına-ekran Rate (cursor, `calibration_company_index`) → adım 13 → Revision diff onayı (bekleme ekranı + diff review + Apply) → adım 14 → yeniden-approve + "Tekrar örnekle" seçeneği (adım 11'e geri döngü) VEYA "Mantığı onayla" (Finish) → adım 15 → placeholder (WP9'u bekliyor: ölçek ekranı + orkestratör).

**Hangi ICP için kalibrasyon yapılacağı:** WP8a'da müşteri onaylı BİRDEN FAZLA ICP olabilir (kart-kart onaylanmış). Kalibrasyon akışı muhtemelen "en yüksek skorlu onaylı ICP" ile başlamalı (ana-akış adım 12'nin niyeti budur) — ya da onaylı her ICP için sırayla kalibrasyon istenebilir (daha kapsamlı ama daha uzun). **Açık karar, WP8b'yi dispatch ederken netleştirilmeli** — basit başlangıç: en yüksek `human_score`'lu onaylı ICP ile tek kalibrasyon turu, diğerleri `/research/full` gelişmiş görünümden elle kalibre edilebilir (WP8b'nin scope'unu makul tutmak için).

## 4.20 WP7 — profile:crawl + FAZ 1 adım 2-6 — ✅ **BİTTİ → codex xhigh 9-tur zinciri → SHIP** (2026-07-09, UNCOMMITTED)

**Kavram:** yeni `profile:crawl` job'ı (website + ilk 3 sosyal link → `fetchPage` SSRF-korumalı fetch → ucuz `reading`-role LLM → zod şema → `research_projects.profile`'a **yalnız `ai_draft` (donmuş, hiç insan-düzenlemez) + boşsa `company_country`** merge-write; `what_they_do`/`products`/`differentiators`/`target_markets`/`exclusions`/`contact_name`/`social_links` KESİNLİKLE dokunulmaz — bunlar wizard adımlarının insan-onaylı yazdığı alanlar). Profile JSON şeması ResearchPage'in mevcut ICP-üretim alanlarıyla (`website`/`what_they_do`/`products`/`target_markets`/`exclusions`) ÇAKIŞMAZ — aynı key'ler bilinçli paylaşılıyor, wizard bittiğinde gelişmiş görünümün ICP formu zaten dolu geliyor.
- **Client:** `ResearchFlowPage.tsx` KNOWN_STEPS 2→7: adım 2 (crawl otomatik tetikler + job poll + canlı anlatım + `ai_draft` varsa otomatik atla), adım 3 (özet + firma ülkesi onayı), adım 4 (ürün/hizmet listesi onayı), adım 5 (farklılaştırıcılar, opsiyonel/atlanabilir), adım 6 (ipuçları: lookalike/hedef pazar/negatif liste, opsiyonel). Yeni route `POST /research/projects/:id/crawl` + in-flight guard (`geographies.ts` kalıbı).
- **Codex xhigh review (9 dosya, 1268 satır diff — ilk deneme 300s'de timeout, 570s'de tamamlandı):** **0×P1, 5×P2.** (1) adım 2 remount'ta crawl'ı ikinci kez tetikleyebiliyor; (2) crawl başarılı olduktan sonraki proje refetch'i başarısız olursa ekran sonsuza kadar takılı kalıyor; (3) `profileCrawl.ts` job/worker/lease ile fence'siz — girdi (website/social) ortasında değişirse veya reap edilmiş worker geç dönerse bayat `ai_draft` yazabilir; (4) crawl COGS'u admin marj paneline (`research_admin_cost_summary`) hiç girmiyor; (5) tenant-switch'te `hydrated` hiç sıfırlanmıyor — `invalidateQueries()` local `useState`'i temizlemediği için önceki tenant'ın verisi kalıcı kalabilir (WP6'nın query-key sertleştirmesinden FARKLI bir regresyon — bu local state, cache değil).
- **Triyaj:** (4) bilinçli ERTELENDİ — Hunter enrichment'ın kendi COGS-görünürlüğünün ayrı bir sonraki migration'da (103) kapatılmış olması emsal alındı, WP7'yi bloklamıyor. **FIX round 1:** (1)(2)(3)(5) düzeltildi — server-side already-crawled kısa-devre (+`crawlRequested` one-shot latch), refetch-hata için ayrı `fetchDraftMut` + `crawlFailed`/`draftFetchFailed` ayrımı, `profileCrawl.ts`'de hafif staleness-guard (website/social_links job-başında yakalanır, yazımdan hemen önce yeniden okunup karşılaştırılır — farklıysa `{skipped:true, reason:'input_changed'}`, tam lease-fence değil), tenant-değişiminde `hydratedForTenant` ile TÜM wizard local state reset + crawl job key'ine de tenantId. `tsc -b`+eslint temiz.
- **Verify-1 turu (codex xhigh, 9 dosya/1398 satır):** 4 iddia da doğrulandı ÇALIŞIYOR. **3 YENİ P2** — (1) `skipped` dönen bayat job hâlâ client'ı boş adım 3'e ilerletiyor (job.result.skipped kontrol edilmiyor); (2) adım 1 website'i zorunlu kılmıyor ama adım 2 website olmadan 400 dönüyor → jenerik "başarısız" döngüsü; (3) tenant-switch sırasında in-flight `saveStepMut`/`fetchDraftMut` callback'leri hangi tenant'ta başladığını kontrol etmiyor — reset SONRASI eski tenant'ın verisini geri yazabilir. **FIX round 2:** skipped-job'da otomatik yeniden-crawl (`crawlWasSkipped` + "girdileriniz değişti" mesajı) + adım 1'de website zorunlu alan + `activeTenantIdRef` ile üç mutation'ın (`saveStepMut`/`fetchDraftMut`/`crawlMut`) başlangıç-anı tenantId'sini yakalayıp mismatch'te no-op (agent kendi inisiyatifiyle `crawlMut`'a da uyguladı, tutarlılık için doğru karar). `tsc -b`+eslint temiz.
- **Verify-2 turu (codex xhigh, 9 dosya/1443 satır):** 3 iddia da doğrulandı (sonsuz-döngü riski yok, tenant guard'ı üç mutation'ın hepsinde eksiksiz). **2 YENİ P2 (daha dar kapsamlı)** — (1) eski/website'siz bir proje adım 2+'da resume edilirse yeni "website zorunlu" kuralını atlayabiliyor; (2) adım 3'ten "Geri" tuşu no-op. **FIX round 3:** resume'da `step>=2 && !website` ise adım 1'e clamp; Back'e özel tek-seferlik bayrak (`explicitBackNav`, "leaving step 2" effect'inde temizleniyor — tüketim anında değil, re-render yarışını önlemek için). `tsc -b`+eslint temiz.
- **Verify-3 turu (codex xhigh, 9 dosya/1475 satır):** **3 YENİ P2 (biri güvenlik-ilgili)** — (1) adım 1'de website değiştirilirse eski `ai_draft` GEÇERSİZLEŞMİYOR (hasAiDraft true kalıyor → adım 2 otomatik ileri atlıyor + server zaten-draft-var diye yeni crawl'ı reddediyor → adım 3-5 ESKİ website'in verisini gösterir); (2) round-3'ün `explicitBackNav` düzeltmesi YENİ bir çıkmaz yarattı — draft varken Back'le adım 2'ye dönülünce hem auto-skip hem auto-crawl guard'ı devre dışı kalıyor → sadece spinner, ileri gitme yolu yok; (3) **`profile/prompt.ts`'de gerçek bir prompt-injection fence sızıntısı** — `input.website` `<<<END_UNTRUSTED_DATA>>>` kapanışından SONRA, yani GÜVENİLİR talimat bölgesinde interpolate ediliyor (`geo/prompt.ts`/`icp/prompt.ts`'nin "untrusted veri asla trusted bölgede olmaz" disiplinine aykırı). **FIX round 4:** adım 1'de website/social_links DEĞİŞİRSE `ai_draft` + adım 3-5 seed bayrakları temizlenir; `step===2 && explicitBackNav && hasAiDraft` için "zaten araştırıldı" kartı (İleri + Tekrar-araştır, `researchAgainMut` önce server'da `ai_draft:null` PATCH'ler — crawl route'un zaten-var guard'ını bypass etmek için — sonra `retryCrawl()`); website tamamen prompt input'undan çıkarıldı, sadece fence içindeki sayfa bloğu başlığında geçiyor (`geo/prompt.ts` kalıbı). `tsc -b`+eslint temiz.
- **Verify-4 turu (codex xhigh, 9 dosya/1560 satır): fence sızıntısı TAM KAPANDI.** **2 YENİ P2** — (1) adım 1'de input değişince adım 3-5 seed'leri temizleniyor ama `crawlRequested`/`crawlJobId` TEMİZLENMİYOR → adım 2 eski job'ın terminal durumunu gösterebiliyor; (2) `researchAgainMut` sırasında İleri butonu hâlâ tıklanabilir (yarış: draft temizlenir ama yeni crawl başlamaz) + yalnız `profile` PATCH'liyor, `flow_state` değil → server step'i 4+ ise reload eski (artık bayat) adıma döner. **FIX round 5:** input-değişti dalında `retryCrawl()` senkron çağrılır (crawlJobId/crawlRequested/crawlWasSkipped sıfırlanır, query key değişir → eski terminal job cache'i izole kalır); `researchAgainMut` artık `flow_state:{step:2,completed_gates}` de PATCH'liyor + pending sırasında Next/Back/buton'lar disable. `tsc -b`+eslint temiz.
- **Verify-5 turu (codex xhigh):** `retryCrawl()` temizliği ve tenant/fence/registration hepsi TEMİZ doğrulandı. **1 dar P2** kaldı — `researchAgainMut` paylaşılan `latestResearchProjectQueryKey` cache'ini güncellemiyor (hard-reload'da sorun yok ama SPA remount'ta bir kez bayat proje okunabilir). **FIX round 6:** `saveStepMut`'ın cache-write kalıbı `researchAgainMut` + `fetchDraftMut`'a da uygulandı. `tsc -b`+eslint temiz.
- **Verify-6 turu (codex xhigh):** cache-sync + tenant guard'ları TEMİZ. **1 dar P2** — `profileCrawl.ts`'nin `company_country: current ?? value` (yalnız boşsa doldur) kuralı input-değişince TEMİZLENMİYOR → eski website'in ülkesi yeni crawl'ın doğru tahminini engelliyor; ilişkili ikinci nokta: adım 3-5'in "zaten cevaplandı" sinyali (`stepNSeeded`) yalnız LOCAL state — reload'da sıfırlanıyor, kasıtlı BOŞ bırakılmış bir alan (ör. ürün listesi boş kaydedilmiş) reload sonrası truthiness kontrolüyle ai_draft'tan yeniden dolabilir. **FIX round 7:** input-değişti dalında `company_country` da temizlenir + `removeGates:['step3']` ile gate demote edilir (aksi halde round-6'nın "gate tamamsa asla ai_draft'a dönme" kuralı bu düzeltmeyi geçersiz kılardı — agent bu etkileşimi kendi tespit etti); "cevaplandı mı" sinyali `flow_state.completed_gates`'e taşındı. `tsc -b`+eslint temiz.
- **Verify-7 turu (codex xhigh):** aynı sınıftan **1 P2** — `removeGates` yalnız `step3`'ü demote ediyor, `step4`/`step5` (ürünler/farklılaştırıcılar) da AYNI kilitlenme riskini taşıyor ama unutulmuş. **FIX round 8:** `removeGates:['step3','step4','step5']`. `tsc -b`+eslint temiz.
- **Verify-8 (final) turu (codex xhigh): SHIP, 0 bulgu.** "4. alan da mı aynı sorunu taşıyor" sorusu özellikle test edildi: `what_they_do` zaten `step3` demote'una dahil; adım 6 (`lookalike_customers`/`target_markets`/`exclusions`) muaf çünkü hiçbir zaman `ai_draft`'tan seed edilmiyor (yalnız final profile alanlarını okuyor). Merge-write, fence, tenant-izolasyon, cache-sync, adım 1-7 UX akışı hepsi temiz. `tsc -p client` + `tsc -p server` + iki locale JSON'ı da PASS.
- **Sonuç:** 9 codex xhigh turu (0×P1 sabit; P2: 5→3→2→3→2→1→1→1→1→0), her turda gerçek (uydurma olmayan) bulgu — özellikle **gerçek bir prompt-injection fence sızıntısı** (round 5) ve **çoklu gate-demotion/staleness zinciri** (round 6-8, `ai_draft`↔`company_country`↔`completed_gates` etkileşimi) dahil, hepsi kapatıldı. 1 madde bilinçli ERTELENDİ (crawl COGS admin-panel görünürlüğü — Hunter enrichment emsaliyle, ayrı bir sonraki migration'a). `graphify update .` çalıştırıldı (topology değişikliği yok). UNCOMMITTED.

## 4.18 ANA AKIŞ YENİDEN KURGUSU + WIZARD-FIRST PLAN (A-1) — ✅ **PLAN TAMAM, KOD YOK** (2026-07-09)

**Ne yapıldı (yalnız doküman — kod/migration değişikliği YOK):**
- **`tg-research-ana-akis.md` baştan yazıldı** (kanonik müşteri akışı): "tek ekran, tek iş" (Typeform) ilkesi anayasa yapıldı; 7 faz / 24 adım — FAZ 1 tanışma (4 alanlı kurulum → `profile:crawl` → özet/ürün/farklılaştırıcı onayları), FAZ 2 sub-ICP kartları (kart-kart, altında dropdown'lu ülke chip'leri) + geo yerelleştirme, FAZ 3 kalibrasyon (firma-başına-ekran 👍/👎 → revizyon diff → mantık onayı), FAZ 4 offer/açı kartları, FAZ 5 ölçek + derin araştırma (Y1+Y2+Y3, doygunluk), FAZ 6 Hunter kişiler, FAZ 7 CRM devri + geri-besleme + yaşayan döngü. v1'in 23 maddesinin tamamı eşlenerek korundu (dosya sonunda eşleme tablosu). Sonradan inşa edilen katmanlar (WP1-WP5, enrichment, export pin) akıştaki yerlerine oturtuldu. **Akış/mimari çelişirse ana-akış dosyası kazanır.**
- **Kod envanteri (graphify + route/schema taraması):** yeniden yazım gerekmediği doğrulandı — `research_projects.status` CHECK'i (`setup→icp→calibration→scaling→enrichment→handoff`) wizard fazlarını 055'ten beri taşıyor; `profile JSONB` + `scale_target` kolonları hazır; `research_jobs.progress JSONB` canlı-anlatım zemini; 11 route dosyasında adım 9-23'ün TÜM endpoint'leri mevcut (icps calibrate/feedback/revise/apply/mark-calibrated, geographies create-or-reuse/analyze/approve, channels discover/harvest/coverage, offers CRUD, enrichment, export); 7 panel bileşeni (IcpCard, CalibrationDrawer, GeographiesPanel, CompaniesPanel, OffersPanel, TradeImportsPanel, EnrichmentPanel) yeniden giydirilecek. Gerçekten eksik: wizard kabuğu (`flow_state` işaretçisi, mig 105), `profile:crawl` handler'ı, kart-kart/firma-başına-ekran UI reflow'ları, ölçek ekranı, `research:orchestrate` kondüktör job'ı, müşteri-yüzü suppress route'u, onboarding yönlendirmesi. HS/TradeMap (adım 7-8) ertelendi (tablolar 056'da var, handler yok).
- **ÜRÜN KARARI: TG-Research = TG-Core'un İLK ADIMI.** Yeni tenant login → CRM değil `/research` wizard'ı (ilk export'a kadar). Ürün-akışı kararı; **K10'a dokunmaz** (ayrı Railway projesi + izole DB kalır; TG-Core prod yüzeyi export + auth). Yönlendirme client'ta (`App.tsx:118` route + `Layout.tsx:121` nav); canlı doğrulama staging'de.
- **Plan `05_SONRAKI_ADIMLAR.md`'ye A-1 (WP6-WP10) olarak yazıldı:** WP6 wizard iskeleti + giriş (mig 105 `flow_state`; sekmeli ResearchPage `/research/full`'da "gelişmiş görünüm" olarak AYNEN kalır) → WP7 `profile:crawl` + FAZ 1 → WP8 FAZ 2-3 giydirme → WP9 ölçek + orkestratör + canlı anlatım → WP10 sonuçlar/suppress/kapanış. Her WP mevcut disiplinle (izole DB migration + tsc + smoke + codex xhigh).
- **WP11 eklendi (aynı gün, kullanıcı talebi):** geo onayı (adım 10) için TradeMap **API** (scrape değil) ile HS kod eşleme + **satıcı ülkesinden aday ülkeye bilateral ihracat** (hacim+büyüme) — genel "dünya ithalatı" rakamından daha güçlü kanıt. `tg-research-ana-akis.md` adım 7-8-10 güncellendi (bağımlılık: `profile.company_country`, WP7'nin ürünü olmalı). WP11 spec'i `05_SONRAKI_ADIMLAR.md`'de; **WP6-10'dan SONRA** sıraya girer — açık konu: TradeMap API erişimi/plan.

## 4.19 WP6 — WIZARD İSKELETİ — ✅ **BİTTİ → codex xhigh 4-tur zinciri → SHIP** (2026-07-09, UNCOMMITTED)

- **WP6 inşa edildi** (Sonnet coding agent, orkestratör=Fable): migration 105 (`research_projects.flow_state JSONB DEFAULT '{}'`, izole DB'ye uygulandı) + `updateProjectSchema`'ya `flow_state` + yeni `WizardShell.tsx`/`ResearchFlowPage.tsx`/`RootRedirect.tsx` + `App.tsx` route değişikliği (`/research`→wizard, `/research/full`→eski sekmeli sayfa, `/`→`RootRedirect`) + i18n `research.wizard.*`. `tsc -b` (client+server) temiz.
- **Codex gpt-5.5 xhigh review (custom-instructions path, DIFF_START/END fence'li, 8 dosya):** **2×P1 + 3×P2** gerçek bulgu — (P1) `ResearchPage.tsx`'in PATCH'i profile'ı 4-5 alanlık alt-kümeyle wholesale eziyor (server JSONB merge yapmıyor) → `contact_name`/`social_links` silinme riski; (P1) "gelişmiş görünüme geç" linki hiçbir project referansı taşımıyor → `ResearchPage` boştan başlayıp İKİNCİ bir proje satırı yaratıyor; (P2) `LinkedInConnectPage.tsx` hâlâ eski `/research?tab=...`'a dönüyor (artık wizard, sekme yok); (P2) `RootRedirect` ile `ResearchFlowPage`'in ayrı query-key'leri var → ilk proje yaratıldıktan sonra `/` bayat cache'le tekrar wizard'a atabilir; (P2) create+PATCH iki ayrı istek → retry'de duplicate proje riski.
- **FIX round 1:** aynı Sonnet ajanına (SendMessage, context korunarak) 5 bulgunun tam düzeltme talimatı verildi — profil merge-before-patch, query-param'lı proje devri, LinkedIn deep-link düzeltmesi, paylaşılan query-key (`client/src/lib/researchProjects.ts`), `createProjectSchema`'ya `flow_state` ekleyip tek-istekli create. Tamamlandı: `tsc -b` (client+server) + eslint temiz.
- **Verify turu (codex xhigh, 11 dosya):** 2/5 tam kapandı (LinkedIn deep-link, tek-istekli create). Kalan **1×P1 + 2×P2** — (P1) `?project=` hydrasyonu async, GET tamamlanmadan Generate'e basılırsa `projectId` hâlâ null → ikinci proje riski TİMİNG PENCERESİNE taşındı, yapısal olarak kapanmadı; (P2) `ResearchFlowPage`'in create-sonrası `invalidateQueries`'i senkron değil — React Query v5'te `RootRedirect` stale boş cache'i görebilir; (P2) `ResearchPage`'in kendi doğrudan-create yolu paylaşılan cache key'e hiç dokunmuyor.
- **FIX round 2:** aynı ajana gönderildi — hydrasyon-tamamlanana-kadar-guard (`hydratedFromProjectParam` flag'i, GET hatasında sessiz düşme yerine açık hata), `setQueryData` ile senkron cache güncellemesi (hem wizard create hem advanced-view create yolunda). Tamamlandı: `tsc -b` + eslint temiz.
- **Verify-2 turu (codex xhigh):** round-2'nin 3 iddiası da doğrulandı ÇALIŞIYOR, ama **2 yeni P2** buldu — (P2) `ResearchFlowPage`'in kendi ilk-yükleme hatası sonsuza kadar gizli kalıyor (`isLoading || !hydrated` kontrolü `isError`'dan önce kazanıyor, `hydrated` bir hata durumunda asla true olmayacağı için); (P2) `ResearchPage`'in PATCH dalı **YANLIŞ projeyi** "en son proje" cache'ine yazabiliyor — `?project=<eski-id>` deep-link'iyle açılan ESKİ bir proje PATCH'lenirse, o eski proje "en son" gibi cache'e yazılır ve `ResearchFlowPage` yanlış projeyi resume eder (gerçek bir tasarım hatası, sadece mekanik bir eksiklik değil). Verdict: FIX-FIRST.
- **FIX round 3:** aynı ajana gönderildi — render sırası düzeltmesi (`isError` `hydrated`'dan bağımsız kontrol edilir) + PATCH dalından `publishToSharedCache()` tamamen kaldırılması (yalnız CREATE dalı "en son proje" cache'ini yazar — PATCH edilen proje tanım gereği "en son" olmayabilir). Tamamlandı: `tsc -b` + eslint temiz.
- **Verify-3 (final, holistic) turu (codex xhigh):** round-3'ün 2 iddiası doğrulandı ÇALIŞIYOR. 2 YENİ bulgu — (P1) yeni 2 query-key tenantId taşımıyor (cross-tenant stale-read teorik riski); (P2) whole-profile PATCH, hipotetik gelecekteki eşzamanlı bir arka-plan yazıcıya karşı hâlâ kırılgan. **Fable triyajı:** P1 için `AuthContext.tsx` incelendi — `switchTenant()` zaten `queryClient.invalidateQueries()` (filtresiz, TÜM query'leri) çağırıyor; bu, uygulamadaki **HER** query key'in (mevcut `['research','icps',projectId]` dahil) zaten sahip olduğu AYNI koruma seviyesi — WP6'ya özgü bir regresyon değil, app-genelinde var olan bir desen. Yine de ucuz olduğu için 2 yeni key'e `activeTenantId` eklendi (round 4, aşağıda) — app-geneli düzeltme WP6 kapsamı DIŞINDA bırakıldı. P2 için: bugün `research_projects.profile`'a başka HİÇBİR yazıcı yok (profile:crawl henüz yok) — gerçek bir çakışma yolu yok; **WP7'ye not olarak taşındı** (`profile:crawl` handler'ı fetch-merge-write ile yazmalı, blind-overwrite değil — zaten planlanan tasarım).
- **FIX round 4 (küçük, düşük riskli):** `LATEST_RESEARCH_PROJECT_QUERY_KEY` sabiti → `latestResearchProjectQueryKey(tenantId)` fonksiyonuna çevrildi; hem `useLatestResearchProject()` hem `ResearchFlowPage`/`ResearchPage`'in doğrudan `setQueryData`/`invalidateQueries` çağrıları `activeTenantId` (useAuth()) ile bu fonksiyonu kullanıyor; `ResearchPage`'in ayrı `?project=` deep-link key'i de `activeTenantId` aldı. Diğer pre-existing key'lere (`['research','icps',projectId]` vb.) DOKUNULMADI — kasıtlı, app-geneli `invalidateQueries()` güvenlik ağıyla tutarlı kalması için.
- **Verify-4 (final) turu (codex xhigh): SHIP, bloklayıcı bulgu yok.** Tenant-key değişikliği tutarlı doğrulandı (eski sabite hiç referans kalmamış, reader/writer key-şekli eşleşiyor); tüm WP6 yolu (root redirect, wizard create/patch, advanced-view deep-link, server create/patch, route taşımaları, LinkedIn redirect) baştan okundu, yeni regresyon yok. `tsc -p client/tsconfig.app.json --noEmit` + `tsc -p server/tsconfig.json --noEmit` ikisi de PASS.
- **Sonuç:** 4 codex xhigh turu (2P1+3P2 → 1P1+2P2 → 2P2 → SHIP), her turda gerçek bulgu, hepsi düzeltildi veya bilinçli-kabul olarak triyaj edildi (tenant-key: app-geneli mevcut desenle tutarlı olduğu için WP6 kapsamını genişletmedi, sadece 2 yeni key'e ucuz iyileştirme yapıldı; whole-profile-clobber: bugün yazıcı yok, WP7'ye not). `graphify update .` çalıştırıldı (topology değişikliği yok, AST cache tazelendi). UNCOMMITTED.

## 4.17 EXPORT-ANI ICP/GEO PİNLEME (WP5 §4.13 P3 kapandı) — ✅ **BİTTİ → codex 1×P2 → fix → verify RESOLVED + SQL smoke** (2026-07-09, migration 104, UNCOMMITTED)

**Sorun (§4.13'te dokümante P3):** `feedback:aggregate` kampanya sonuçlarını firmanın **GÜNCEL rollup** (icp_id, geo_id) hücresine atfediyordu. Export'tan SONRA başka bir ICP altında yeniden keşfedilen firma yanlış hücreye rapor ediyordu (upsert+prune recompute tüm hücreyi taşıyıp yanıt-oranı istatistiğini ve icp:revise kanıtını bozuyordu).

- **Migration 104** (`research_export_icp_pin`, izole DB'de uygulı): `research_companies` +`crm_exported_icp_id`/`crm_exported_geo_id` (export-anı pin) + **FK ON DELETE SET NULL** (ikisi de) + kısmi index; `research_mark_exported` **2→3 arg** (`p_icp_id`, DEFAULT NULL) — UPDATE'te icp'yi pinler + `crm_exported_geo_id = c.geo_id` ile satırın o-anki rollup geo'sunu snapshot'lar (**yalnız ilk export'ta**, `crm_company_id IS NULL` guard'ı zaten geçerli; re-export atfı yeniden yazamaz). Eski 2-arg DROP → overload ambiguity yok. Billing/verdict/rollup'a DOKUNMAZ (dar export-takip RPC'si, advisory-lock serileşmesi + suppression re-check aynen).
- **Server:** export route (`harvest.ts`) `p_icp_id: icp_id` geçer; `feedbackAggregate.ts` `effectiveIcp`/`effectiveGeo` helper'ları (pin varsa pin, yoksa rollup) — ruleset/angle türetme, bump anahtarı, upsert anahtarı, prune anahtarı hepsi effective üzerinden; **legacy NULL-pin satırlar eski davranışa düşer** (geriye uyumlu).
- **Codex xhigh:** 1×**P2** — pin kolonları FK'sizdi ama `research_outcome_stats.icp_id/geo_id` FK'li: proje-silme cascade'i ICP/geo'yu düşürüp pinli firmayı sağ bırakınca dangling pin → sonraki aggregate outcome-stats upsert'ünde FK ihlali → job fail. **FIX:** iki pin kolonuna `ON DELETE SET NULL` FK (silinen ICP/geo → pin null → rollup fallback). **Verify: RESOLVED.** Codex'in perf notu (parent-delete seq-scan) da karşılandı: kısmi index ×2.
- **Smoke (izole DB, self-cleaning):** pin+geo-snapshot / guard-repin-block / 2-arg-legacy-NULL / non-null-geo-snapshot / **FK-cascade** (geo-delete→geo-pin null, icp-delete→icp-pin null, firma sağ) — **hepsi PASS, 0 residue**. `tsc -b` server temiz.
- **NOT (replay):** izole DB migration-tracker'ı ilk apply'ı (kolonlar+fonksiyon) kaydetti; FK+index'ler raw SQL ile eklendi ama **104 dosyası tam+idempotent** — prod replay dosyadan tümünü alır.

## 4.16 HUNTER ENRICHMENT COGS GÖRÜNÜRLÜĞÜ (§4.15 P3 kapandı) — ✅ **BİTTİ → codex xhigh SHIP (0 bulgu) + SQL smoke** (2026-07-09, migration 103, UNCOMMITTED)

**Amaç:** §4.15'te dokümante edilen P3 — admin marj panelinde Hunter-istek COGS satırı yoktu. Her `enrich:run` domain-search isteği 1 Hunter kredisi; handler istek sayısını zaten job result'ına (`summary.hunter_requests`, top-level) yazıyor. Bunu tenant başına yüzeye çıkarır.

- **Migration 103** (`research_enrichment_cogs`, izole DB'de uygulı): `research_admin_cost_summary` **2→3 arg** (`p_hunter_usd NUMERIC DEFAULT 0`) — `jobs` CTE'sine `enrich_runs` (succeeded) + `hunter_requests` (Σ `result->>'hunter_requests'`, top-level anahtar) + çıktıya `hunter_cost_usd = hunter_requests × p_hunter_usd` (GREATEST ile ≥0 clamp). Eski 2-arg DROP → overload ambiguity yok. **Reporting-only**: billing/ledger/hold'a dokunmaz; enrichment ayrı faturalanır (`research_enrichment_events`). `cost_per_lead_usd` HARVEST-only kalır (Hunter ayrı ürün hattı, per-MATCH COGS'a katılmaz).
- **Server:** `pricing.ts` +`HUNTER_PER_REQUEST_USD` (`RESEARCH_PRICE_HUNTER_REQUEST`, default **0 = free/trial planı** → ham istek SAYISI anlamlı figür; ücretli plan gelince env ile gerçek oran). `admin.ts` `/costs` route p_hunter_usd geçer (tek config kaynağı = pricing.ts).
- **Client:** `ResearchAdminPage` fleet tile "Hunter enrichment" (istek sayısı + $, tooltip) + tenant tablosunda `Hunter req`/`Hunter $` kolonları (tooltip: ayrı ürün hattı, per-lead COGS değil, free planda $0). i18n TR+EN. `tsc -b` server+client temiz, eslint temiz.
- **Smoke (izole DB, gerçek 4 hunter_requests / 7 succeeded enrich:run):** P1 default→$0 / P2 paid 0.068→$0.272 (=4×0.068) / P3 negatif→clamp 0 / P4 per-tenant / P5 harvest regression clean (8 run, 6 billed) / P6 2-arg overload çözülüyor — **ALL PASS**. **Codex xhigh: SHIP, 0 bulgu.**

## 4.15 HUNTER CONTACT ENRICHMENT (kişi/karar-verici) — ✅ **BİTTİ → 3-tur codex zinciri + canlı smoke** (2026-07-09, commit `2d27d85`)

**Kavram:** müşteri MATCH firmalarını seçer → Hunter domain-search → **STRICT domain match** (Hunter'ın döndürdüğü domain ≠ firmanın registrable domain'i ise persist/fatura YOK) → **çok-dilli title bundle** sıralaması (8 bucket × EN/DE/FR/ES/IT/NL/PL/TR; founder_exec/purchasing[einkauf, einkäufer, procurement, satın alma…]/sales/marketing/operations/technical/quality/logistics; müşteri custom kelimeleri hepsinden önce) → firma başına max kişi (1-10, default 3) → **kişi bulunan firma başına 1 kredi, ömürde-bir** (`research_enrichment_events` UNIQUE(tenant,canonical_key) — MATCH invariant'ına AYRI tablo, dokunulmadı). Kredi havuzu ORTAK (`research_usage_ledger`, reason='enrichment').

- **Migration 102** (`research_enrichment`, izole DB'de uygulı): foundation `research_contacts` GENİŞLETİLDİ (job_id/seniority/department/confidence/title_bucket/domain/email_type + partial unique(tenant,company,email) + source CHECK'e 'hunter') + `research_enrichment_events` (RPC-only: PUBLIC+service_role dahil DML revoke) + `research_bill_enrichment` fenced RPC (bill_match kalıbı: per-tenant advisory lock + suppression + lease fence + hold tüketimi + ledger −1; dedup'ta hold'a dokunmaz).
- **Hunter maliyet disiplini:** istek=1 Hunter kredisi → retry YOK, pagination YOK, free-plan `limit≤10` clamp'i (`RESEARCH_HUNTER_PAGE_LIMIT`, canlıda 400 pagination_error ile öğrenildi), run başına `RESEARCH_HUNTER_RUN_CAP` (25), quota/auth reddi döngüyü DURDURUR, already-enriched = 0 istek (bedava skip), bill→insert crash'i = bedava BACKFILL (event-var-kişi-yok → tek istek, dedup fatura).
- **Fatura sırası (codex P1'leri):** Hunter→rank→**tenant-genelinde yenilik kapısı** (müşterinin HİÇBİR yerde olmayan ≥1 e-posta yoksa fatura yok; case-insensitive ilike) → **BILL önce** (suppressed/fenced ret → hiçbir veri kalmaz) → kişi insert. Fresh listesi `hold.reserved`'a clamp'li (rezervsiz Hunter isteği atılamaz); route 402'yi yalnız fresh-chargeable seçimde döner (bedava re-run/backfill kredisiz tenant'ta çalışır); route'ta tenant-mutex (çift enqueue = çift Hunter isteği kapandı).
- **CRM kontak aktarımı:** export artık **self-healing sweep** — CRM'e bağlı TÜM zengin firmaların kişileri TG-Core `contacts`'a kopyalanır (keyset-paginated, e-posta dedup'lu, kontaksız firmanın ilk kişisi `is_primary`; kopya hatası şirket export'unu düşürmez, sonraki export onarır). Yanıt: `contacts_exported`.
- **UI:** ResearchPage "Kişiler" sekmesi — bucket öncelik seçici (tıkla-öne-al), custom kelimeler, max kişi, **maliyet önizlemesi** (seçili N − zaten-zengin M = en fazla X kredi; sayfa ∪ seçim status'u), kişi drawer'ı (domain-match kanıtı + bucket/confidence rozetleri). i18n TR+EN.
- **Review zinciri:** codex xhigh R1 **4×P1+4×P2** (bill-önce-persist, hold clamp, route yarışı, reconciliation faturası; sweep, PUBLIC revoke, cross-page status, sıra UX) → düzeltildi → R2 **1×P1+1×P2** (tenant-wide yenilik, 402 sınıflandırması) → düzeltildi → R3 **1×P2** (case-normalizasyon) → düzeltildi. `temp/enrichment-smoke.sql` **P1-P6 ALL PASS** (fence/once-ever/suppression/lease/exhaustion/null-hold, kendini geri sarar). Canlı smoke ×2 (intercom.io + stripe.com, ~3 Hunter kredisi): 1 istek → 3 personal kişi (founder_exec/technical sıralı) → 1 kredi; re-run → 0 istek/0 fatura. Smoke bulgusu: bare 'president' substring'i "VICE president"i yutuyordu → kelime paketten çıkarıldı.
- **Bilinen sınır (dokümante):** Hunter'ın tüm e-postaları tenant'ta scrape kaynaklı zaten varsa backfill hiç tamamlanmaz (her explicit re-seçimde 1 istek); UI event sayısını gösterdiği için pratikte tekrar seçilmez.

## 4.14 RAILWAY DEPLOY (A-0 build canlıda) — ✅ **BİTTİ → codex PASS** (2026-07-09)

**Sonuç:** worker (`2b0b982b`) + research-api (`05bb9f5e`) Railway'de commit `52569e2` ile CANLI; `/api/health` OK; post-deploy smoke: SQL-enqueue edilen `feedback:aggregate` job'ını deploy edilen worker ~1sn'de `succeeded` işledi (zero-export prune yolu; `crm_read_errors=0`). Yeni handler'lar (channels:discover/harvest, offer:generate, feedback:aggregate, calibrate/geo/icp:revise) canlı registry'de.

- **Ön-koşul doğrulandı:** migrations 090-100 izole DB'de (`research_geo_persist_projection` → `research_outcome_stats_2`) `list_migrations` ile teyit — deploy sırası ihlal edilmedi.
- **Deploy yöntemi:** clean **HEAD archive** stage (`git archive HEAD` → scratchpad) + `deploy/<svc>/railway.json` root'a — çalışma ağacındaki commit'lenmemiş LinkedIn Faz 4 hunk'ları build'e SIZMADI (infra/README §staged-archive; working-tree rsync yerine bilinçli archive).
- **İlk worker deploy'u CRASH etti → 2 kök neden, ikisi de import-zamanı:** (1) LinkedIn Faz 2/3 handler'ları worker grafiğine `lib/linkedin/crypto → middleware/errorHandler → lib/posthog`'u soktu; PostHog keysiz constructor'da assert atıyor → **fix `161d113`**: keyless-safe init (placeholder key + `disabled:true`; keyli süreçlerde davranış birebir). (2) WP5 `feedbackAggregate` CRM client'ını (`lib/supabase`) statik import ediyordu — worker'ın prod-izolasyon sözleşmesini deliyor → **fix `52569e2`**: lazy dynamic import + savunmacı degrade (client yoksa tüm CRM sinyali sıfır, `crm_read_errors=1`; env'li ortamda davranış aynı). İkisi de lokal keyless boot testiyle kanıtlandı ("research worker started"), sonra deploy.
- **Codex xhigh review (iki commit diff'i): GATE PASS, 0 bulgu** — SDK (posthog-node 5.29.2) disabled yolunun gerçek no-op olduğu, lazy cache'in bozuk client yakalayamayacağı, kısmi CRM hatalarının savunmacı kaldığı doğrulandı.
- **Ortam gerçeği (README düzeltmesi):** Railway worker'da `SUPABASE_*` ZATEN set ve **izole test DB'ye** işaret ediyor (prod DEĞİL) — yani canlıda feedback:aggregate degrade değil TAM çalışır; prod cred yok invariant'ı ruhen korunuyor. `POSTHOG_API_KEY` worker'da yok (bilinçli).

## 4.13 WP5 — KAMPANYA GERİ-BESLEME AGREGATI — ✅ **BİTTİ → codex SHIP** (2026-07-09)

**Kavram:** "doğru offer" iddiası artık ÖLÇÜLÜYOR. `feedback:aggregate` (worker; günlük tick + admin run-now) research'ün EXPORT ettiği firmalar (`crm_company_id` set) için TG-Core kampanya sinyallerini okur — `email_replies` (OUT=sent; IN satırlarında label INTERESTED/category positive|meeting_request=positive; category not_interested VEYA label NOT_INTERESTED=opt-out) + `campaign_enrollments` (unsubscribed=opt-out) — ve `research_outcome_stats`'a (ICP × geo × açı) hücreleri + tüm-açılar rollup'ı yazar (period='all', SADECE SAYILAR). **K8 tek-yön sınır:** CRM YALNIZ okunur ve SAVUNMACI (tablo/kolon yoksa o sinyal sıfır sayılır, job düşmez); yazımlar yalnız research-owned tablolara. **Opt-out senkronu:** CRM opt-out sinyali → fenced `research_suppress_company` ('opt_out') — suppression > dedup.

- **Migrations 099+100 (izole DB'de uygulı):** `research_outcome_stats` (UNIQUE NULLS NOT DISTINCT hücre; RLS user SELECT; updated_at trigger) + **100**: `research_tenants_with_exports()` DISTINCT RPC + partial index (günlük tick keşfi satır-hacminden etkilenmez — büyük tenant diğerlerini AÇLIĞA süremez).
- **Handler:** deterministik + idempotent tam-recompute; exported şirketler **keyset-paginated** (sınırsız); CRM okumaları **dedupe'lu id listesi + 200'lük chunk + satır-bazlı .range() pagination** (gateway URL limiti ve PostgREST satır tavanı aşılamaz); açı atıfı ICP'nin **güncel ruleset**'ine filtreli; paylaşılan `crm_company_id` grupları sayıları **BİR KEZ** atfeder (temsilci = açı-taşıyan satır öncelikli, ties id-sıralı); **bayat hücre budaması** (taşınan icp/geo/açı hücreleri silinir — çifte sayım yok; sıfır-export yolu tüm stats'ı budar); suppress RPC per-firma best-effort. Reap loop re-entry guard'lı; günlük tick tenant-başına-UTC-gün idempotent (duplicate-check 200'lük chunk).
- **Entegrasyonlar:** `icp:revise` prompt'una "Measured campaign outcomes" bloğu (toplam + açı bazlı; stripFence'li; okuma hatası eski prompt'a düşer); `GET /icps/:id/outcomes` (yalnız sayılar); IcpCard **yanıt-oranı rozeti** + OffersPanel açı kartlarında **stat satırı**; `POST /admin/feedback/aggregate` run-now (internal). i18n TR+EN.
- **Review zinciri:** 2-lens Workflow (**1×P1**: chunk'suz 5000-UUID .in() → gateway limitinde job ölümü + opt-out senkronunun durması; **3×P2**: 2000-satır tenant taraması açlığı, budanmayan bayat hücreler, paylaşılan crm-id N× sayımı; 3×P3) → hepsi düzeltildi → codex xhigh **Not-SHIP 2×P1+3×P2** (chunk'lar arası duplicate crm-id çifte okuması, satır-paginationsuz CRM okumaları, bayat-ruleset açı atıfı, sıfır-export budaması, tenant-id .in() chunk'ı) → hepsi düzeltildi → **verify: SHIP**.
- **Smoke:** `temp/outcomes-smoke.ts` (deterministik, LLM YOK) ×3 geçti: sentetik CRM satırları → rollup exported=4/sent=3/replies=2/positive=1/optouts=2 + açı hücresi doğru (paylaşılan-CRM temsilci testi smoke'ta **gerçek bir bug yakaladı**: rastgele UUID sırası açısız kopyayı temsilci seçebiliyordu → açı-öncelikli seçim); opt-out→suppression ('opt_out' kaynağıyla, non-optout dokunulmadı); idempotent re-run stabil. tsc server+client temiz.
- **Bilinen sınır (P3, dokümante):** açı/ICP atıfı şirketin GÜNCEL rollup icp_id'siyle — export-anı ICP pinleme (research_mark_exported'a kolon) follow-up.

## 4.12 WP4 — OFFER/ANGLE KATMANI + FİRMA HOOK'LARI + EXPORT GENİŞLETME — ✅ **BİTTİ → codex SHIP** (2026-07-09)

**Kavram:** `research_offers` = ICP başına insan-onaylı **outreach açı kartları** (angle_code slug + ağrı hipotezi + değer önerisi + kanıt noktaları + itirazlar — mesaj METNİ DEĞİL, o TG-Core'da). `offer:generate` (strategy/Opus, ~$0.05) profil+farklılaştırıcılar+ICP+geo pazar notu+GERÇEK match kanıtlarından 3-5 taslak üretir → müşteri düzenler + /10 onaylar (ICP/geo insan-kapısı; approve `updated_at` CAS'lı). **AYNI reading-role validasyon geçişi** artık MATCH firmalara `hooks[]` (≤3 sayfa-temelli kişiselleştirme kancası) + `angle_suggestion` (yalnız ONAYLI kodlardan) yazar — **ek fetch/LLM çağrısı YOK**. Export custom_fields: `Research ICP` / `Research Angle` (kod — value_prop) / `Research Hooks`.

- **Migrations 096+098 (izole DB'de uygulı):** `research_offers` (tenant RLS + updated_at trigger + UNIQUE (tenant,icp,lower(code))); verdicts +`hooks` JSONB +`angle_suggestion`; **`research_persist_verdict` 12→14 arg** — faturalı-match erken-dönüşü HER yazımdan önce (dokunulmazlık aynen; verdict-smoke **P1-P10** ALL_PASS: hooks faturalı satırı mutasyona uğratamaz, re-persist hooks'u whole-replace eder, non-array reddi); export RPC genişledi + **098**: angle yalnız hâlâ-onaylı offer join'inde export edilir (reddedilen kod CRM'e sızamaz — P10a/P10b smoke).
- **Server:** `offers/{schema,prompt}.ts` (noFence + slug regex; UNTRUSTED fence'li kanıt/pazar-notu; existingCodes slice(0,40)), `offerGenerate.ts` (approved-ICP şartı; kanıt örnekleri **güncel ruleset + suppressed=false + registry FAIL-CLOSED** filtreli; tavan defense-in-depth + insert-loop **hard clamp** — taşma imkânsız, skipped_ceiling raporlanır), `validate.ts` (verdictSchema +hooks/+angle — hooks preprocess'te **CLAMP edilir, reddedilmez**: advisory alan ücretli geçişi asla düşüremez; `hardenPersonalization`: **MATCH-only** + kod onaylı-listede + hook token-grounding + hijyen [fence/URL/email/pipe nötralize]; evidence-gate ÖNCE koşar), `ledger.ts` persistVerdict passthrough, `harvestRun` onaylı açı haritasını deterministik okur (ORDER BY + limit 10). `harvest.ts` /companies hooks+angle döner; export custom_fields üçlüsü (yalnız doluysa; hooks 'untrusted-derived' uyarısı yorumda).
- **Routes/Client:** `offers.ts` (generate [in-flight adopt + kredi kapısı + **MAX_OFFERS_PER_ICP=20** + tüm echo'lar sanitize + taze-rol], list, PATCH [content-edit→draft; **rejected reaktivasyonu tavan-kapılı**], approve [CAS + rejected reaktivasyonu tavan-kapılı], **reject** [slot boşaltır, kod rezerve kalır]); OffersPanel (kart düzenle/onayla/reddet + job poll) + ResearchPage "Offer açıları" sekmesi; CompaniesPanel satırlarında angle+hook chip'leri. i18n TR+EN.
- **Review zinciri:** 2-lens Workflow (correctness SHIP 3×P3 / security FIX_FIRST 1×P2+2×P3 → hepsi düzeltildi: match-only, hook hijyeni, clamp, ORDER BY, slice, tavan) → codex xhigh **FIX FIRST 1×P1+2×P2** (kanıt örneklerinde suppression/ruleset filtresi yok; tavan 16-19'dan 24'e taşabiliyor; reject yolu yok) → düzeltildi → **verify: 1×P1+2×P2** (registry okuması fail-open; rejected reaktivasyonu tavansız; export'ta reddedilmiş kod) → düzeltildi (098) → **verify-2+3: SHIP**.
- **Smoke:** verdict-smoke **P1-P10 + P10b** yeşil (izole DB); **canlı e2e ×2** (`temp/offers-smoke.ts`): 5 grounded açı ($0.05 Opus) → 2 onay → harvest'te **2/2 MATCH verdict hooks+angle taşıdı, non-match satırlar temiz** (match-only kapı canlıda), angle grounding onaylı kodlara sadık; exportable RPC satırları icp_name/hooks/angle_value_prop taşıdı; SearXNG internal-only olduğundan discovery Gemini fallback ($0.014/sorgu) ile koştu. tsc server+client temiz.


## 4.11 WP3 — Y1 KANAL KEŞFİ + LİSTE HASADI + KALICI SATURASYON — ✅ **BİTTİ → codex SHIP** (2026-07-08)

**Kavram:** `research_channels` canlandı (🥇 Y1): `channels:discover` bir ONAYLI sub-ICP hücresi için çok-dilli şablonlarla (00 §3, geo spec'in yerel terimleri + kanal tohumuyla) SearXNG'yi tarar ($0), reading role sonuçları firma-listesi kanallarına sınıflandırır (URL'ler yalnız sunulan sonuçlardan — grounding filtresi) → hücre-bazlı URL-dedup ile upsert. `channels:harvest` TEK kanalın üye listesini `fetchPage`(48k bütçe) + reading role ile çıkarır → **mevcut fenced spine** (dedup → validate → verdict → bill; `source_path='Y1'` + `channel_id` provenance, 092). `research_chunks` = hücrenin KÜMÜLATİF kapsama kaydı: Y3 saturasyonu artık hücre-bazında run-aşırı (32-sorgu minimumu kümülatif), kural-A (liste doygunluğu) persist; `fully_covered = A && B` RPC İÇİNDE hesaplanır (093).

- **Migrations (izole DB'de uygulı):** **091** kanal harvest bookkeeping + hücre-URL unique + chunk kümülatif kolonları (`angle_stats`/`queries_total`/`saturation_a+b`/`channels_found+harvested`/`discovery_rounds_no_new`) + fenced `research_update_chunk_coverage` (lease fence, DB-içi atomik açı-merge, NULL=dokunma partial semantiği). **092** fenced `research_upsert_company` +`p_channel` (22 arg; COALESCE — kanal-dışı run mevcut ref'i silmez; cross-tenant kanal reddi). **093** `fully_covered` RPC-içi A&&B (caller'ın bayat okuması tutarsız çift yazamaz; p_fully_covered yok sayılır). **094** `research_chunks` DML revoke (service_role dahil) — coverage RPC yapısal tek yazıcı.
- **Server:** `channels/{schema,prompt,discovery,coverage}.ts` (zod noFence + boş-string→undefined normalizasyonu [canlı DeepSeek davranışı]; UNTRUSTED fence'li promptlar; 8-kategori round-robin sorgu şablonları; kural-A değerlendirici + fenced RPC sarmalayıcı), `channelsDiscover.ts` (round-bazlı; **degrade tur** [SearXNG outage → `complete:false`] rounds_no_new'ü İLERLETMEZ + angles-run İDDİA ETMEZ), `channelsHarvest.ts` (**partial-run ≠ harvested**: `stopped_by` doluysa kanal `pending` kalır + stop notu; unreachable ayrı; `harvest_error` TEK yazım noktasında `sanitizeJobError`'dan geçer), `channelListSource` (CandidateSource; üye website'i **kod-içi grounding** — sayfa metninde geçmeyen domain domainless'a düşer; junk-domain koruması), `sources.ts` kümülatif `y3FullyCovered` (prior seed + **sıfır-sinyal asla doyurmaz**: uniqueDomains 0 bail + son-2 pencerede sonuçlu sorgu şartı), `harvestRun.ts` chunk prior yükleme→`priorStats` + koşu sonu coverage persist (advisory, asla run'ı düşürmez) + `channelId` passthrough, `fetch.ts` **store/return clip ayrımı** (cache 48k saklar, default dönüş 12k; kısa-cache'li satıra uzun-bütçeli okuma → bypass+refetch).
- **Routes:** `routes/research/channels.ts` — discover (approved cell + kredi kapısı + **saturasyon 409 kapısı** [non-internal] + in-flight adopt), list, coverage, `:id/harvest` (**ICP-genelinde 4-tip in-flight guard** — harvest/trade/icps-calibrate listeleri de CHANNELS_HARVEST içerir; <24h re-harvest 409 [non-internal]; caps yalnız **taze** `effectiveCostRole` internal ise parse). **Tüm job echo'ları `sanitizeJobForRole`** (068 kuralı — adopt edilen internal job'ın caps'i müşteriye sızmaz).
- **Client:** GeographiesPanel drawer'ına `CellCoveragePanel` (onaylı hücrede): N/E + sorgu + kural-A/B rozetleri + fully-covered, kanal tablosu (tip/durum/üye/hasat butonu), keşif butonu; 7s poll. i18n TR+EN (`research.channels.*`).
- **Review zinciri:** 2-lens Workflow (correctness+billing / security+injection+tenancy) → **1×P1+3×P2+5×P3 düzeltildi** → codex gpt-5.5 xhigh → **FIX FIRST 3×P1+2×P2** (üye-website grounding, chunks DML revoke, ICP-guard genişletme, complete-aware roundValid, taze-rol caps) → düzeltildi → verify: 1 kalan P1 (icps calibrate guard) → düzeltildi → **verify-2: SHIP** (4 guard birebir doğrulandı).
- **Smoke:** `temp/coverage-smoke.sql` ALL_PASS P1-P6 (fence, kümülatif merge, NULL partial, **RPC-içi fully_covered A&&B + regress**, unique hücre, tip reddi); 092 channel-ref smoke ALL_PASS P1-P3; `temp/verdict-smoke.sql` **P1-P9 YEŞİL KALDI** (faturalı-match dokunulmazlığı 092 sonrası). **Canlı e2e ×3** (`temp/channels-smoke.ts`, yarışsız fenced job'larla — Railway worker'ın eski deploy'u queued job'ı kapıp düşürmüştü): keşif 11-22 kanal/$0.004-0.007 → hasat 11-12 üye spine'dan → chunk coverage persist → ikinci Y3 koşusu kümülatif devraldı (queries_total 0→2→4, `cumulative_seeded=true`); Δkredi 0; test verisi temizlendi. tsc server+client temiz (paralel oturumun linkedin WIP hataları hariç).
- **DEPLOY NOTU (kritik sıra):** kod `p_channel`'ı her zaman gönderir — **091+092+093+094 hedef DB'ye uygulanmadan bu server build'i deploy edilirse TÜM upsert'ler düşer** (PostgREST named-arg eşleşmesi). Önce migration, sonra deploy.

## 4.10 WP2 — PAZAR YAPISI → SUB-ICP (geo-instantiation) — ✅ **BİTTİ → codex SHIP** (2026-07-08)

**Kavram:** `research_geographies` = SUB-ICP hücresi (ICP'nin tek ülkeye instantiate hali: yerel dil terimleri, lokalize sinyaller, kanal listesi [WP3 tohumu], sertifikalar, alıcı unvanları, pazar notu, E tahmini + güven). `geo:analyze` (Opus + opsiyonel $0 SearXNG kanıt taraması) taslağı üretir → müşteri düzenler + /10 onaylar → harvest `geo_id` ile koşar: engine yerel terimleri sorgu üretiminde, lokalize işaretleri validasyon bağlamında kullanır. **Billing bağlantısı YOK** (verdict anahtarı (icp, ruleset_version) kaldı); geo_id'siz harvest davranışı birebir eskisi gibi.

- **Migration 086**: geographies'e `spec`/`ai_draft`/`generated_by_job_id` + UNIQUE (tenant, icp, lower(country)) + fenced `research_persist_geo_analysis` (lease fence + approved→draft demote + kolon projeksiyonu).
- **Server**: `geo/schema.ts` (zod; `<<<` fence-marker reddi dahil), `geo/prompt.ts` (UNTRUSTED fence + url-uydurma yasağı), `geoAnalyze.ts` handler (meter'lı, SearXNG çökük olsa da job düşmez), `routes/research/geographies.ts` (create-or-reuse [analizli hücreyi SESSİZCE ezmez — reused+job:null döner], /analyze [in-flight adopt + 402 kredi kapısı], PATCH [spec→draft demote + estimate/confidence/rationale kolon projeksiyonu], approve [spec şart], **MAX 25 hücre/ICP**), harvest route+worker `geo_id` (approved + ICP eşleşme şart; **hücrenin ülkesi = coğrafya, serbest metin yok sayılır**), `sanitize.ts` geography-hatası dalı.
- **Engine**: `buildQuerySpecs(icp, geo, max, geoSpec?)` — ≥2 yerel terim localSectorTerms'i değiştirir + hücre dizinleri directory açısına eklenir; `buildMapsKeywords` yerel terimleri LEAD anahtar kelimeden hemen sonra sıralar (küçük cap kesemez); `validate.ts` lokalize işaretleri **stripWebFence'li + DATA-etiketli** render eder (web-türevli string verdict prompt'unda talimat olamaz).
- **Client**: GeographiesPanel (hücre tablosu + analiz + detay drawer'da spec düzenleme/onay) + ResearchPage "Coğrafyalar" sekmesi + CompaniesPanel launcher'da onaylı hücre seçimi (serbest metin fallback). i18n TR+EN.
- **Review**: 2-lens Workflow (2×P2 + 8×P3, İKİSİ DE SHIP) → 10 bulgunun hepsi düzeltildi (yukarıdaki parantezler). **codex gpt-5.5 xhigh: FIX FIRST (1×P1+1×P2+1×P3)** → üçü de düzeltildi: (P1) approve **CAS** — client `updated_at` token'ı gönderir, UPDATE `.eq` eşleşmezse `current_updated_at` taşıyan 409 (needSpec-409'dan yapısal ayrım); drawer key `id:updated_at` (ekrandaki spec ile CAS token'ı hep aynı satır nesli) + save/approve dönen satırı cache'e yazar (Save→Approve hızlı ardışıklığı post-save token'la CAS'lar). (P2) migration **090** `research_persist_geo_analysis` projeksiyonu COALESCE→doğrudan atama (geçerli `estimate:null` re-analizi bayat kolon bırakmaz); test DB'ye uygulandı. (P3) exact-match **reuse tavandan ÖNCE** — 25-hücre tavanı yalnız gerçek CREATE'e; 23505 dalı yarış için korunur. **verify: 2×P1+2×P2** → kapatıldı (CAS token artık **ZORUNLU** — endpoint'in legacy caller'ı yok, `updated_at`'sız approve 400; tavan tetiklenirse aynı-ülke yarışına **re-check→reuse**, 409 yalnız gerçekten yeni ülkeye) → **verify-2: SHIP**. Paralel bağımsız ikinci verify de SHIP (2 advisory P2 bilinçli kabul: 23505 fallback'inin nadir yarışta çift DB turu + updated_at-remount'un kaydedilmemiş taslağı düşürme UX riski — güvenlik > taslak).
- **Smoke**: `temp/geo-smoke.sql` ALL_PASS **P1-P4** (case-insensitive unique hücre, fence + persist + approved→draft, **null re-analiz projeksiyon temizliği [090]**, non-object spec reddi); `temp/geo-smoke.ts` canlı Opus e2e GEÇTİ (Almanya: 11 gerçek yerel terim, 10 kanal, 5 dizin, E=1500 conf 0.5, $0.063 COGS; buildQuerySpecs yerel terimi kullanıyor). Test verisi temizlendi. server tsc + client `tsc -b` + eslint temiz (fix sonrası yeniden doğrulandı).

## 4.9 WP1 — KALİBRASYON DÖNGÜSÜ (C1-C2) — bitti ✅ → codex SHIP (2026-07-08)

**Ürün akışı:** onaylı ICP × coğrafya için küçük örneklem harvest'i (server-forced caps 6q/18f/12c, normal fenced billing — trial emer) → müşteri firma başına 👍/👎 + not → `icp:revise` (Opus, strategy role) geri bildirimden 4 ruleset dizisinin TAM replasman revizyonunu önerir → müşteri diff'i görüp uygular (062 trigger bump + approved→draft) → yeniden onay → yeniden örneklem → "mantığı onayla" (`calibrated`).

- **Migration 084**: `research_company_feedback` (UNIQUE (tenant,icp,company,ruleset), user SELECT-only) + ICP kolonları (`calibration_state/revision_draft/revision_job_id/calibrated_at`) + fenced `research_persist_icp_revision`. **085**: RPC `p_base_ruleset` bağlama (DETAIL=`RULESET_MOVED`) + calibrated-terminal (DETAIL=`CALIBRATED`) + trigger taslak temizliği. **087**: trigger ruleset değişiminde kalibrasyonu SIFIRLAR (state='none', calibrated_at=NULL — kanıt per-ruleset).
- **Server**: `icp:revise` handler (icpGenerate kalıbı: meter + partial-COGS catch; feedback 400-pencere + kırpılma bayrağı + no-op guard; `maxAttempts:1` = job id ↔ tek proposal). 6 endpoint: calibrate (kredi kapısı + in-flight guard + sanitized echo) / feedback GET+POST (**ruleset CAS**: puanlanan versiyona sabitli, 409'da yeniden puanlama) / revise / apply-revision (**çift CAS**: ruleset_version + revision_job_id; no-op reddi) / mark-calibrated (kanıt kapısı ≥1 feedback + approved+version+no-pending-revision hepsi UPDATE içinde CAS).
- **Client**: `CalibrationDrawer` (4 adım: örneklem → değerlendirme → revizyon diff'i → tamamla; `ratingsVersion` pinleme — 409 sonrası bayat batch yeniden gönderilemez), IcpCard rozet+buton, i18n TR+EN.
- **Review zinciri**: 2-lens Workflow review (1×P2+7×P3 → düzeltildi) → **codex gpt-5.5 xhigh: FIX FIRST (5×P1+1×P2)** → hepsi düzeltildi → **verify: 4 FIXED + 2 eksik** → kapatıldı → **verify-2: SHIP**.
- **Smoke**: `temp/calibration-smoke.sql` ALL_PASS P1-P6 (unique, lease fence, RULESET_MOVED/CALIBRATED refusal, persist-no-bump, apply bump+draft+kalibrasyon sıfırlama, state CHECK); `temp/calibration-smoke.ts` canlı Opus e2e GEÇTİ (fenced seed → feedback → revise: 6 izlenebilir değişiklik, $0.044 COGS → apply bump). Test verisi temizlendi.

> Bu dosya "şu an ne var" özetidir. Kararlar `02_ACIK_KONULAR.md`'de, mimari `00_MIMARI_PLAN.md`'de, fiyat `01_KREDI_FIYATLAMA.md`'de, alıcı değerlendirme `03_...`'te. Sonraki adımlar `05_SONRAKI_ADIMLAR.md`'de.

---

## 0. DB izolasyonu (ÖNEMLİ)

- Bu worktree **izole test DB**'sine bakar: Supabase projesi **`iehqsuludghrhosgxhnr`** (TG-Core-coldcrm-test, TG-Core'un tam klonu — `tenants`/helper'lar var).
- **Prod TG Core `ehnbhkxmsdticaodndvy` temiz** (sıfır `research_*` satırı). Migration'lar prod'a UYGULANMADI.
- Test tenant: `11111111-1111-1111-1111-111111111111` (Tenant A). Smoke'lar bunu kullanır + sonrası temizlenir.
- `.env` (`SUPABASE_URL`/ANON + `VITE_*` + service key) test DB'ye repoint edilmiş.

---

## 1. Veritabanı (migration 055–066, hepsi test DB'ye uygulandı)

- **055** foundation: `research_projects`, `research_jobs` (Postgres kuyruğu), `research_usage_ledger`, `research_usage_holds`, `research_claim_job()`, `research_reap_stale_jobs()`.
- **056** ICP/market: `research_hs_codes/markets/icps/geographies/channels/chunks`.
- **057** companies: `research_companies` (dedup kütük), `research_contacts/trade_imports/messages`, `research_search_cache`.
- **058/059** kuyruk RPC revoke + jobs INSERT/UPDATE policy drop (queue = service-role state machine).
- **060** sertleştirme-1: `canonical_key` (dedup+billing birimi), `research_company_verdicts` (per-ICP verdict + `ruleset_version`), `research_search_log` (COGS) + `research_page_cache`, `research_billable_events` (once-ever guard), `research_suppression` + trigger, **`research_bill_match()`** RPC, fair `research_claim_job`.
- **061** ICP eval: `research_icps.ai_draft` + `source` + `generated_by_job_id`.
- **062** sertleştirme-2 (codex round-1, 11 bulgu): tüm research tablolarında **user clients SELECT-only**; billing tabloları (`billable_events`/`usage_ledger`) DML service_role'den dahi revoke → **sadece SECURITY DEFINER RPC'ler yazar**; yeni RPC'ler `research_grant_credits` / `research_credit_balance` / `research_upsert_company` / `research_suppress_company` — **hepsi aynı `pg_advisory_xact_lock('research_bill:'||tenant)` kilidini** alır (suppress-vs-bill + insert-vs-suppress TOCTOU kapandı); bill RPC artık verdict'in ICP'si **approved VE `verdict.ruleset_version = icp.ruleset_version`** olmasını şart koşar; `research_icps_ruleset_guard` trigger atomik version bump + approved→draft; kuyruk **lease** fencing; suppression contact key = sha256 hex CHECK.
- **063** sertleştirme-3 (codex round-2 doğrulaması): bill RPC `FOR UPDATE OF v,c,i` + `v.tenant_id` pin (ICP satırını da kilitle); `research_upsert_company` preserve-on-NULL (`p_*` kullan, `EXCLUDED` değil — EXCLUDED default sonrası) + project/icp/geo ref tenant doğrulama; `research_grant_credits` idempotent (`ON CONFLICT DO NOTHING`, UUID `p_ref_id` anahtarı); **`research_persist_icp_drafts`** = fenced atomik delete+insert `(job,locked_by,lease)`; approve = `ruleset_version` **CAS**.
- **064** kota tutmaları (pre-run quota holds, 05 §A.2 / D3-D4): `research_usage_holds` artık **sadece RPC yazar** (service_role'den de DML revoke). Yeni RPC'ler — hepsi aynı `research_bill:'||tenant` advisory kilidini alır: `research_reserve_hold` (admission; `available = balance − Σ açık holds`; `< min_required` ise **check_violation** ile reddet; `LEAST(estimate, available)` rezerve; iş başına idempotent), `research_settle_hold`/`research_release_hold` (kapat + kalanı serbest bırak), `research_available_credits` (UX/route okuması), `research_release_stale_holds` (çökmüş worker'ın takılı holdunu serbest bırakan reaper). `research_bill_match`'e **sert taban**: taze bir charge bakiyeyi 0'ın altına itemez (event INSERT'i geri sarar; `check_violation` → wrapper'da null).
- **065** holds sertleştirme-1 (codex round-1, 2×P0+3×P1): `research_bill_match` artık **hold-aware + lease-fenced** — taze charge holdu **artımlı tüketir** (`settled += 1`, ledger decrement ile atomik) ⇒ `available` run boyunca **sabit** (P0#1: mid-run negatif yok); hold tükenince **reddeder** (DB cap'i uygular, lokal sayaç değil); `(job, worker, lease)` verilirse reaped/reclaimed attempt **fence'lenir** (P0#2: zombie billing yok). `settle/release` artık sayaç almaz (kalan = `reserved−settled`). Reaper `pg_try_advisory_xact_lock` + sıralı (P1: deadlock yok).
- **066** holds sertleştirme-2 (codex round-2 doğrulaması, 1×P0+1×P1+1×P2): taze charge **yapısal olarak hold ŞART** (`p_hold_id` null ise RAISE) + job-attributed charge `worker+lease` ŞART (P0: default-NULL ile bypass kapandı); lease fence artık `FOR UPDATE` ile job satırını kilitler (P1: reap/reclaim'e karşı atomik, TOCTOU kapandı); exhaustion artık `DETAIL='RESERVATION_EXHAUSTED'` **yapısal işaret** taşır (P2: İngilizce mesaj eşleme yerine `error.details`).
- **067** verdict sertleştirme-1 (codex A.3 deferral'ları): **`research_persist_verdict`** fenced atomik RPC — (job,worker,lease) YAPISAL ŞART (unfenced verdict yazarı yok), advisory kilit + job `FOR UPDATE` lease fence + suppression-under-lock + **faturalanmış-MATCH dokunulmazlığı** (charge'ın dayandığı satır ezilmez, mevcut satır döner → caller DÖNEN satırdan sayar/faturalar); **`research_unbilled_match_verdicts`** SQL anti-join reconciliation (bounded 500, PostgREST satır-tavanı riski bitti); **settle/release lease-fenced** (job-attributed hold'u yalnız leaseli attempt kapatır; jobless/ops hold fence'siz; eski 1-arg formlar DROP).
- **068** COGS görünürlük ayrımı (ürün kuralı: **admin dolar görür, müşteri ASLA**): kolon-seviyesi grant'lar — `research_jobs` SELECT'i authenticated'a `result`+`payload` HARİÇ yeniden verildi (result=cost breakdown, payload=caps.maxSpendUsd); `research_search_log` müşteri SELECT'i tamamen kaldırıldı; `research_billable_events` `amount_usd`+`pricing_version` hariç; **`research_admin_cost_summary()`** cross-tenant marj RPC'si (service_role-only).
- **069** verdict sertleştirme-2 (codex 067 review'ü, 1×P0+1×P1+2×P2): verdict tablosu DML'i **service_role'den de revoke** (RPC-only, 062 kalıbı); reconciliation suppressed match'leri filtreler (batch starvation bitti); suppression refusal'ları **yapısal DETAIL** taşır (`SUPPRESSED`/`SUPPRESSED_OR_MISSING` — score CHECK'i suppression sanılamaz); `research_billable_events.verdict_id` kolonu + bill RPC yazar → **dokunulmazlık HASSAS** (yalnız charge'ın gösterdiği satır donar; legacy NULL event = muhafazakâr canonical-key freeze).
- **070** rollup yazarı fence'i (Workflow 14-agent adversarial review bulgusu): **`research_upsert_company` (job,worker,lease) ŞART** + atomik lease fence — son unfenced yazar kapandı (park path'leri artık zombie'de hiçbir şey yazamaz); suppression refusal DETAIL='SUPPRESSED'.

**Smoke ile doğrulandı** (self-rollback sentinel, test DB): bill-once, draft/stale-ruleset reddi, ruleset trigger, suppress>dedup, grant idempotency, preserve-on-NULL, cross-tenant ref reddi, lease fence. Smoke sırasında yakalanan **3 gerçek bug** düzeltildi: `SUM(delta)` bakiyesi (son-satır okuma değil — txn içi `created_at` eşitliği random-uuid tiebreak), `EXCLUDED` yerine `p_*` ile preserve, boolean-vs-int ROW_COUNT.

**Holds smoke ile doğrulandı** (3 ayrı self-rollback suite, test DB): reserve-caps-to-available + idempotent reserve + 0-bakiyede reddet; bill-to-reserved; **sert taban + orphan-event yok + dedup taban-muaf**; settle/release (`released = reserved − settled`); reaper. Round-1 fix sonrası: **consume artımlı → `available` run boyunca sabit (mid-run negatif yok)**, DB-enforced exhaustion, **lease fence (yanlış lease engelle / doğru lease bill)**, dedup hold tüketmez. Round-2 fix sonrası: **hold ŞART (hold-less taze charge reddi)**, worker+lease ŞART, atomik `FOR UPDATE` fence, `DETAIL='RESERVATION_EXHAUSTED'` (PG_EXCEPTION_DETAIL ile doğrulandı). Holds smoke ilk turda yakaladığı gerçek bug: `SELECT sum(...) FOR UPDATE` yasak (aggregate+lock) → reserve advisory-kilit serileştirmesine güvenir (ledger SUM'ları gibi).

---

## 2. Sunucu kodu (`server/src/lib/research/` + `routes/research/`)

- **LLM router** (`llm/`): ROLE→provider. strategy=Claude Opus 4.8, search=Gemini 3.1 Pro (grounding), reading=DeepSeek V4 Pro. `runLlm` / `runLlmJson`. grounding⊻JSON ayrı.
- **Queue** (`queue.ts`): enqueue/claim/heartbeat/complete/fail — **lease fencing** (per-attempt UUID, complete `boolean` döner). Worker `worker/runner.ts`.
- **ICP Master (B5)** dikey dilim: `icp/{schema,prompt}.ts`, handler `worker/handlers/icpGenerate.ts` (`icp:generate`, fenced `research_persist_icp_drafts` RPC ile persist), route `routes/research/icps.ts` (generate→202, list/get/PATCH/approve-CAS). Client: `pages/research/ResearchPage.tsx` + `components/research/IcpCard.tsx`. **Gerçek Opus ile e2e geçti.**
- **Engine (Y1 list-harvest)** — `lib/research/engine/`:
  - `canonical.ts` — `canonical_key` = eTLD+1 (`tldts` deps) / domainless `name|country|city`, ß→ss translit. 18 unit test geçiyor (`temp/canon-test.ts`).
  - `pricing.ts` — COGS oranları (env-override, `pricing_version=v1`). **Oranlar varsayımsal** — pilot faturasıyla kalibre edilecek.
  - `caps.ts` — `CapTracker` (maxQueries/Fetches/Candidates/SpendUsd, env tavanlar; default spend $5).
  - `discovery.ts` — 2 geçiş: grounded `runLlm('search')` → grounded-OLMAYAN `runLlmJson('reading')` extraction (Gemini citation'ları Vertex redirect olduğu için aday METİNDEN çıkar). search_cache + search_log.
  - `fetch.ts` — Jina Reader (r.jina.ai) primary + **SSRF-korumalı** direct fallback (DNS çöz + private IP reddi + manuel redirect) + streamed byte cap + page_cache.
  - `validate.ts` — kanıt-bağlı verdict (DeepSeek), injection fence + **evidence-overlap kapısı** (kanıtı sayfada doğrulanmayan 'match' → 'review', faturalanmaz).
  - `ledger.ts` — tüm yazımlar RPC'ler üzerinden; `unbilledMatchVerdicts` (reconciliation); **holds wrapper'ları** (`reserveHold`/`settleHold`/`releaseHold`/`availableCredits`, `InsufficientCreditsError`/`ReservationExhaustedError`); `billMatch` artık `holdId`/`worker`/`lease` geçer ve exhaustion'ı `error.details` ile ayırt eder. **Cross-ICP re-score (A.3):** `existingCanonicalKeys` → `existingCompanies` (tam-satır Map) + `companiesWithCurrentVerdict` (dedup artık "(company,icp,ruleset) verdict var mı").
  - `fetch.ts` — ayrıca `cachedPageContent(domain)`: ağ çağrısı OLMADAN taze page_cache metnini okur (re-score girdisi), `fetchPage` ile aynı `pageUrl`+sha256 anahtarı.
  - Handler `worker/handlers/harvestRun.ts` (`harvest:run`, maxAttempts=1): **run başında hold rezerve** (yetersiz kredi → COGS harcamadan fail), her bill `hold.id`+`job.locked_by`+`job.lease` ile (lease-fenced, holdu tüketir), `ReservationExhaustedError` → dur, başarıda `settleHold` (kalanı serbest), herhangi bir throw'da `releaseHold` (best-effort) + rethrow. `newly_billed = settledHold.settled` (run'a özel, kesin). **Cross-ICP re-score ikincil geçişi** (yeni keşiften sonra): mevcut-ama-verdict'siz firmaları cached metinden yeniden skorlar (`billOne` DRY bill yardımcısı; detay §4.2).
  - `worker/runner.ts` reaper tick'i `reapStaleJobs` + **`releaseStaleHolds`** çağırır (çökmüş worker'ın holdunu serbest bırakır).
  - Route `routes/research/harvest.ts`: `POST /run` (approved-ICP gate + **`availableCredits < 1` → 402** hızlı-fail; otoritatif gate worker'ın `reserveHold`'u) →202, `GET /companies`, **`GET /credits`** (`{balance, available, reserved}`).

`tsc` (server+client) temiz.

---

## 3. Bu oturumda yapılan codex review'leri (ikincil göz)

`codex exec -m gpt-5.5 -c model_reasoning_effort=xhigh` ile **3 tur**, hepsi FIX-FIRST, hepsi düzeltildi:
1. **Geçmiş adımlar** (055–061 + ICP slice) → 11 bulgu → **062**.
2. **062 doğrulaması** → 6 PASS + yeni P1'ler → **063**.
3. **Engine kodu** → SSRF, billing-completeness, cost-safety → engine düzeltmeleri (aşağıda). canonicalizer/eTLD+1/ß→ss/tenant filtreleri/cache-hit pricing'i PASS dedi.

**Holds için 3 tur daha** (sonraki oturum, hepsi FIX-FIRST, hepsi düzeltildi):
4. **064 holds kodu** → 2×P0 (hold mid-run tüketilmiyor → `available` negatif; reaper çalışan run'ın holdunu serbest bırakır → zombie billing) + 3×P1 → **065** (hold-aware bill + incremental consume + lease fence + try-lock reaper).
5. **065 doğrulaması** → 1×P0 (default-NULL ile hold bypass) + 1×P1 (fence TOCTOU) + 1×P2 (SQLERRM eşleme kırılgan) → **066** (hold ŞART + worker/lease ŞART + `FOR UPDATE` fence + `DETAIL` işaret).
6. **066 doğrulaması** → **No findings → VERDICT: SHIP**. Her tur gerçek defekt buldu.

Engine round-3 düzeltmeleri: SSRF guard + streamed cap; **billMatch transport hatasında THROW** (sessiz faturasız match yok); **uçtan-uca billing reconciliation** (crash-gap kapatır); maxAttempts=1; fetch-boş→LLM'siz 'review'; cache-hit fetch cap'i yemez; evidence-overlap kapısı; fence sanitization; domainless **city**; cap tavanları; `newly_billed` credits-delta'dan (kesin).

---

## 4. CANLI PİLOT — çalışıyor ✅

`temp/harvest-smoke.ts` (Germany, caps 2 query/5 fetch/$0.75), **3 kez yeşil** (holds dahil son tur):
- En son (holds ile): 13 aday → **3 MATCH** (Wiedemann 95, Penning 90, Eisen-Fischer 85 — gerçek Alman sıhhi tesisat toptancıları), 3 partial, **1 doğru ELIMINATED** (Arnold Lammering = üretici), 1 domainless→review.
- **Hold + faturalama** (gerçek worker, lease aktif): `hold={reserved:8, settled:3, released:5}`, `newly_billed=3`, credits 50→47 (Δ=−3), `billable_events=3`. Lease fence hiçbir meşru bill'i engellemedi; consume artımlı çalıştı (settled=3); settle kalanı (5) serbest bıraktı. COGS $0.0103.
- SSRF guard meşru Alman domainlerini bloklamadı; evidence kapısı meşru match'leri düşürmedi.

Test verisi her run sonrası temizlendi (page/search cache tutuldu — cross-tenant, faydalı).

---

## 4.1 LLM USAGE METER + COGS KALİBRASYON PİLOTU — bitti ✅ (2026-06-30)

**Sorun:** `pricing.ts` "pilot ham token/çağrı sayımlarını tutar, COGS sonradan gerçek oranla yeniden hesaplanabilir" diyordu ama **tutmuyordu** — `CapTracker` sadece *varsayılan oranla hesaplanmış doları* tutuyor, ham token'ları atıyordu. Para harcayan pilot bu halde geri-kalibre edilemez veri üretirdi.

**Çözüm — router seviyesinde usage meter** (`llm/meter.ts`, `AsyncLocalStorage`): `runLlm` tek choke point olduğu için her sağlayıcı çağrısının ham token'ı otomatik sayılır (runLlmJson iç retry'leri dahil — dolar tracker'ın kaçırdığı; codex deferral B.3 kapandı). Job sonucu artık `usage_raw` (sağlayıcı başına in/cached/out token + grounded sorgu) + `cost_recheck` (ham'dan yeniden hesaplanmış dolar, çapraz kontrol) taşır. Eşzamanlı job'lar izole tally (ALS scope başına).

**3 review turu (codex gpt-5.5 xhigh + 4-lens Workflow), 14 düzeltme — hepsi FIX-FIRST:**
- **P1 (Workflow yakaladı, codex kaçırdı):** Gemini `thoughtsTokenCount` (düşünme token'ı, **çıktı oranıyla faturalanır**, `candidatesTokenCount`'tan AYRI) sağlayıcı sınırında düşüyordu → en baskın maliyet satırı **underpricing yönünde eksik** + usage_raw'dan kurtarılamaz. Düzeltme: output = candidates + thoughts. **Pilotta doğrulandı: Gemini çıktısının ~%55'i düşünme — düzeltme olmasa COGS %28 eksik.**
- DeepSeek `prompt_cache_hit_tokens` ayrımı yakalanıyor (kesin uzlaşma için); `maxRetries:0` (SDK retry'leri meter'ın altında kalıyordu); withLlmMeter throw'da kısmi usage'ı hata nesnesine ekliyor (frozen-error guard'lı); discovery pass-2 throw'da grounded maliyeti atfediyor; Jina-faturalanabilir fetch ayrımı; harness billed çift-sayım + hit-rate paydası (`evaluated`) düzeltmeleri.
- **Anthropic oranı $15/$75 → $5/$25** düzeltildi (otoritatif Opus 4.8 liste fiyatı; Opus harvest path'inde DEĞİL, sadece ICP-setup).
- Round-3 codex: **No findings → SHIP.** SDK alan adları (`@google/genai@2.10.0` thoughtsTokenCount, DeepSeek prompt_cache_hit_tokens) tip-tanımlarından bağımsız doğrulandı.

**Soğuk kalibrasyon run'ı** (`temp/scale-pilot.ts`, NL+PL, 6q/30f/40c/$2, gerçek Gemini+DeepSeek):
- **18 MATCH** (NL 4, PL 14), 61 değerlendirilen, isabet %29.5. İlk (Germany) run sıcak cache + kirli tenant nedeniyle 0 MATCH verdi → soğuk coğrafyalara geçildi (gerçek grounding + dedup'suz).
- **İlk ölçüm $/MATCH = $0.034** (grounding $0.007) / **$0.045** (grounding $0.014, Google listesi); **Gemini grounded arama %86**. DeepSeek %14. **codex calib review → REVISE:** rakam büyüklük mertebesi (n=2, fiyat-kanıtı değil), grounding 2× daha yüksek olabilir, marj düşük tier'larda kötümserde %40, kaldıraç sıralaması (self-host #1 değil). Düzeltilmiş detay: `01_KREDI_FIYATLAMA.md §4.1`.
- Meter çapraz kontrol geçti: `cost_recheck` ($0.616) ≈ `cost_usd` ($0.615), fark %0.2 (retry token'ları). `usage_raw`/cached-split/jina-split hepsi beklendiği gibi. Pilot sonrası test DB temizlendi (TRUNCATE, cache tutuldu); prod'a dokunulmadı.

---

## 4.2 CROSS-ICP RE-SCORE (A.3 / codex deferral 2) — bitti ✅ (2026-06-30)

**Kapanan P0 boşluk:** Engine mevcut `canonical_key`'i TAMAMEN atlıyordu (`existingCanonicalKeys` dedup) → farklı/yeni/düzenlenmiş bir ICP altında mevcut firmaya verdict YAZMIYORDU; faturalanabilir match'ler sessizce kayboluyordu. Tek-ICP pilotunda etkisizdi, ölçeklemede şarttı.

**Çözüm:** Dedup kapısı artık "firma var mı?" değil **"(company, icp, ruleset_version) için verdict var mı?"** (`companiesWithCurrentVerdict`). Üç yol:
- **Yeni firma** (registry'de yok) → değişmemiş fetch+validate (birincil yol).
- **Mevcut firma, bu ICP için güncel verdict YOK** → **cached metinden yeniden skorla** (`cachedPageContent(domain)` taze page_cache tam metin; yoksa durable `site_summary`; **asla re-fetch yok**) → verdict → match ise faturala. Yeni keşiften SONRA çalışan ikincil geçiş, `RESCORE_EXISTING` (env, default açık; `'0'` → eski skip-all) ile kapılı.
- **Mevcut firma, güncel verdict VAR** → gerçekten atlanır (dedup).
- Ruleset bump (062 CAS) sonrası eski-versiyon verdict'ler predicate'i sağlamaz → aynı ICP yeni ruleset'te doğal olarak yeniden skorlanır.

**Billing güvenliği (kilitli invariant'lara dokunmaz):** Re-score yalnız YENİ gelir üretir — `research_bill_match` `ON CONFLICT (tenant_id, canonical_key) DO NOTHING` ile ömürde-bir. ICP-A altında zaten faturalanmış firma ICP-B altında re-score+match olursa verdict yazılır ama **fatura DEDUP** (hold tüketmez, ledger düşmez, **çift-charge yok**). `insertVerdict` bill'den ÖNCE → transport hatasında verdict kalıcı → reconciliation sonraki run'da bulur (kayıp gelir yok). `research_companies` re-score'da **UPSERT EDİLMEZ** (rollup status/score korunur, migration 060 §2 tasarımı; per-ICP gerçek = verdict tablosu). Cap'ler: `canTakeCandidate` candidate+spend kapılar → re-score sınırlı; içeriksiz skip `countCandidate` yemez ama `reScoreTargets ≤ canon` ile zaten sınırlı.

**Kod:** `engine/fetch.ts` (`cachedPageContent` + `pageUrl` tek-kaynak URL-hash), `engine/ledger.ts` (`existingCanonicalKeys` → `existingCompanies` tam-satır Map + `companiesWithCurrentVerdict`), `worker/handlers/harvestRun.ts` (sınıflandırma bloğu + re-score döngüsü + DRY `billOne` + yeni summary: `rescored`/`rescore_matches`/`rescore_skipped_no_content`/`skipped_current_verdict`/`existing_surfaced`). **server+client tsc temiz.**

**Review + düzeltmeler (2 tur — streaming düzeldikten sonra tamamlandı):** İlk turda ortam streaming-API'si bozuktu (ağ kesintisi 15:14–16:19 + uzun-bağlantı düşmeleri) → codex (×2) + Workflow (×2) hang etti. Streaming düzelince **codex gpt-5.5 xhigh (VERDICT: FIX FIRST) + slim 2-lens Workflow (4 bulgu)** İKİSİ de tamamlandı ve büyük ölçüde örtüştü. **3 bulgu FIX-FIRST düzeltildi:**
- **(P1) Re-score fetch-cap bug:** birincil geçiş `maxFetches`'e çarpınca döngü altındaki `reasonToStop()` `fetch_cap` döndürüyordu → sıfır-fetch re-score geçişi 1 hedeften sonra duruyordu. Düzeltme: re-score döngüsü altta yalnız **spend**'e bakar (`cost().totalUsd ≥ maxSpendUsd`), üstte `canTakeCandidate` candidate+spend kapılar; fetch_cap re-score'u durdurmaz.
- **(P1) site_summary fallback KALDIRILDI:** re-score artık **yalnız tam cached sayfa metninden** skorlar (`cachedPageContent`); yoksa atlar (`rescore_skipped_no_content`). Neden: `site_summary` bir önceki validator'ın ÇIKTISI (doğrulanmamış kaynak değil) → (a) halüsinasyon özet ~tek cümleyle **faturalanabilir match**'e kanıt oluyordu, (b) özete karşı yanlış non-match yazılınca firma o ICP+ruleset için **kalıcı dedup**'lanıp gerçek lead sessizce kaybediliyordu. `isPlaceholderSummary` + `ExistingCompany.siteSummary` de kaldırıldı.
- **(P1) Görünürlük — `GET /companies` verdict-aware:** `icp_id` verilince artık `research_company_verdicts`'ten (ICP'nin **güncel ruleset_version**'ı) okuyup `research_companies` ile join eder, per-ICP verdict status/score/evidence'ını döner (rollup değil). `icp_id` varken `project_id` yok sayılır (ICP zaten projeye scope; rollup project'e filtrelemek cross-project re-score match'ini yeniden gizler + count'u bozar). `icp_id`'siz → eski düz rollup listesi. Böylece re-score match'i faturalanıyor VE görünüyor.
- **server+client tsc temiz.** Düzeltmelerin **codex re-review'ü → VERDICT: SHIP** (yeni bulgu yok; 3 düzeltmenin hepsi doğrulandı: fetch-cap yok sayılıyor/candidate+spend kapılı, yalnız tam sayfa cache skorlanıyor, verdict view güncel-verdict alanları + tam filtreli count + tenant-scope'lu, rollup project_id yok sayımı doğru, ertelenen hardening scope makul).

**Bilinçli ERTELENEN sertleştirme (kendi hardening turuna — holds 064-066 gibi; 05 §1d):**
- **(codex P0) `insertVerdict` mutable UPSERT, lease/suppression-fenced DEĞİL** → eşzamanlı/zombie attempt faturalanmış bir match'in verdict'ini ezebilir. **Pre-existing** (her iki döngü de `insertVerdict` kullanır); tek-worker `maxAttempts=1` pilotunda (tenant başına tek job) TETİKLENMEZ. Planlı düzeltme: fenced `research_persist_verdict` RPC (per-tenant kilit + lease fence + suppression + faturalanmış-match'i-ezme) → `{id,verdict}` döner, dönen verdict'e göre faturala. **Migration 067.**
- (codex P1) reconciliation sayfasız client-side anti-join → SQL RPC; (codex P2) settle/release lease-fenced değil; (codex P1) yumuşak `maxSpend` (maliyet çağrıdan sonra sayılıyor) = mevcut deferral B.1.

---

## 4.3 VERDICT HARDENING (067/069/070) — bitti ✅ (2026-07-02)

**Kapanan:** codex A.3 deferral'ları (P0 `insertVerdict` unfenced mutable upsert + P1 client-side reconciliation + P2 settle/release fence'siz) + codex 067-review bulguları (4) + Workflow 14-agent adversarial review bulguları (2 confirmed).

- App-side `insertVerdict` SİLİNDİ → `persistVerdict` (fenced RPC wrapper, **row-of-record** döner; handler sayım+faturayı DÖNEN karardan yapar). `upsertCompany` da fence'li (070). Handler'a **rollup-repair** eklendi: persist guard faturalanmış match'i koruyunca rollup DÖNEN karara hizalanır (ödenen lead listede 'eliminated' görünemez).
- Route'a **ICP başına tek in-flight harvest** guard'ı (409): aynı ICP'nin eşzamanlı iki run'ının persist→bill aralığında unbilled-match ezme yarışı pratikte kapandı (DB invariant'ları zaten güvendeydi; fatura her zaman güncel karar satırını izler).
- **Smoke:** `temp/verdict-smoke.sql` 9 faz ALL_PASS (yapısal fence, yanlış/ölü lease reddi, upsert-of-record, faturalı-match dokunulmazlığı, hassas cross-ICP freeze [ICP-B satırı serbest, faturanın dayandığı ICP-A satırı donuk], DETAIL marker'ları, anti-join+suppression filtresi, settle/release fence [no-args/yanlış-lease/cross-job reddi], zombie-release reddi+reaper, service_role DML revoke). `temp/holds-smoke.sql` fence imzalarına güncellendi, ALL_PASS.
- **Canlı pilot (fence zinciri uçtan uca):** Germany run — 13 aday → 4 MATCH faturalandı (50→46), verdict'ler fenced RPC'den, hold consume+settle fence'li, sıcak page-cache yolu da doğrulandı. COGS $0.0104 (cache sayesinde sıfır network fetch).

## 4.8 TIER KOTALARI (STRIPE'SIZ) + FAILED-COGS + CRM HANDOFF — bitti ✅ → codex SHIP (2026-07-02)

**Review zinciri:** codex batch-3 (1×P0+4×P1+2×P2 → 074 + route yeniden yazımı) → verify (2 kalıntı: cross-ICP telafi yarışı → mutex TENANT-geneline + multi-instance onay-kemeri [silmeden önce research satırı bizim CRM id'mizi gösteriyorsa BAŞARI say]; domain fallback → `exportDomain(website) ?? exportDomain(domain)`) → **final VERDICT: SHIP** (fonksiyonel regresyon yok).

Kullanıcı kararı: **Stripe YOK** — operatör + otomatik dönem grant'ı kota yaşam döngüsünü döndürür. Migration **073** (test DB'ye uygulandı) + kod:
- **`research_tenant_settings`**: tenant başına tier (trial/starter/growth/scale/custom) + aylık lead kotası + reserve boyutu + auto_grant. Müşteri kendi satırını okuyabilir (adet, dolar değil); yazan admin route'u.
- **`research_apply_period_grants(period)`**: dönemsel otomatik kota — tenant başına try-advisory-lock (meşgulü atla), ledger'a **deterministik ref** (`md5('research_period_grant:'||tenant||':'||period)::uuid`) → `uq_research_usage_ledger_ref` ile dönem başına YAPISAL ömürde-bir (çift grant imkânsız). Worker reaper tick'i her turda idempotent çağırır + admin "şimdi uygula" butonu. Reserve estimate artık tier'a göre (`TIER_RESERVE` map: 10/25/50/100; settings override edebilir).
- **Failed-run COGS kalıcı**: runner, başarısız attempt'in kısmi meter tally'sini (`usage_raw`+`cost_recheck`) `failJob` üzerinden job result'ına yazar; admin özeti **`failed_cost_usd`** kolonuyla toplar, UI'da "Failed $" (turuncu). Marj panelinin kör noktası kapandı (05 §1b(3)).
- **CRM handoff (ürün döngüsünü kapatan halka)**: `POST /harvest/companies/export` — ICP'nin güncel-ruleset MATCH'lerini TG Core `companies`'e kopyalar (stage `'cold'`, fit_score=skor, özet+kanıt custom_fields'ta), üç katman dedup: (a) `crm_company_id` zaten set → atla, (b) mevcut CRM satırıyla aynı eTLD+1 domain → BAĞLA (kopyalama), (c) yeni satır → insert + **korelasyon anahtarıyla** (custom_fields 'Research Ref') sıra-varsayımı OLMADAN geri-bağlama (`research_mark_exported` dar-kapsamlı RPC: yalnız export kolonları, tenant-scoped, relink yok). UI: CompaniesPanel "Uygun firmaları CRM'e gönder" + satırda "CRM ✓" rozeti; admin credits sekmesinde TierSettingsForm (key-remount kalıbı) + dönem-grant butonu.
- **Smoke**: `temp/tiers-smoke.sql` 4/4 ALL_PASS (grant+gating, idempotency [last_grant_period sıfırlansa bile ledger ref'i çift yüklemeyi engeller], dönem devri + geçersiz dönem reddi, mark_exported [own-tenant/no-relink/cross-tenant red]). server tsc + client `tsc -b` + eslint temiz.
- **074 export hardening (codex batch-3 review: 1×P0+4×P1+2×P2 → hepsi ele alındı):** **P0** suppressed firma export edilebiliyordu → `research_exportable_companies` RPC (rollup flag + registry anti-join + unexported, hepsi LIMIT'ten ÖNCE — P1 batch-stall de bitti: tekrar çağrılar alt skorlara sayfalanır) + `research_mark_exported` kilit altında suppression re-check yapar ve GERÇEKTEN işaretlediklerini döner; route işaretlenmeyenlerin CRM kopyalarını SİLER (telafi). **P1 concurrency/retry:** (tenant,icp) başına in-process mutex (tek uzun ömürlü süreç; multi-instance için DB claim notu) + `custom_fields.research_ref` korelasyon anahtarı yarım-kalan export'un retry'ında satırları KOPYALAMAK yerine BAĞLAR (domainless dahil). **P1 yanlış-bağlama:** export-side domain eşleme `allowPrivateDomains:true` ile (a.github.io ≠ b.github.io; billing canonicalizer'a dokunulmadı). **P1 failed-COGS retry birikimi:** kabul+belgelendi (harvest maxAttempts=1; icp retry ¢). **P2:** dönem regex ay 01-12'ye sıkıldı; PUT settings `reserve_estimate`'i yalnız gönderilmişse yazar. 074 ek smoke 3/3 ALL_PASS (exportable filtreleri, mark suppression-recheck, regex).

## 4.7 BATCH-2 REVIEW ZİNCİRİ → SHIP ✅ (2026-07-02)

Batch-2 (068-072 + admin/companies UI + pricing v2) için **4 codex turu** (gpt-5.5 xhigh) + düzeltmeler:
1. **codex batch-2 review → FIX FIRST** (1×P0+4×P1+4×P2) → **072** (`research_companies` DML'i service_role'den de revoke; `research_jobs.error` kolonu müşteri direct-read'inden gizli; admin CTE projects/search_log/holds ile genişledi) + caps şema ayrımı (USD sınırları müşteri validation metnine bile sızmaz) + role-sanitized 202 echo + job `error` metni role göre süzülür (ham sağlayıcı/billing string'i müşteriye ulaşmaz) + generic `POST /jobs` internal-only + grant `idempotency_key` ŞART.
2. **codex verify → FIX FIRST** (kalan 3 + 2 yeni) → **`freshRole.ts` `effectiveCostRole`**: TÜM maliyet yüzeyleri (jobs×4, harvest echo, admin kapısı) internal rol iddiasını her istekte canlı doğrular (ops_agent=aktif membership, superadmin=taze app_metadata), doğrulanamazsa FAIL-CLOSED müşteri görünümü (60s auth-cache indirme penceresi kapandı) + NULL-score repair guard + grant key parametre-bağlı (useMemo: param değişimi/başarı → yeni key; aynı-param retry → aynı key) + response'tan yanlış `granted` iddiası kalktı + icpGenerate failed-but-paid partial usage logu. **Ayrıca keşif: client'ta `npx tsc --noEmit` NO-OP** (solution-style tsconfig) — gerçek kontrol **`npx tsc -b`**; bunun ortaya çıkardığı 4 hata (2'si pre-existing IcpCard) kök nedeniyle düzeltildi: `onError: showErrorFromApi` doğrudan geçirilince `fallback?: string` parametresi TVariables'ı string'e zorluyordu → lambda sarımı.
3. **codex verify-2 → 2 blocker** → freshRole tenant-scoped (rol tenant'a bağlı; A'da indirilen ama B'de ops kalan operatör A'nın dolarını göremez) + CRM `supabaseAdmin` ile kimlik okuma (model-B ayrışmasına dayanıklı) + icpGenerate catch'i tüm paralı bölümü kapsar (LLM+heartbeat+persist).
4. **codex final → VERDICT: SHIP** (regresyon yok; cross-tenant semantiği auth middleware ile birebir doğrulandı).

## 4.4 COGS GÖRÜNÜRLÜK AYRIMI + ADMIN MARJ PANELİ — bitti ✅ (2026-07-02)

**Ürün kuralı:** admin (superadmin/ops_agent) gerçek dolar maliyeti görür; müşteri (client_admin/viewer) ASLA — müşteri yalnız lead/kredi SAYILARI görür.

- **Sızıntı kapatıldı (2 katman):** (a) **DB** — 068 kolon-seviyesi grant'lar: müşteri JWT'siyle direkt PostgREST'ten `research_jobs.result` (cost_usd/usage_raw/cost_recheck), `payload.caps`, `search_log.cost_usd`, `billable_events.amount_usd` artık OKUNAMAZ; (b) **API** — `lib/research/sanitize.ts`: jobs route'unun 4 cevabı da role göre süzülür (cost_usd/cost_recheck/usage_raw/caps/pricing_version + payload.caps müşteriden düşer; lead sayıları kalır).
- **Admin API** (`routes/research/admin.ts`, tüm router `requireRole('superadmin','ops_agent')`): `GET /costs` (per-tenant marj özeti — 068 RPC'si: run/fail sayısı, harvest COGS, search payı, billed leads, bakiye/rezerve, $/lead), `GET /runs` (tam cost breakdown'lu run geçmişi + tenant adları), `POST /credits/grant` (idempotent operatör top-up).
- **Admin UI** (`pages/research/ResearchAdminPage.tsx`, route `/research/admin`, nav yalnız internal): filo kartları (toplam COGS, billed, blended $/lead, açık rezervasyon) + tenant tablosu + run geçmişi (maliyet tooltip'li) + kredi yükleme formu. Rol dışıysa `/research`'e redirect.
- **Companies UI (müşteri)** (`components/research/CompaniesPanel.tsx`, ResearchPage sekmeleri "ICP Master"/"Lead'ler"): proje→ICP seçimi, kredi rozetleri (SAYI), coğrafya+`Lead bul` (onaylı ICP + kota şartı, job polling, bitişte toast+invalidate), verdict-aware firma tablosu (durum/puan/kanıt, filtre, sayfalama). i18n TR+EN tam.

## 4.6 İSABET GENİŞLETME PİLOTU — bitti ✅ (2026-07-02)

3 yeni soğuk konfigürasyon (`temp/scale-pilot.ts`): **Fransa** (farklı dil, keskin ICP) → %13 isabet, $0.11/MATCH; **Çekya** (seyrek pazar; ilk deneme worker-kill ile yarıda kaldı → takılı job düşürüldü, hold reaper'la serbest, kaldığı yerden warm-cache+dedup ile tamamlandı) → %35; **İspanya** (kasıtlı BULANIK MRO-distribütör ICP'si = kötümser uç) → %20, $0.083/MATCH. **Toplam veri seti artık n=5 coğrafya / 2 arketip / 41 MATCH: isabet bandı %13–45 (harman %26), soğuk $/MATCH $0.02–0.11 (harman $0.062).** CZ run'ı **cross-ICP re-score'u üretimde ilk kez doğruladı** (7 rescored → 4 match → 13 match ama 10 yeni tahsilat: 3 dedup, çift charge yok). Tümü pricing v2 + tam fence zinciriyle koştu; `cost_recheck` ≈ `cost_usd` her run'da. Test verisi temizlendi (cache tutuldu: 164 sayfa / 38 arama). Detay + fiyat-kilidi durumu: `01 §4.1`.

## 4.5 ORAN TEYİDİ → PRICING v2 — bitti ✅ (2026-07-02)

- **Liste fiyatlarıyla teyit:** Gemini 3.1 Pro $2/$12 per 1M + grounding $14/1000=$0.014 (✅ varsayım doğru; ayda 5.000 ücretsiz Gemini-3 grounding prompt'u var — fiyatlama ücretli orana göre). DeepSeek V4 Pro **in $0.435** (varsayım $0.30 düşüktü) / **out $0.87** (varsayım $1.20 yüksekti) / **cache-hit $0.003625** (~bedava).
- `pricing.ts`: default'lar güncellendi + **cache-aware tokenCost** (miss inPerM + cached cachedInPerM; `costFromUsageSummary` cached-split kullanır) + `PRICING_VERSION='v2'` (audit stamp; fatura uygunluğunu etkilemez).
- **Yeniden hesap (ham sayımlardan, re-run'sız):** toplam $0.815 → **$/MATCH = $0.0453** (Gemini %88 / DeepSeek %12). Önceki $0.045 tahminiyle örtüştü. **3 oran artık fiyat-kanıtı; kalan tek büyük belirsizlik isabet oranı (n=2).** Detay `01 §4.1`.

---

## 5. Kilitli invariant'lar (asla bozma)

- Billing birimi: **tenant başına unique canonical company için ÖMÜRDE BİR kez**; sadece `verdict='match'`; dedup + PARTIAL/ELIMINATED asla faturalanmaz. Tek giriş = `research_bill_match()` RPC.
- **Bakiye asla negatife düşmez** + **Σ açık reserved ≤ balance her an** (064-066): taze charge **hold ŞART** + holdu artımlı tüketir + tükenince reddeder + sert taban backstop; charge **lease-fenced** (zombie billing yok). Holds tablosu sadece RPC yazar (settle/release/reserve/reaper hepsi aynı per-tenant advisory kilidi).
- **Suppression > dedup** (KVKK); suppression registry PII-free (company=canonical_key, contact=sha256(email)); silme `billable_events`'i SİLMEZ.
- User clients SELECT-only; app+worker service-role (manuel tenant scope). Billing + verdict + holds + company-rollup **yazımları yalnız fenced SECURITY DEFINER RPC'ler** (067-070: persist_verdict/upsert_company/settle/release hepsi (job,worker,lease) fence'li; verdict DML service_role'den de revoke).
- **Faturalanmış MATCH'in karar satırı dokunulmaz** (067/069): charge'ın `verdict_id` ile gösterdiği satır ezilerek değiştirilemez; caller her zaman DÖNEN row-of-record'dan sayar/faturalar.
- **Müşteri rolleri ASLA dolar görmez** (068): cost kolonları DB'de kolon-grant'la + API'de sanitize ile kesilir; dolar yalnız internal admin yüzeyinde.
- Sadece research-owned path'lere dokun; CRM/campaign/auth/import READ-ONLY; CRM dosyası silme.

---

## 6. Nasıl çalıştırılır (resume için)

```bash
# tip kontrol
cd server && npx tsc --noEmit ; cd ../client && npx tsc --noEmit
# canonicalizer testi
cd server && npx tsx ../temp/canon-test.ts
# ICP üretim e2e (gerçek Opus, ~32s)
cd server && npx tsx ../temp/icp-smoke.ts
# HOLDS smoke (deterministik, LLM yok) — temp/holds-smoke.sql'i MCP execute_sql ile TEST DB'de çalıştır;
#   PASS = 'SMOKE_ROLLBACK ALL_PASS :: P1 ... P4 ...' (kendini geri sarar, DB temiz kalır)
# VERDICT smoke (deterministik, LLM yok) — temp/verdict-smoke.sql aynı şekilde; PASS = ALL_PASS P1..P9
#   (yapısal+lease fence, upsert-of-record, faturalı-match dokunulmazlığı + hassas cross-ICP freeze,
#    DETAIL marker'ları, anti-join+suppression filtresi, settle/release fence, zombie+reaper, DML revoke)
# CANLI harvest pilot (gerçek Gemini+DeepSeek+holds, ~$0.01-0.03, küçük caps)
cd server && npx tsx ../temp/harvest-smoke.ts
# WP smoke'ları (hepsi izole DB):
#   coverage-smoke.sql (091/093 chunk RPC, MCP execute_sql, ALL_PASS P1-P6)
#   verdict-smoke.sql (067/069/096/098, ALL_PASS P1-P10+P10b — faturalı-match + hooks + export)
#   geo-smoke.sql (086/090, ALL_PASS P1-P4)
#   cd server && npx tsx ../temp/channels-smoke.ts   # WP3 canlı (SearXNG internal-only → RESEARCH_SEARXNG_URL gerek ya da geçici public domain)
#   cd server && npx tsx ../temp/offers-smoke.ts     # WP4 canlı (~$0.10; SearXNG'siz Gemini fallback ile koşar)
#   cd server && npx tsx ../temp/outcomes-smoke.ts   # WP5 deterministik (LLM yok)
# NOT: canlı worker aynı DB'yi dinler — smoke'lar queued job yerine yarışsız fenced RUNNING job insert eder.
# pilot sonrası temizlik: MCP execute_sql ile research_* satırlarını sil (page/search cache TUT);
#   service_role billing/holds DML revoke edilmiş — temizlik postgres/owner rolüyle (MCP) yapılır
# codex review: codex exec -m gpt-5.5 -c model_reasoning_effort=xhigh -c approval_policy=never -c sandbox_mode=read-only - < prompt.txt
```

`.env`: `GEMINI_KEY` + `DEEPSEEK_KEY` SET (harvest bunları kullanır). `JINA_KEY` yok (keysiz düşük rate çalışır). `ANTHROPIC` ICP için yapılı.
