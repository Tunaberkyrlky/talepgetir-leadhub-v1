# TG-LinkedIn — İlerleme & Resume Dokümanı

> Son güncelleme: 2026-07-09 (Faz 5). Bu doküman `/clear` sonrası devam için "nereden devam edilir" özetidir.
> Tam strateji: `00_STRATEJI_VE_MIMARI.md` · Faz-0 spec/kritik: `01_FAZ0_BUILD_SPEC.md`, `02_FAZ0_CRITIQUE.md`.

## Tek cümle
Headless LinkedIn outreach modülü TG-Core'a **izole modül** olarak ekleniyor (research worker/queue'ya biniyor). **Faz 0-5 kodu yazıldı ve TAMAMI COMMIT'Lİ** (0/1 `6cae4bd`, 2 `e996da7`, 3 `b226986`, **4+5 `d654e8a`** — 2026-07-09 scoped commit, paralel research işleri dışarıda). — Aşağısı Faz 0-4 detayı; Faz 5 en altta.

<!-- ESKİ ÖZET (Faz 0-4): -->
Headless LinkedIn outreach modülü. **Faz 0-4 kodu yazıldı** (0 iskelet · 1 bağlan+validate · 2 tek invite+mesaj DRY-RUN · 3 limitler+warmup+çalışma-saati+jitter+withdraw+auto-pause+Accept-Language · **4 kampanya/sekans motoru: enrollment state machine + suppression + sender rotation + poll (accept/reply) + per-account serialization**), server+client tsc yeşil, migration 095+097 izole DB'ye uygulı, RPC+state-machine+tz/warmup smoke'ları ALL_PASS, her faz 5-boyutlu adversarial review'dan geçti (Faz 4: 1 P1 + P2'ler düzeltildi). Canlı gönderim testi henüz yapılmadı (env key + gerçek hesap + staging; kod dry-run varsayılanıyla kaza-koruma altında). **Faz 3 COMMIT b226986; Faz 4 UNCOMMITTED.**

## Sabit gerçekler (ezber gerektirmesin)
- **Branch:** `ssalihyetim/TG-Research` (origin'de). PROD = `main` (dokunulmadı).
- **Supabase projeleri:** PROD (müşteriler) = `ehnbhkxmsdticaodndvy` ("TG Core") · **İZOLE TEST DB** = `iehqsuludghrhosgxhnr` ("TG-Core-coldcrm-test"). Local `.env` → `SUPABASE_URL` test DB'ye bakıyor (`RESEARCH_SUPABASE_URL` yok → Model A, tüm veri test DB'de). `ROTATING_5G_PROXY` `.env`'de var.
- **Migration durumu (test DB):** uygulanmış → research serisi (calibration `084-087` DAHİL — 2026-07-09'da `list_migrations` ile doğrulandı, eski "uygulanmamış" notu geçersizdi), coldcall 079-082+, **linkedin 083 + 093 + 094 + 095 + 097 + 101** (bu modül). Staging'de CalibrationDrawer 500 riski YOK.
- **Env (Faz 1 canlı için gerekli, HENÜZ set değil):** `LINKEDIN_COOKIE_ENC_KEY=67093e7459874c6b7b31cd1c8ab99f0cb39f27f3a03f4eb853820f328a00cdf4` (bu session'da üretildi) · `LINKEDIN_APP_ORIGIN=<staging/local origin>`.
- **LinkedIn resmî API/OAuth invite/DM GÖNDEREMEZ** → cookie-uzantı yolu tek teknik yol (tüm ticari araçlar böyle).

## Yapıldı — Faz 0 (izolasyon + iskelet)
Migration `083_linkedin_foundation.sql`: `linkedin_accounts` (şifreli `li_at_enc`/`jsessionid_enc` + `proxy_session_id` + warmup + status enum), `linkedin_link_tokens` (tek-kullanım hash'li), `linkedin_actions` (audit+COGS). **Deny-all RLS** (ENABLE + 0 policy). Test DB'ye uygulandı + doğrulandı. Kuyruk aynen research_jobs; worker değişmedi. `crypto.ts` (AES-256-GCM, `LINKEDIN_COOKIE_ENC_KEY`, fail-closed), `proxy.ts` (sticky undici ProxyAgent). Route: `/api/linkedin` (accounts list/health/link-token/validate) + public `/api/linkedin/capture` (token-gated, auth öncesi mount). Mantine "LinkedIn Hesapları" sekmesi + TR/EN i18n. Adversarial review: 0 P0/P1, P2'ler düzeltildi.

## Yapıldı — Faz 1 (bağlan + gerçek doğrula) — KOD HAZIR, CANLI TEST YOK
- `server/src/lib/linkedin/voyager.ts` — golden-recipe header'lar, csrf tırnak-strip, **deterministik** `/me` kimlik çözümü (`data['*miniProfile']` pointer), accept-language.
- `server/src/lib/linkedin/client.ts` — `validateSession`: sticky proxy üzerinden `/voyager/api/me`, §4.4 sınıflandırma, **2xx-ama-JSON-değil → "unknown"** (ölü oturumu canlı raporlamaz), 30sn hard deadline.
- `server/src/lib/research/worker/handlers/linkedinValidate.ts` — decrypt → proxied probe → status+kimlik. **member_urn çakışması RACE-SAFE** (23505 yakala → RESTRICTED'e katla, job düşürme). PAUSED health-probe'a karşı sabit. dup-read + audit hataları kontrollü.
- `linkedin-extension/` — MV3 uzantısı: `chrome.cookies` → `/api/linkedin/capture` POST, origin doğrulamalı, https zorunlu, token pre-check, `host_permissions`'da app origin (CORS fix). README'de kurulum + iki akış (app-driven / manuel popup).
- Adversarial review (wquvwvgel): 2 P1 (member_urn yarışı; extension CORS) + 12 P2 → değerli olanlar düzeltildi. Server+client tsc 0.

## Yapıldı — Faz 2 (tek aksiyon: invite + message, DRY-RUN varsayılan) — KOD HAZIR + codex SHIP, CANLI TEST YOK
- **voyager.ts** (hot-update yüzeyi genişletildi): `invitePath`+`inviteDecorationId` (`verifyQuotaAndCreateV2`), `buildInvitePayload` (NOTSUZ varsayılan, not ≤300 hard-trim), `messagePath` (`createMessage`), `buildMessagePayload` (`originToken`=uuid-v4, `randomTrackingId`=16 kod-noktası 0-255 → `String.fromCharCode`; JSON.stringify→UTF-8→parser round-trip'i açıklandı; uuid/base64 DEĞİL), `parseProfileUrnFromHtml` (§4.3 public HTML blob regex). **decorationId/queryId CANLI doğrulanmalı.**
- **client.ts**: `WriteClassifier` + `classifyWriteResponse` (**§4.4: status'a asla tek başına güvenme** — 2xx gövdesinde gömülü quota/restrict/challenge/already_connected/cant_resend_yet taraması; temiz 2xx = tek `sent`), `sendInvite`/`sendMessage` (sticky proxy seam), `resolveProfileUrn` (best-effort GET, miss=null), `isNotSent` (refund kararı).
- **Migration 093** `linkedin_quota.sql` (izole test DB'ye UYGULI): `linkedin_try_consume_quota` (FOR UPDATE fence + UTC gün devri + cap kontrolü + artış, atomik) + `linkedin_release_quota` (aynı-gün refund, floor 0). Service-role only. `temp/linkedin-quota-smoke.sql` **ALL_PASS P1-P4**.
- **actions.ts** (paylaşılan spine): load/decrypt/dispatcher, `consumeQuota`/`releaseQuota`, `statusForWrite`+`applyWriteHealth` (403→RESTRICTED,999→CHALLENGED,401→NEEDS_REAUTH; PAUSE kalkmaz), `auditAction`. **DAILY_CAPS** Faz-2 güvenlik tavanı (invite 40 / message 60 — warmup rampası Faz 3).
- **Handler'lar** `linkedinInvite.ts` + `linkedinMessage.ts`: **DRY-RUN VARSAYILAN** (`dry_run!==false`) → decrypt/network/consume YOK, sadece plan önizlemesi (would_send, quota, target, noteless). Gerçek gönderim: ACTIVE guard (hard state=skip, throw yok=retry yakmaz) → **reserve-before-send** (atomik) → decrypt+proxy → gönder → §4.4 sınıflandır → **isNotSent ise refund** → health geçişi → tek audit satırı. message ayrıca `member_urn` (mailboxUrn) şart → yoksa `no_identity` skip. `maxAttempts=1`.
- **Route'lar**: `POST /api/linkedin/accounts/:id/invite` + `/:id/message` (202, `dry_run` body default TRUE — bare çağrı önizler; gerçek gönderim `dry_run:false` şart). Sanitized job echo. jobTypes + handler registry kaydı (`linkedin:invite`/`linkedin:message`).
- **Client**: LinkedInAccountsPanel'e **"Davet denemesi"** dry-run modal (profil URN/public id → dry_run invite → `/research/jobs/:id` poll → önizleme kartı: would_send/quota/noteless/target). Faz-1 kalan **`/linkedin/connect`** köprü sayfası (hash token → extension `CONNECT_LINKEDIN` [VITE_LINKEDIN_EXTENSION_ID] + her zaman manuel kopya fallback). i18n TR+EN. App route eklendi.
- server tsc + client `tsc -b` + eslint (yeni dosyalar) temiz.
- **Codex gpt-5.5 xhigh review: FIX FIRST (3×P1 + 2×P2) → hepsi düzeltildi:** (P1) `parseProfileUrnFromHtml` geniş fallback KALDIRILDI — scoped-only owner match, yoksa null → skip (yanlış kişiye davet engellendi); (P1) `classifyWriteResponse` yeniden yazıldı — gövdeyi JSON parse eder, hata ZARFINI yalnız status≥400/exceptionClass/errors[] varken çıkarır ve **sadece code/message/exceptionClass alanlarını** tarar (mesaj metni echo'su false-positive vermez), non-JSON 2xx = 'unknown' (sent değil); (P1) migration **094** ACTIVE gate consume RPC'sine FOR UPDATE altında katıldı (reason=not_active) + `applyWriteHealth` DB-guard `.neq('status','PAUSED')` (eşzamanlı PAUSE ezilmez); (P2) `resolveProfileUrn` `{urn,httpStatus}` döner → 401/403/999'da health geçişi; (P2) generic `POST /api/research/jobs` linkedin write tiplerini 400'le reddeder (yalnız dedike route, maxAttempts=1). Codex ayrıca dry-run'ın decrypt/network/consume YAPMADIĞINI doğruladı. `temp/linkedin-quota-smoke.sql` ALL_PASS **P1-P5** (094 ACTIVE gate dahil). tsc yeşil. **codex verify: 5/5 FIXED + 1 yeni P2** (non-JSON 2xx / `unknown` refund edilmiyordu → quota sızıntısı) → düzeltildi: `isNotSent` artık `sent`/`already_connected`/`cant_resend_yet` DIŞINDA her sonucu refund eder (dead-session 2xx slot yakmaz; nadir 5xx-sonrası-send bilinçli kabul — cap billing değil, backstop). tsc yeşil. **→ SHIP.**

## Yapıldı — Faz 3 (limitler + zamanlama + auto-pause + Accept-Language) — KOD HAZIR + adversarial review SHIP, CANLI TEST YOK
- **Migration 095** `linkedin_scheduling.sql` (izole test DB'ye UYGULI): `linkedin_accounts`'a `warmup_started_at` (ramp origin, backfill=created_at, kalıcı), `working_hours` JSONB (`{days:[1-5 ISO],start,end}` varsayılan Pzt-Cum 09-18), `accept_language` eklendi. Consume RPC **imza değişti** (DROP 4-arg → CREATE 5-arg): `linkedin_try_consume_quota(tenant,account,action_type,daily_cap,weekly_cap)` — **aynı FOR UPDATE altında** ACTIVE gate + daily counter + **rolling 7-gün weekly ceiling** (linkedin_actions status='ok' sayımı). `release_quota` değişmedi.
- **`limits.ts`** (yeni, pure): warmup step-fonksiyonu → effective daily cap (invite start 5 → +3/2gün → plato 25; message 15→plato 60); `WEEKLY_CAP` (invite 100, message 250); `warmupDay` fail-closed.
- **`schedule.ts`** (yeni, pure): tz-aware çalışma-saati penceresi + jitter. `nextSendAt` (min-gap ~90s + jitter ~150s), `isWithinWindow`, `nextWindowStart` (hafta sonu/tatil atlar), `zonedTimeToUtc` **2-geçiş DST düzeltmesi**, `normalizeWorkingHours` + `safeTz` güvenli fallback. **21/21 tsx harness ALL_PASS** (NY EDT dönüşümleri, hafta sonu ertelemesi, min-gap, warmup, garbage-normalize).
- **`actions.ts`**: `dailyCapFor`/`weeklyCapFor`/`weeklyCount`, `scheduleSendAt` (route+preview), **`maybeDeferSend`** (execution-time çalışma-saati recheck → gecikmiş worker off-hours atmaz → taze job re-enqueue), **`cancelPendingAccountJobs`** (§6 auto-pause: hard state'te hesabın queued linkedin:* joblarını canceled yap), `credsFor`+ACCOUNT_COLUMNS `accept_language`.
- **`voyager.ts`/`client.ts`**: per-account Accept-Language header; withdraw hot-surface (`sentInvitationsPath`/`withdrawInvitationPath` — **RE-VERIFY LIVE**), `parseSentInvitations`, `listSentInvitations` (**ok flag**: non-JSON 2xx login-wall ≠ boş liste, §4.4), `withdrawInvitation`.
- **`linkedinWithdraw.ts`** (yeni handler): **DRY-RUN varsayılan** bekleyen davet geri çekme (yaş > `withdraw_after_days` 7-30, per-run cap 10). Bilinmeyen-yaş davete DOKUNMAZ; hard classifier/2-ardışık-fail'de durur; dry-run tam side-effect-free.
- **Handler'lar** (invite/message): warmup daily + weekly cap; execution-time `maybeDeferSend` (send_now bypass); preview weekly-aware `would_send`; validate `warmup_started_at`'ı ilk canlı validate'te başlatır + hard state'te auto-pause.
- **Route** `accounts.ts`: real send'e working-hours `scheduled_at`; **`send_now` sadece internal role** (client_admin scheduling'i atlayamaz); yeni `POST /accounts/:id/withdraw`. `capture.ts` + extension `background.js`: `accept_language` (navigator.languages q-order) yakala+sakla. `jobTypes`+registry: `linkedin:withdraw`; generic enqueue reddeder.
- **Adversarial review (5 boyut × opus, 23 ajan, adversarial verify):** 18 bulgu → 15 CONFIRMED (3 refuted). **Düzeltilen:** (P1) validate status UPDATE'ine DB-level `.neq('status','PAUSED')` guard + PAUSE-won branch; (P2) `markDuplicate` PAUSE guard; (P2) `listSentInvitations` non-JSON 2xx = ok:false (login-wall ≠ boş); (P2) execution-time working-hours recheck (gecikmiş worker off-hours atmaz); (P2) `send_now` internal-only; (P3) withdraw dead-session loop-stop + dry-run side-effect-free; (P3) jitter re-snap'te taze jitter; (P3) preview weekly-aware. **Doküman edilen sınırlar (Faz-4 sequence engine kapatır):** weekly-ceiling in-flight overshoot (concurrency width, backstop-not-billing), batch min-gap (queued kardeşler görünmez), deploy-ordering (095 DROP eski worker'ı migrate→deploy penceresinde kırar), stale-401-vs-fresh-reauth (session epoch gerekir).
- server tsc + client `tsc -b` yeşil. `temp/linkedin-faz3-smoke.sql` (RPC ALL_PASS P1-P6) + `temp/faz3-schedule-check.ts` (21/21) izole DB/harness'ta doğrulı. **→ SHIP.**

## Yapıldı — Faz 4 (kampanya/sekans motoru) — KOD HAZIR + adversarial review düzeltmeleri, CANLI TEST YOK, UNCOMMITTED
- **Migration 097** `linkedin_sequences.sql` (izole test DB'ye UYGULI): `linkedin_leads` (workspace dedupe_key unique), `linkedin_suppression` (central do-not-contact), `linkedin_campaigns` (sender pool + dry_run default TRUE), `linkedin_sequence_steps` (invite/message/wait, ordered), `linkedin_enrollments` (state machine + lease). `linkedin_accounts.next_available_at` (per-account pacing). Deny-all RLS. RPC'ler: `linkedin_enroll_lead` (suppression + one-active-campaign, **lead FOR UPDATE** → write-skew yok), `linkedin_claim_due_enrollments` (**per-account DISTINCT** lock-pool+dedup, min-gap cursor, **lease TTL**, SKIP LOCKED — Faz-3 residual'larını kapatır), `linkedin_suppress_identity` (**lead FOR UPDATE**, stop-all-active).
- **`executor.ts`** (yeni): `performInvite`/`performMessage` — Faz-2/3 send spine'ı çıkarıldı (reserve→send→classify→refund→health→audit); Faz-2 handler'ları buna refactor edildi (DRY, davranış aynı); motor da inline kullanıyor.
- **`sequences/`**: `personalize.ts` (spintax `{{a|b}}` + `{var}`), `enroll.ts` (dedupeKey, least-loaded sender rotation, enroll/suppress wrapper), `engine.ts` (`processEnrollment` state machine: pending→invited→accepted→messaged→completed; cap→reschedule vs hard-fail; dry_run; already_connected; no_accept timeout; **optimistic-concurrency guard** `.eq('state', enr.state)` her write'ta; resolved urn'i lead'e geri yazar).
- **Handler'lar**: `linkedinSequenceTick` (claim batch → processEnrollment → self-reschedule + **dropped-poll re-seed**), `linkedinPoll` (accept: connections match → message step wait_days ile schedule; reply: conversations → suppress+stop; non-ACTIVE'de bile loop'u yaşatır). jobTypes `linkedin:sequence-tick`+`linkedin:poll` + registry.
- **Route'lar** `campaigns.ts`: campaign CRUD + activate/pause (loop seed), steps PUT, leads upsert (**in-batch dedupe**), enroll (rotation), suppression add/list. `routes/linkedin/index.ts` mount.
- **Adversarial review (5 boyut × opus):** 15 bulgu; **DÜZELTİLDİ:** (P1) motor resolved targetUrn'i lead'e yazmıyordu → invite→message public_id-only lead'de kırılıyordu; (P2) updateEnrollment id-only → poll accept / suppress stop'u eziyordu → guardState; (P2) enroll write-skew → lead FOR UPDATE; (P2) poll loop non-ACTIVE'de ölüyordu → her exit'te reschedule + tick re-seed; (P2) leads upsert in-batch dup 500 → Map dedupe; (P3) post-accept wait_days düşüyordu → poll step wait_days uygular. **Doküman edilen P3'ler:** seedLoops check-then-enqueue race (safe, paced), claim pool-TOCTOU (pacing self-corrects), reply-regex best-effort (REFUTED — kasıtlı fail-safe). *(7 verify ajanı session-limit'e takıldı; P1 + concurrency P2'ler tamamlanan verifier'larca bağımsız CONFIRMED.)*
- server+client tsc 0. `temp/linkedin-faz4-smoke.sql` (enroll/claim/lease/suppress ALL_PASS) + `temp/faz4-engine-check.ts` (personalize + dry-run state machine E2E 9/9) izole DB'de geçti.

## Yapıldı — Faz 5 (UI tamamlama + uyumluluk sertleştirme) — KOD HAZIR + 55-ajan review düzeltmeleri, CANLI TEST YOK, UNCOMMITTED
- **Migration 101** `linkedin_retention.sql` (izole test DB'ye UYGULI, v2) — 4 service-role RPC: `linkedin_purge_retention` (90g+ `linkedin_actions` sil + süresi dolmuş link-token sil + **stale lead PII anonimleştir**; suppressed kimlik PII'ı null'lanır ama **dedupe_key korunur** → opt-out eşleşmeye devam; non-suppressed lead `purged:<id>`'e relabel; terminal enrollment istatistikleri + suppression satırı KORUNUR), `linkedin_replace_steps` (**atomik delete+insert tek txn** → tick 0-adım görüp live lead'i "completed" yapamaz), `linkedin_account_usage` (per-account 7-gün GROUP BY → health kolonları; db-max-rows kırpması giderildi), **`linkedin_suppress_identity` YENİDEN TANIMLANDI** (097'den override): ON CONFLICT artık kalıcı sebebi (replied/opted_out/connected/bounced) removable satırın (manual/do_not_contact) üzerine **ESCALATE** eder, asla downgrade etmez.
- **`linkedin:retention` job** (`linkedinRetention.ts`) — günlük **self-healing** loop: purge RPC hata verse bile önce successor'ı enqueue eder (loop asla tek transient hatada ölmez), sonra hatayı görünürlük için rethrow. `ensureRetentionLoop(tenantId, delayMs)` helper'ı **kampanya aktivasyonunda (delay 0)** + **hesap validate'te (delay 0)** seed edilir → kampanya açmayan tenant da kapsanır. Default 90g pencere; manuel `POST /retention/run` (internal-only) custom pencere.
- **Yeni endpoint'ler** (`campaigns.ts`): `GET /:id/enrollments` (lead join + state filtresi + **created_at stable pagination**), `POST /:id/archive` (non-terminal enrollment'ları `stopped` yapar → lead donmaz + purge'e uygun), `POST /:id/enroll` archived reddi (409), `PUT /:id/steps` atomik RPC'ye geçti, `GET /leads` (arama+sayfalama), `GET /inbox` (yanıtlayanlar), `DELETE /suppression/:id` (**yalnız manual/do_not_contact** silinebilir), `POST /suppression` purged-key reddi (409). **`POST /leads` kimlik-tekrarında mevcut PII'ı NULL'lamıyor** (sadece yeni insert, mevcut satır dokunulmadan döner). `GET /accounts` warmup günü + günlük/haftalık kullanım/limit döner (usage RPC).
- **`personalize.ts`**: `{first_name}`/`{last_name}` snake_case token'ları artık çözülüyor (hint bunu öğretiyordu; underscore-normalize fallback). **`engine.ts`**: gönderim-anı **çok-anahtarlı suppression re-check** (`isLeadSuppressed`: pub/urn/nc anahtarlarının HEPSİ) → kişi farklı tanımlayıcıyla enroll edilmiş olsa bile opt-out workspace-wide tutar; eşleşirse stop+suppress.
- **UI** (`LinkedInPanel.tsx` 4 alt-sekme): Hesaplar (sağlık kolonları: warmup günü + davet/mesaj gün·hafta kullanım/limit, cap'e yakın turuncu/kırmızı) · Kampanyalar (`LinkedInCampaignsPanel` liste+oluştur · `LinkedInCampaignDetail` kurucu: adım editörü invite/mesaj/bekle + spintax + template, gönderen rotasyonu **optimistic draft**, dry-run→CANLI **onay kapısı** [isim ayrı JSX, i18next escape yok], enrollment tablosu **created_at stable + shrink-safe empty-state**, adım-editörü **in-flight edit koruması** [editSeq], leads yapıştır→import→enroll [invalid parse çevrilmiş mesaj], archived'da Add-leads disable) · Yanıtlar (inbox v2, LinkedIn deep-link, içerik saklanmaz) · Engel Listesi (`stopped` sayısı toast'ta; kalıcı satırlar kilitli). TR+EN i18n; `ResearchPage` LinkedIn sekmesi `LinkedInPanel`'e bağlandı.
- **55-ajan 5-boyutlu adversarial review** (correctness/security/concurrency/compliance/contract; bulgu başına 2 şüpheci lens): **23 CONFIRMED + 1 split → HEPSİ düzeltildi.** Ana kökler: (P1) `{first_name}` boş render; (P2×4) retention loop tek hatada kalıcı ölüm; (P2) archive lead'leri sonsuza dek dondurup purge'den muaf bırakıyor; (P2) enroll archived kampanya kabulü; (P2) PUT steps delete→insert yarışı; (P2) suppressed lead PII sonsuza saklanıyor; (P2) leads upsert mevcut PII'ı null'lıyor; (P2) senders MultiSelect lost-update; (P3) DELETE suppression bir yanıtı geri alabiliyor (DO NOTHING); (P3) identity-split opt-out açığı; (P3) weekly usage db-max-rows kırpması; (P3) enrollment pagination updated_at kayması + shrink empty-state; (P3) step-edit in-flight kaybı; (P3) confirmLive `{{name}}` HTML-escape; (P3) `no_leads` ham hata; (P3) purged lead_id suppression; (P3) toDedupeKey `stopped` feedback yutması.
- **Fix-review turu (15-ajan, düzeltmelerin doğruluğu + regresyon avı):** 4 CONFIRMED → **hepsi düzeltildi:** (P1) **retention self-heal loop ASLINDA çalışmıyordu** — `ensureRetentionLoop` guard'ı çalışmakta olan job'ın KENDİSİNİ görüp successor enqueue etmiyordu (loop seed başına 1 kez çalışıp duruyordu) → handler artık `job.id`'yi `excludeJobId` ile hariç tutuyor; `temp/faz5-retention-loop-check.ts` **6/6 ALL_PASS** ile ampirik doğrulandı. (P3×2 aynı) POST /leads eşzamanlı-insert'te raced key'i response'tan düşürüyordu → insert sonrası **tüm key'ler final re-select** (committed satırları her zaman döner). (P3 regresyon) engine send-time re-check `nc:isim|şirket` anahtarını türetip **homonim over-suppress** ediyordu → `candidateKeysForLead` yalnız pub/urn + lead'in kendi key'i (nc türetmez). 1 refuted.
- server tsc + client `tsc -b` + eslint (linkedin/) **temiz**. `temp/linkedin-retention-smoke.sql` (v2: purge+replace_steps+usage+escalation) **ALL_PASS** · Faz-4 regression smoke **ALL_PASS** (suppress redefine kırmadı) · `temp/faz4-engine-check.ts` **9/9** (engine re-check false-positive vermiyor) · `temp/faz5-retention-loop-check.ts` **6/6** (self-heal). İzole DB. **Sıradaki mig = 102.**

## UX — otomatik bağlanma-geri-dönüş (2026-07-09, staging canlı testinden çıktı)
Kullanıcı geri bildirimi: hesap bağlandıktan sonra Hesaplar sekmesine **elle yenilemeden** düşmeli. Kök neden: capture başka sekmede/uzantı popup'ında olur, orijinal panelin poll'ü yalnız zaten `isResolving` bir hesap varken tetiklenir → ilk bağlamada hesap yok → poll yok → elle reload gerekti. Çözüm (kod-tamam, tsc+eslint+build temiz):
- **Hesaplar paneli** (`LinkedInAccountsPanel`): "Hesap bağla" tıklanınca connect-anı hesap-id baseline'ı alınır + 4 dk sınırlı **bağlanma-izleme** başlar (3sn poll). Baseline dışı yeni id belirince otomatik görünür + "LinkedIn hesabı bağlandı" toast'ı; bu sırada "oturumunuz bekleniyor" banner'ı (Durdur butonlu) gösterilir. `justCaptured` render'da türetilir (setState-in-effect yok); toast ref-korumalı; timer/cancel ile temizlenir; `isResolving` validate poll'ü devralır.
- **Connect köprü sayfası** (`LinkedInConnectPage`): manuel popup yolunda sayfaya callback gelmediği için `/linkedin/accounts`'ı sınırlı poll eder (baseline mount'ta), yeni hesap belirince otomatik "Bağlandı" durumuna geçer + **`/research?tab=linkedin&sub=accounts`**'a 2sn sonra otomatik yönlendirir ("Hesaplara dön" butonu da var).
- **Deep-link:** `ResearchPage` dış sekmesi `?tab=` ve `LinkedInPanel` alt-sekmesi `?sub=` ile URL-kontrollü yapıldı (dış Tabs `keepMounted` varsayılanı korundu — CompaniesPanel harvest poll'ü düşmesin). i18n TR+EN.
- **KALAN (bu UX için):** staging'e deploy + canlı doğrula (bağla → banner → hesap otomatik düşsün → connect sekmesi geri yönlensin).

## CANLI SMOKE — staging'de gerçek hesap (2026-07-09) — ✅ validate + invite KANITLANDI
Staging'de gerçek hesap (Salih Yetim) bağlandı; uçtan uca canlı test edildi. Yol boyunca 3 gerçek bug bulundu+düzeltildi, hepsi **canlı gateway/LinkedIn'e karşı** doğrulandı (tahmin değil):
- **Proxy §1 — şema (`cb1ff50`):** `ROTATING_5G_PROXY` çıplak `USER:PASS@host:port` (şemasız) → `new URL()` kimlikleri düşürüyordu → validate transport_error. `proxy.ts` şema yoksa `http://` ekliyor.
- **Proxy §2 — sticky sözdizimi (`7200d56`):** kod Bright-Data tarzı `username-session-<id>` enjekte ediyordu → DataImpulse **407**. Canlı probe ile doğru form bulundu: **`username;sessid.<id>`** → 200 + gerçekten sticky (aynı id → aynı çıkış IP). Kodu buna çevirdim, id alfanümeriğe indirgeniyor.
- **§4.3 profil-URN çözümü (`ddadbc7`) — EN ÖNEMLİ:** public-profil HTML'i artık sahibin fsd_profile urn'ini stabil çıpayla TAŞIMIYOR; onun yerine BAŞKALARININ urn'lerini gömüyor (SDUI "people also viewed"). Eski HTML-scraper canlıda bir **yabancıyı** (önerilen bağlantı Jeff Kolego) çözüyordu; yalnız strict-parser'ın null→güvenli-atlaması yanlış kişiye davet gitmesini engelledi. Kimlik API'sine geçildi: `/voyager/api/identity/dash/profiles?q=memberIdentity&memberIdentity=<publicId>` → yalnız sorgulanan üyeyi döndürür, parser SADECE `publicIdentifier` eşleşen entity'nin urn'ini kabul eder (deterministik). Canlı doğrulandı: doğru id (harf-duyarsız) → doğru urn; sahte id → 403 → null. **Ek:** kimlik endpoint'i çözülemeyen üyeye 403 döndürdüğü için resolve-403 artık hesabı RESTRICTED YAPMAZ (tek bayat lead tüm kampanyayı durdurmasın) — `classifierForResolve` yalnız 401/999'u health'e haritalar; periyodik `/me` validate yetkili sağlık probu.

**Kanıt zinciri (linkedin_actions):** capture ok → validate transport_error ×2 → **validate success** (classifier=success, kimlik=Salih Yetim, member_urn çözüldü, warmup başladı) → invite skipped(urn_unresolved, HTML-resolver güvenli-atlama) → **invite SENT** (classifier=sent, http 200; hedef Reddyreddy Manasa'ya doğrulanmış URN ile gerçek bağlantı isteği; kota 0→1/5). Böylece Faz-1 `/me` + Faz-2 invite (`verifyQuotaAndCreateV2`+decorationId) hot-surface'ları CANLI kanıtlandı.

**Staging kimlik:** `staging.admin@tgcore.local` / `Staging!2026` (Tenant A, superadmin) — izole test DB'de oluşturuldu (auth.users+identities+memberships). Worker env: `LINKEDIN_COOKIE_ENC_KEY` + `ROTATING_5G_PROXY`.

**KALAN canlı doğrulama:** public-id resolver fix'ini deploy sonrası dry-run'la teyit (public_id → doğru urn, would_send); tek **mesaj** (kabul sonrası; `createMessage`+trackingId); **withdraw** dry-run; **dry-run kampanya** → connections/conversations poll hot-surface'ları. `sentInvitationsPath`/connections/conversations hâlâ canlı-kanıtsız.

## CANLI SMOKE #2 — withdraw dry-run + iki bulgu (2026-07-10)
Staging'de gerçek hesap (Salih Yetim, static_required, IPRoyal TR IP'sine bağlı) üzerinde withdraw dry-run çalıştırıldı (`POST /accounts/:id/withdraw {dry_run:true}` → job `c77b1925…` → `succeeded`). İki net bulgu:
- **BULGU 1 (olumlu — proxy gate kanıtı):** withdraw job'ı hesabın **static TR proxy'sinden** tünellendi (fallback yok) → §10 P1.1'in "withdraw/poll `resolveDispatcher`'dan geçer, rotating'e sızmaz" düzeltmesi **canlı doğrulandı**.
- **BULGU 2 (bug — bayat endpoint):** result `{"skipped":"list_unavailable","http_status":400}`. `sentInvitationsPath` = `/voyager/api/relationships/sentInvitationViewsV2` LinkedIn tarafından **deprecate edildi** (V2 REST → GraphQL taşındı) → **canlı 400**. Handler doğru davrandı: 400'ü `list_unavailable`'a katladı, hiçbir daveti ellemedi (dry-run side-effect-free korundu). **Fix DİSPATCH EDİLDİ** (sonnet ajanı): sentInvitations/withdraw/connections/conversations 4 hot-surface'ı modern GraphQL şekline çekiyor + parser'ları güncelliyor, kaynak-URL'li, tek canlı teyit gerektiren şekilde işaretli.

> **Not:** message + poll (connections/conversations) canlı doğrulaması hâlâ bekliyor — message kabul-edilmiş 1.derece kontak ister; endpoint-shape fix'i deploy sonrası tek canlı geçişle teyit edilecek (hesabın bağlı static IP'sinden, `LINKEDIN_PROXY_ENC_KEY` gerektiği için operatör/worker tarafında).

## PROXY P1 + PROD-HARDENING P1.4 + VOYAGER MODERNİZASYONU (2026-07-10, orkestratör + opus/sonnet ajanları)
Üç iş paralel ajanlarla yapıldı, hepsi izole test DB'de (`iehqsuludghrhosgxhnr`) smoke-doğrulı, server tsc temiz, DB temiz bırakıldı. **Migration'lar 108 + 109 uygulandı; sıradaki = 110.**
- **Proxy P1 (mig 108, opus):** `linkedin_claim_proxy(tenant,account)` (geo'dan ülke türet, fail-closed, `FOR UPDATE SKIP LOCKED` uygun-proxy seçimi, idempotent, validated-pointer NULL'la) + `linkedin_import_proxy_to_pool` (hesap-bağlamadan havuza al) + route'lar `POST /accounts/:id/claim-proxy` (internal, fresh-bind'de validate enqueue) + `POST /proxies/pool`. `geo` P0'da zaten ACCOUNT_COLUMNS'ta (doc'un "eksik" iddiası bayattı). Smoke A-G ALL_PASS.
- **Prod-hardening P1.4 (mig 109, sonnet):** assignment-scoped **send-lease** gate↔network TOCTOU'yu kapatıyor. `linkedin_acquire_send_lease` (proxy-keyed `pg_advisory_xact_lock` + lease row TTL 45s, tüm gate'leri taze re-check) + `linkedin_release_send_lease` (token-eşleşmeli) + `linkedin_burn_proxy` v2 (aynı advisory-lock, canlı lease varken `lease_active` ile burn'ü REDDEDER). executor.ts `performInvite`/`performMessage` send'den hemen önce lease alır, başarı+throw'da bırakır. Smoke A-G ALL_PASS (burn-refused-while-live + burn-after-lapse dahil).
- **Voyager modernizasyonu (sonnet):** BULGU 2'nin fix'i — 4 hot-surface (sentInvitations `q=invitationType`, withdraw `relationships/invitations/{id}`, connections `dash/connections`, conversations Messenger GraphQL) modern şekle çekildi + parser'lar **fail-closed** (tanınmayan zarf → `null` → `ok:false`/`list_unavailable`). Kaynak-URL'li, güven seviyeleri işaretli. Poll handler `!ok`'ı muhafazakâr no-op sayıyor (yanlış accept/reply yok).
- **Codex review (gpt-5.6-sol, staged diff): 2 P1 + 2 P2 → HEPSİ DÜZELTİLDİ + re-smoke:** (P1) send-lease canlı lease'i **overwrite** ediyordu → concurrent send TOCTOU'yu geri açıyordu → `lease_held` ile reddet (expired hâlâ overwrite); (P1) `parseConversations` fallback'i `included`-only zarfı tarayıp **sahte incoming reply** üretebiliyordu (→ yanlış stop+suppress) → recognized ref-list şart, yoksa null; (P2) lease RPC throw'u executor'da refund/skip'i atlıyordu → try/catch → `lease_error` fail-closed skip; (P2) pool import concurrent ilk-insert'te `unique_violation` fırlatıyordu → LOOP + exception → idempotent. Codex verify pass usage-limit'e takıldı → bağımsız Claude adversarial verify ajanıyla teyit (Y2 emsali).
- **KALAN (proxy sonraki fazlar):** ~~P2 + withdraw/poll lease~~ → **P2 ve lease genişletme TAMAMLANDI, aşağıdaki bölüme bak.** Kalan: P3 (IPRoyal API oto-provizyon), session epoch, prod Railway env'leri.

## PROXY P2 + LEASE GENİŞLETME + MICROSERVICE DEĞERLENDİRMESİ (2026-07-10 2. tur, orkestratör Fable + opus/sonnet ajanları)
Tümü izole test DB'de (`iehqsuludghrhosgxhnr`) smoke-doğrulı, server tsc temiz, DB temiz bırakıldı, gerçek hesap/proxy satırına dokunulmadı. **Migration'lar 110 + 111 uygulandı; sıradaki = 112.**
- **Proxy P2 (mig 110+111):** `linkedin_apply_proxy_health` — TEK txn'de hard-classifier status geçişi + static hesapta assignment retire + gen++ + **karantina** + queued `linkedin:*` iptali. `p_expected_proxy` CAS'ı yanlış-proxy karantinasını engeller (import yarışı); retire **canlı lease altında bile** uygulanır (ilk tasarımdaki "deferred retire" ASLINDA hiç uygulanmıyordu — P1 kapandı; DB satırını retire etmek uçuştaki soketi etkilemez, hesap zaten RESTRICTED). NEEDS_REAUTH proxy'ye DOKUNMAZ (cookie≠IP, P1.16). Sağlık karantinası exit-IP'yi ASLA denylist'lemez — kalıcı burn ayrı, lease-aware `linkedin_burn_proxy`'de (P1.15). `linkedin_proxy_sync_apply` — staged reconcile: incomplete = SIFIR yıkıcı değişiklik; N=3 ardışık complete-miss'te provider-gone; **zero-match floor** (dolu envanterde 0 eşleşme → `zero_match_suspicious` incomplete, miss pass atlanır — yanlış endpoint 3 günde gerçek proxy'yi söndüremez); eşleşme **ext_id VEYA exit_ip** (gerçek satır `iproyal:order:76592603` ancak exit-IP'den eşleşir — read-only kanıtlandı); per-entry subtransaction (bozuk expiry/ip run'ı ve audit satırını düşürmez); audit-id == `last_seen_sync`. `linkedin:proxy-sync` günlük self-healing loop: successor-first + **queued-only dedup + partial unique index + 23505 toleransı** (eşzamanlı çift-tetik loop'u öldüremez), 0 proxy'de durur, provider allowlist, **pagination guard** (düz+nested marker + `Link: rel="next"` → `paginated_unverified`). Adapter alanları `LIVE-VERIFY:` işaretli — tek gerçek reseller çağrısıyla teyit gerek (yanlış şekil yalnız zararsız incomplete üretir). Mig 111: import RPC **hesap→proxy kilit sırası** (health RPC ile A-B/B-A deadlock kapandı; çift-proxy advisory kilidi kanonik uuid sırasında) + canlı lease'te **`lease_active` reddi** (import uçuştaki send'in altından IP çekemez).
- **Lease genişletme (withdraw + poll):** `withStaticLease` (tek network çağrısı = tek taze lease, 45s TTL vs çok-çağrılı koşular; try/finally release; denial asla throw değil — iki job da maxAttempts=1) withdraw'ın LIST + her POST'una (dry-run LIST dahil) ve poll'un connections + conversations okumalarına sarıldı. `acquireStaticLease` artık legacy'de de RPC çağırır: `not_static_required` + gate-legacy **çifte onayı** = lease'siz devam; RPC lease verdiyse `lease_mode_transition` fail-closed skip (legacy→static flip TOCTOU 5 yolda birden kapandı — helper ortak). Poll: her iki tespit bloğu try/catch — transport throw `reschedulePoll()`'u atlayamaz (loop ölümsüz, önceden var olan maruziyet kapatıldı); audit classifier `lease_skipped:n` içerir.
- **Review zinciri (bu tur):** 2 bağımsız Claude adversarial reviewer (SQL/concurrency + fail-closed/integration lensleri) → 1 gerçek P1 (deferred-retire drop) + false-COMPLETE çifte P1 + P2/P3'ler → 8 fix ALL_PASS. Codex gpt-5.6-sol high (staged diff) → 5 P1 + 2 P2 → 5 fix + 2 dokümante sınır. Codex **verify** turu → 5/7 FIXED + 2 kalan + 2 yeni P2 → 3 küçük fix. Smoke: health 23-case + lease A-E + TS harness 18/18 **ALL_PASS**.
- **Dokümante sınırlar (bilinçli):** (1) health RPC erişilemezse (deploy penceresi/DB blip) TS fallback hesabı kilitler ama proxy karantinasız kalır — recovery validate ancak probe BAŞARILIYSA IP'yi diriltir, fail-safe. (2) Zero-match floor: TÜM envanter provider'dan gerçekten kaybolursa provider-gone hiç işaretlenmez — operatör `zero_match_suspicious` audit satırlarını görür; ölü proxy transport'ta zaten düşer. (3) Legacy→static flip'in RPC-sonrası ms penceresi: tek çağrı rotating'den çıkabilir; import validated pointer'ları sıfırladığından sonraki her send kapılıdır (tam kapatma = import/claim'in tanıdığı legacy-mode lease — bilinçli yapılmadı).
- **Microservice split değerlendirmesi:** `Tg-LinkedIn/05_MICROSERVICE_SPLIT.md` (yeni) — **ÖNERİ = O0** (şimdi split YOK; egress izolasyonunu static proxy havuzu zaten çözdü, worker drenajı var) + ~0.5g dikiş hijyeni; **O1** (yalnız `linkedin:*` çeken 2. worker servisi — `claimJob` zaten tip listesi alıyor, ~0.5-1g) tetik: >5-8 aktif hesap veya ilk gecikme incident'i; O2 gerçek müşteri prod-launch'ında; O3 yalnız compliance. **KULLANICI ONAYI BEKLİYOR** (§7 karar menüsü).
- **COMMIT'LER:** batch 1 `97c8e45` (P2 + lease + split doc), batch 2 `c0d4bf7` (session epoch + P3 auto-provision) — ikisi de origin'de.

## SESSION EPOCH + PROXY P3 AUTO-PROVISION (2026-07-10 3. tur, commit `c0d4bf7`)
İzole test DB'de smoke-doğrulı, tsc temiz, DB temiz, gerçek satırlar korundu. **Migration 112+113 uygulandı; sıradaki = 114.**
- **Session epoch (mig 112):** `linkedin_accounts.session_epoch`. Capture re-auth artık atomik `linkedin_capture_reauth` RPC (yeni cookie yazımı + `session_epoch = session_epoch+1` aynı UPDATE'te — read-then-write yarışı yok). `linkedin_apply_proxy_health` 6. param `p_expected_epoch` aldı (mig 110/111'in `p_expected_proxy` CAS + lease-live retire + karantina davranışı KORUNDU) — **yalnız NEEDS_REAUTH** epoch-gated; RESTRICTED/CHALLENGED epoch'tan bağımsız (hesap-seviyesi, cookie değil). `applyWriteHealth`/`applyWriteHealthTs` + `linkedinValidate` NEEDS_REAUTH yazımını `.eq(session_epoch)` ile korur; 0-satır = PAUSE-won tarzı no-op (gerçek status re-read, job iptal YOK). **Uçuştaki job'ın bayat-cookie 401'i, yeni epoch'a re-auth edilmiş hesabı artık düşüremez** — dokümante KNOWN RESIDUAL kapandı. Smoke 7/7 ALL_PASS. Codex session-epoch'u TEMİZ geçti (tüm NEEDS_REAUTH yolları epoch-gated, hard state cross-epoch, atomik capture, 110/111 fence'leri korunmuş).
- **Proxy P3 auto-provision (mig 113):** `linkedin_proxy_orders` + reseller client (`provision.ts`: getQuote read-only, placeOrder=para çağrısı, pollOrder). Route'lar `POST /proxies/provision` (quote vs confirm) + `GET /proxies/provision/:id` (idempotent re-poll). **HARCAMA GÜVENLİĞİ = işin özü:** placeOrder ancak `linkedin_claim_provision_order` (tenant advisory-lock'lu TEK txn: günlük cap re-check by immutable `ordered_at` + quote FOR UPDATE claim <15dk single-use) ok:true dönerse çağrılır. Cap her sayım hatasında **fail-closed**; olası-ücretlenmiş order ASLA un-count olmaz (`import_failed` sayılır); re-poll/import CAS-idempotent (`importing` claim); paid-path persistence yazımları hata durumunda **sesli** loglar (operatör reconcile). 4-katman guard: internal-role + taze single-use quote_id + günlük cap<3/24h + env. Order-create build/smoke'ta ASLA çağrılmadı; canlı read-only getQuote product 9/plan 22/loc 147 TR $4 (§11 ile aynı) teyit. Smoke H1-H11 SQL + 24/24 TS harness ALL_PASS.
- **Review:** provision money-path 3 tur — codex 5 P1/P2 → fix → codex verify 5/5 FIXED + 2 yeni (legacy backfill, paid-order persistence) → fix. Kabul sınırı: pre-call crash 1 cap slotu 24s sızdırır (çift-charge'dan iyidir, bilinçli); post-call/pre-ext_order_id crash reconcile ister (provider idempotency olmadan otomatik retry YOK).
- **KALAN (LinkedIn):** staging+worker deploy (mekanizma manuel; en son deploy 12:03, iki batch commit sonrası); prod Railway env'leri (`LINKEDIN_COOKIE_ENC_KEY`, `LINKEDIN_PROXY_ENC_KEY`, `IPROYAL_API`); canlı message+poll kanıtı (kabul edilmiş 1.derece kontak ister); microservice split kullanıcı onayı (05 doc §7).

## Faz 2/3/4/5'te KALAN
- **Canlı smoke** — gating: `LINKEDIN_COOKIE_ENC_KEY`+`LINKEDIN_APP_ORIGIN`(+ops. `VITE_LINKEDIN_EXTENSION_ID`) set → full-app staging test DB'de → gerçek hesap bağla+validate → **önce dry-run** (would_send/quota/schedule/weekly doğrula) → sonra `dry_run:false, send_now:true` tek invite (gerçek profil URN, internal role, working-hours atla) → `linkedin_actions` + hesap status izle → tek mesaj → withdraw dry-run. decorationId/trackingId + **sentInvitations/withdraw shape**'i canlı doğrula (§4.1/4.2/§2).
- **Faz-1 kalan (2)** — canlı smoke aynı gating (yukarıdaki ile birleşti).

## Sıradaki fazlar (özet — detay 00 §9)
- **Faz 2:** `linkedin:invite` + `linkedin:message` (voyager.ts'e invite=`voyagerRelationshipsDashMemberRelationships?action=verifyQuotaAndCreateV2`, message=`voyagerMessagingDashMessengerMessages?action=createMessage` — `trackingId`=16 ham byte; decorationId'ler CANLI doğrulanmalı; 200-gömülü-hata sınıflandırıcı). DRY-RUN önce.
- **Faz 3: ✅ TAMAM (kod).** günlük/haftalık limitler + warmup rampası + çalışma-saati penceresi + jitter + bekleyen-davet geri çekme + checkpoint auto-pause + gerçek Accept-Language capture. (geo = manuel connect-form alanı, uzantıdan güvenilir türetilemez.)
- **Faz 4: ✅ TAMAM (kod).** kampanya + sequence + enrollment + poll (kabul/yanıt) + workspace suppression + sender rotation + per-account serialization (Faz-3 weekly-overshoot + batch min-gap sınırları claim RPC'nin per-account pacing'iyle kapandı). `linkedin:poll`/`linkedin:sequence-tick` artık kayıtlı.
- **Faz 5: ✅ TAMAM (kod).** UI tamamlama (kampanya kurucu + enrollment durumları + hesap-sağlığı kolonları + unified inbox v2 + do-not-contact yönetimi) + uyumluluk (opt-out DELETE-guard + PII retention purge/anonymize + suppress escalation + identity-split re-check). Migration 101; `linkedin:retention` job kayıtlı. **Sıradaki mig = 102.**
- **Sonraki adaylar:** canlı staging smoke (env key + gerçek hesap → dry-run kampanya → küçük `dry_run:false`); voyager hot-surface'ları canlı doğrula (decorationId/trackingId/sentInvitations/connections/conversations); LinkedIn'i kendi microservice'ine ayır (egress izolasyonu, prod-hardening).

## Staging + mimari kararı (KİLİTLİ, 2026-07-09 — bkz. `Tg-Research-v2/00_MIMARI_PLAN.md` K10)
- **TG-Research hep ayrı kalır** (kendi Railway projesi + izole DB); lead'ler TG-Core'a devredilir; **outreach'in evi TG-Core** (cold email + LinkedIn DM + cold call).
- research-api = dar servis (full app değil), coldcall/linkedin UI'ını servis edemez. → mevcut `tg-research` Railway projesine **full-app `tg-core-staging` servisi** ekle (test DB'ye bakan). LinkedIn worker'ı mevcut research worker'ından gelir.
- Sıra: staging kur → **gerçek hesap bağla + canlı smoke** → prod-hardening (gerekirse LinkedIn kendi microservice'ine ayrılır, egress izolasyonu).

## Nasıl devam edilir (/clear sonrası)
Memory `tg-core-linkedin-automation.md` + bu doküman = tam durum. **Faz 0-5 TAMAMI COMMIT'Lİ** (0/1 `6cae4bd`, 2 `e996da7`, 3 `b226986`, 4+5 `d654e8a`). Aktif iş sırası (kullanıcı kararı 2026-07-09): **(1) `tg-core-staging` servisini Railway `tg-research` projesine kur** (full-app, test DB) → **(2) gerçek hesap bağla + canlı smoke** (dry-run kampanya → küçük `dry_run:false`; decorationId/trackingId/sentInvitations/connections/conversations shape'lerini canlı doğrula) → **(3) prod-hardening** (session epoch, egress izolasyonu / gerekirse microservice split, prod Railway env'leri). Migration: linkedin 083+093+094+095+097+101 aldı; sıradaki boş numarayı `ls supabase/migrations` ile kontrol et (paralel oturumlar numara kapıyor; 103+104 research aldı).
